# Paste into Antigravity/Gemini — C09 focused correction before publication

You are the FinPath implementation worker. Astra/Codex is the sole architect, Git publisher, and task-closure reviewer. Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`, PR [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140). Read repository `AGENTS.md`, `GEMINI.md`, `docs/execution/MASTER_REVIEW_PROMPT.md`, `docs/execution/reviews/C09-uncommitted-rework-2026-09-25.md`, `docs/execution/C09_FINAL_REWORK_PROMPT.md`, and the B11/B28 contracts. Inspect the nine existing uncommitted worker files first and preserve them. You may edit code, tests, and your submission/evidence packet; you must not run `git commit`, `git push`, `git reset`, `git checkout`, deploy, or read private credentials. Do not ask for another broad planning pass.

**Significant progress:** B35 (FIRE assumption controls) and B10 (exact saved decision restore) are accepted. 27/35 tasks are closed. The previous product code was `5dda3d2be24246e3470a65e7653a0b6e425cbece` on immutable preview `https://51acbf88.interactive-fire-calculator.pages.dev`, isolated D1 UUID `0dbad68e-7493-452f-8504-98d4c61ee5da`. Both previous synthetic users were independently verified deleted with zero rows in 15 scoped D1 tables and intact tombstones. Your latest uncommitted pass added a revision banner, scroll margin, a proposed hosted runner, 31 passing evaluator tests and one passing UI test. The primary reviewer found the remaining defects below. There is **no** new product code commit, exact-code CI, or preview for this diff yet. B11/B28 remain blocked and C10 locked.

## Repair B11: a real revision, not metadata alone

At `run_c09_proofs.mjs` around lines 1458–1486 the runner fills only version notes and label. `App.tsx` saves `buildSnapshot()` separately, so this does not show that any financial assumption changed. First add a meaningful failing behavioral/collector test. Make the UI next step honest and usable: the metadata form is for version details, while financial assumptions are edited in the FIRE calculator. Verify that the “Open calculator” action retains the selected saved plan/version inputs and offers an obvious route back to save the revised plan. If it does not, implement the smallest reliable route; do not alter `src/lib/fire.ts` or formulas. Do not automatically mutate a plan when recording a review.

For the hosted runner, select the Revise radio via UI and assert it is checked; submit using the UI and verify the persisted Version 2 review decision. Then change a real numeric assumption through the visible calculator control (for example annual expense 50,000 to a distinct valid value, or retirement age 52 to another valid value). Save Version 3 via the product UI. Prove Version 3 `snapshot.plan` or `snapshot.timeline`, its D1 `fire_plan_inputs`, and its calculated result differ appropriately from Version 2; reload the exact Version 3 deep link and verify the changed value is rendered. Compare Version 1 and 2 rows before/after across `plan_versions`, `fire_plan_inputs`, **and** `fire_plan_results`; they must remain byte-identical. Do not declare success merely because a version label or notes changed, a banner appeared, or “Version 3 loaded” text was found.

The current retry test is invalid. The UI sends no explicit idempotency key; the server derives `v2:<actual evidence date>`, while the runner retries using `review-revise-c09`. Replay using the key, evidence date, decision, notes and defer fields from the actual persisted review; verify HTTP 200 replay and exactly one revise row. Send one changed-intent request with the same key and verify structured 409 with no new row. Do not change the server's idempotency contract merely to accommodate the runner. Add collector-level negative tests for wrong retry key, unchanged financial snapshot/result, unchecked radio or unpersisted review, and absent Version 3 data. Tests that only flip final report booleans are insufficient.

## Repair B28: reduced motion and actual overlap proof

The new “Edit version details” action uses unconditional smooth scrolling. Respect `prefers-reduced-motion: reduce` and preserve keyboard focus visibility. Keep the useful scroll-margin change, but do not claim it passes from CSS inspection. Use a local browser/authorized fixture to check the sticky nav, heading and badge rectangles after deep-link/focus navigation at 1280px desktop and 320px mobile, in light and dark modes. Capture legible before/after screenshots, no overlap and no horizontal overflow. If the CSS fallback height is too small or `scrollIntoViewIfNeeded` does not use scroll margin when the panel is partially visible, correct the actual behavior. Check contrast and reduced-transparency fallback on changed elements; avoid expensive native 200% zoom and reader checks, which the owner deferred to B31/C11.

## Finish the worker packet truthfully

Run focused behavior tests, the collector tests and `./scripts/test_all.sh` once after final changes; record counts and exit codes. Run `git diff --check`. Correct `docs/execution/evidence/C09/validation-matrix.md`, `submissions/B11.md`, and `submissions/B28.md`: the current B11-11 and B28-13 PASS rows refer to a not-yet-deployed candidate and missing screenshots. Mark them **PENDING HOSTED PROOF**, with exact local evidence. Leave B11/B28 `blocked` with a precise reason; Astra will commit the product code, obtain exact-code CI, deploy an isolated preview, run one hosted synthetic proof, independently verify cleanup, and change task status only after acceptance. Do not cite the old 51ac preview or its `report.json` as proof of your changed code.

Return one concise handoff listing changed files, targeted and full test results, the exact financial input before/after that the runner will exercise, the retry key derivation and expected response, the four layout cases, remaining blockers, and the exact commands Astra should run after commit. Preserve all previous passing work. Do not touch production, paid services, DNS, production D1, main, or migrations.
