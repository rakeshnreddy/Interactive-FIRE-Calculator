# Primary observations, 2026-09-12

Local candidate at http://127.0.0.1:4174. Only website modification since reviewed 3100b70 is print-specific CSS. Screen styles and financial formulas unchanged.

Native zoom: used Chrome application controls via CUA, Command+0 then five Command+plus actions. Native accessibility control explicitly displayed `Zoom: 200%`, confirmed again after all routes. Captured home, calculators, FIRE, mortgage, savings-goal and signed-out dashboard in light/dark. Inspected all twelve screen captures and the scrolled homepage example. Visible headings, copy and controls reflow without overlap or horizontal cropping. These are actual browser-zoom observations, distinct from CSS-zoom automation. Browser zoom restored and only verification tab closed. Saved captures omit browser tab chrome for privacy. Complete-page automated reflow matrix supplies complementary layout evidence.

Print: failing real-PDF test reproduced the near-empty first page. Switching only `.landing-hero-inner` from grid to block under print removed that first-page fragmentation. Hide topbar and skip link in print; screen behavior unchanged. Four PDFs (light/dark × backgrounds on/off), two pages each: headline and engine-backed example on page one, explanatory steps and remaining links on page two. All eight rendered pages inspected: readable text and values, no clipping/overlap or blank opening sheet; unrelated navigation controls omitted. Test uses pypdf extraction to verify first-page headline, retained outcome/target, no blank content pages and omitted controls. Pixel page inspection supplements extracted text.

Contrast: revised collector hides all descendant glyphs and SVG icons, captures unchanged material background, samples every pixel under actual text-node rectangles (excluding unused rounded-button corners), composites foreground alpha and selects worst threshold-relative ratio. Missing selectors/runs and unsupported ancestor opacity throw rather than pass. Saved background samples support reproduction. Independent evaluator rejects missing/duplicate identities and invalid ratios. Contrast ratios are not rounded into acceptance. This is bounded coverage of 22 required representative text pairs, not a claim about every pixel of the site.

Media: CDP activates actual queries. Reduced-transparency checks backdrop none and alpha=1 backgrounds in both themes; reduced-motion checks computed animation and transition duration violations rather than query match alone. Forced-colors check retained. Automated report still deliberately identifies native zoom/reader as unavailable to its headless collector; this primary observation supersedes native zoom's automation-only limitation, not its raw historical result.

Actual reader smoke: owner explicitly deferred C03 on 2026-09-12. See ACCESSIBILITY_DEFERRALS.md and B31 follow-up; no full WCAG conformance claim.
