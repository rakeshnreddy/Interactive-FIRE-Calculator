# C02 independent browser evidence

Primary reviewer, 2026-09-11. Final product tree ea2614f71e7a0a615793a972967346b63657d815 (subsequent test-environment correction does not change product CSS/JS).

Local synthetic fixtures render the actual exported DashboardPanel and AccountsPanel with React 18 and compiled production CSS. No hosted auth bypass or real records. Replay capture-fixtures.tsx.txt by temporarily copying it to src/c02-browser-capture.test.tsx, running Vitest, then removing it before build. It writes /tmp/c02-browser-fixtures; serve that folder on 127.0.0.1:8771 with dist/assets copied in and the current stylesheet linked. The capture is deliberately outside the permanent test glob and published bundle. Adapt the documented local Playwright runtime path if necessary.

- browser-matrix.json: 100 Chromium cases: two panels, empty/USD/INR/mixed/long states, both themes, 320/390/612/768/1440 CSS-pixel widths. Zero document overflow or clipped tested summaries/form controls. Inspecting real screenshots exposed INR account rows mislabeled with dollars; currency-row-red.log proves regression assertions failed, green proves repair. Actual account currency now feeds snapshot/card/history formatting.
- layout-red.log reproduces account form controls beyond the viewport at intermediate widths. Replaced the fixed seven-column minimum with auto-fitting 160px columns; mobile breakpoint remains. layout-green.log: 100 cases, zero failures.
- Native Google Chrome window: reset zoom then five increments; accessibility toolbar explicitly reports Zoom: 200%. Dashboard mixed and accounts long in light/dark have readable summaries and wrapping form. Tab reaches Reports with visible ring; account fields receive focus in order. native-*.png and native-zoom.txt record actual zoom, not device scale or CSS zoom. Restored 100% and closed review window.
- material-matrix.json: reduced motion and reduced transparency both actually matched; summary surfaces opaque with backdrop none in both themes, no overflow, visible 3px keyboard focus. Normal summaries use the same opaque surfaces. Muted summary text RGB82,104,115 on251,253,255 (light) and164,187,196 on16,35,44 (dark); no gradient under summary digits. No new motion, chart or palette introduced.
- Hosted results: actual landing/library/FIRE and protected account/dashboard gates rendered on mobile without page/console errors; health 200, three protected APIs 401, delivered JS/CSS references match dist. Public-route smoke84/84. Full signed-in lifecycle remains B06/C07; static fixtures do not prove event handlers, imports or hosted persistence. Coordinator/server integration tests supply C02 save behavior evidence.

The earlier static unit-test claims of viewport/zoom passes and React 19 were corrected in B03/ui-verification.md. Screenshots are observed evidence for bounded C02 changes, not whole-product WCAG certification. Existing large main chunk and landing/auth copy work remain in their assigned later tasks.

CI diagnosis: ea2614f failed because the SQLite server integration test inherited jsdom and Vite on Node 22 attempted to bundle node:sqlite. Reproduced locally under Node 22 (node22-red.log); adding @vitest-environment node to that test passes25 cases (node22-green.log). final-node22-suite.log is the full 1325-test rerun under CI's Node major, exit0. No production code or dependencies changed for this repair. Hosted CI remains the final gate.

Final common candidate `ef2cded6441191a26537adf8ddf1a3e1909cf73b` passed exact-candidate CI 34601435599 and is deployed at https://51c5e746.interactive-fire-calculator.pages.dev. final-binding.json, final-public-smoke.log and final-hosted-results.json supersede the earlier4bc02589 publication evidence; same product assets, verified anew on the final URL.
