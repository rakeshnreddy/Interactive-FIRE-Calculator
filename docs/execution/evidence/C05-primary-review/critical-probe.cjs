const {chromium}=require('/Users/Rakesh/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});try{const page=await browser.newPage();await page.goto('https://f51c818b.interactive-fire-calculator.pages.dev/calculators/fire');await page.locator('.core-fire-form').waitFor();const ids=await page.evaluate(()=>{const all=[...document.querySelectorAll('[id]')];return [...new Set(all.map(e=>e.id))].map(id=>({id,count:all.filter(e=>e.id===id).length})).filter(x=>x.count>1)});const help=page.getByRole('button',{name:'Help for Current age',exact:true});await help.focus();await page.keyboard.press('Enter');await page.keyboard.press('Enter');await page.waitForTimeout(300);const helpState=await help.evaluate(el=>({expanded:el.getAttribute('aria-expanded'),opacity:getComputedStyle(el.nextElementSibling).opacity,text:el.nextElementSibling.textContent}));console.log(JSON.stringify({duplicateIds:ids,closedHelp:helpState},null,2));await page.locator('.advanced-shell > summary').click();const labels=page.locator('label[for="field-years"]');if(await labels.count()>1){await labels.nth(1).click();console.log('secondYearsLabelFocus',await page.evaluate(()=>({id:document.activeElement.id,firstInput:document.activeElement===document.querySelector('#field-years')})))} }finally{await browser.close()}})().catch(e=>{console.error(e);process.exit(1)});
