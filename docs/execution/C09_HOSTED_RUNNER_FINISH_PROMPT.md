# Paste into Antigravity/Gemini — C09 hosted-runner final correction

You are FinPath's implementation worker. Astra/Codex remains sole architect, Git publisher, hosted verifier and task-closure reviewer. Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`, PR #140. Read `AGENTS.md`, `GEMINI.md`, `docs/execution/MASTER_REVIEW_PROMPT.md`, `docs/execution/reviews/C09-hosted-runner-readiness-2026-09-25.md`, and the B11/B28 prompts, matrix and submissions. **Preserve all existing uncommitted work.** Do not commit, push, reset, checkout, deploy, read credentials, write hosted D1, touch main/production, or mark tasks done. This is the owner's manual Antigravity workflow, not a request to invoke Gemini CLI.

Significant progress already accepted: B35 and B10, 27/35 tasks done. Your present uncommitted repair fixed the original selector/schema/fixture defects and independently passes 47 collector tests. **Do not repeat general redesign or rereview.** Four exact hosted-runner blockers remain; address them together with tests that reproduce the real sequence.

1. **Truthful synthetic seed:** In `docs/execution/evidence/C09/run_c09_proofs.mjs`, Version 1 and Version 2 setup currently POST/PUT fake `result` objects with `fireNumber` and no `requiredPortfolio`. The new diff verifier therefore fails on the runner's own Version 2. Generate `result` from the existing `calculateFirePlan(snapshot.plan)` in `src/lib/fire.ts` for both versions; Node 24 on this checkout can import that TypeScript module (verify with a focused command), or use an equally source-backed method. Pass the complete result to `/api/plans` and `/api/plans/:id`. Do not hardcode 1.25m or change formulas. Add a test that constructs the same v2 seed as the hosted runner, serializes it exactly as D1 does (`JSON.stringify(payload.result)`), and proves the raw-schema verifier accepts the v2-to-v3 change only when both financial input and computed target changed. Include negative coverage for the old fake result shape.
2. **Correct review-row scope:** The hosted sequence deliberately creates Version 1 `defer` and `keep` rows before the Version 2 `revise` row. After a conflicting replay the total valid plan history is three rows, not one. Query by plan/user and the persisted revise row's version/key (or exact ID) and require one original revise row; independently compare total plan review count before/after the conflict, requiring it remain three in this scenario. The conflict helper must fail if `postConflictReview` is missing, if the original row's decision/key/payload changes, or if a duplicate matching row appears. Add a collector test with all three real rows and a duplicate-after-conflict negative. Do not delete the prior reviews to make the test pass.
3. **Fresh deployment inputs:** The runner currently pins the old `5dda3d2` candidate, `51acbf88` deployment ID and URL. Replace these with strict runtime inputs such as `C09_CANDIDATE_SHA`, `C09_DEPLOYMENT_ID`, `C09_PREVIEW_URL`, validated at hosted entry. No stale default is allowed when creating synthetic users or writing D1. Unit-test missing/malformed inputs and mismatched Cloudflare deployment metadata. The reviewer must be able to commit once, then pass those three values for the new immutable preview **without modifying tracked source**. Keep preview D1 UUID `0dbad68e-7493-452f-8504-98d4c61ee5da` fixed; the preflight must prove it and preview environment before writes. Preserve old report provenance rather than rewriting it as new evidence.
4. **Actual theme/focus verification:** In the hosted clearance block, stop directly changing `.app[data-mode]` and writing nonexistent storage key `finpath:theme-preference`. Reuse `switchTheme(page, 'light'|'dark')` or click the real theme control; assert `.app[data-mode]` and computed theme tokens for each of four viewport/theme cases. Stop focusing an unfocusable `h2` and swallowing errors. Navigate by keyboard to a real review control, assert it is `document.activeElement`, and assert that control and heading/badge are clear of the sticky topbar. If this proof cannot be instrumented honestly, leave B28-13 pending rather than recording pass. Native 200% zoom and actual screen reader remain deferred to B31/C11.

Before handoff, run focused tests, the 47+ collector suite, `./scripts/test_all.sh`, and `git diff --check`. Update `docs/execution/evidence/C09/validation-matrix.md` and B11/B28 submissions with exact new counts and honest pending hosted status. Do not cite 51ac as proof of changed code. Return a concise list of changed files, commands/exit codes, a local test of the **real three-review sequence**, exact runtime variables needed after deployment (names only, never values or secrets), and any remaining blocker. Astra will commit/push only after checking the repaired diff, verify exact-code CI, deploy an isolated preview, run the hosted synthetic proof, independently verify cleanup/tombstones and decide closure.
