# B40 — First calculator-excellence child: library and copy consolidation

Role: Gemini worker; Astra defines subsequent per-calculator contracts and closes tasks. **C14 is queued after C11; not released.** Read `CALCULATOR_EXCELLENCE_PROGRAM.md`, current route inventory, PRODUCT.md, DESIGN.md, B09 review, RESUME.md and the protocol. Do not begin implementation before primary release.

## Bounded user problem

Public discovery repeats loan/EMI variants and formulaic descriptions; two tax entries lack clear jurisdiction, internal terms leak into visible copy, and signed-out navigation still offers Workspace. This first child makes finding the right existing tool simpler without changing calculator math. Record the separate mortgage PITI/scenario-assumption and compound-interest 500% findings in the program's per-route gap inventory for later explicit child contracts. Those are real findings but are **not silently accepted or repaired by this library task**; they require route-specific formula/validation review. Preserve every stable public URL as an alias or redirect.

## Scope and acceptance

- Identify substantive calculator engines versus aliases in a current route table. Consolidate EMI/home/car/personal/auto-loan discovery into honest presets where they share a model; document differences rather than merging incompatible formulas. Consolidate Debt Payoff/Snowball vs Avalanche entry points similarly. Keep old paths working and route smoke green.
- Add clear US/India scope badges and a region filter without user-facing SEO or internal targeting language. Replace templated descriptions with concise question-led copy. Remove visible internal phrases including “Model v2,” “at standard USD display precision,” “account-backed planning shell,” and “Need to number” through a copy-guard regression. Give orphaned figures such as “Time saved 0” an explicit unit or remove them when unsupported. Signed-out navigation shows public choices and an understandable sign-in action.
- Test route alias/preset state preservation, region filter/search/empty state, keyboard navigation, accessible names and normal-size desktop/375px in both themes. PA-7 walkthrough asks whether a new visitor finds a fitting calculator quickly. No auth bypass or existing formula change.
- Create explicit queued follow-on gap rows for mortgage full monthly housing cost (PITI/PMI with assumptions), misleading mortgage scenarios, rate-range warnings and other per-calculator defects. They remain open until later reviewed tasks with formula sources and goldens; B40 closure does not claim all calculators complete.

Analytics: no new collection before B12 consent. Privacy: signed-out public access preserved. Dependencies: B31/B36 and primary release of C14 after C11. Non-goals: mortgage/compound engine changes, tax-law modeling, bulk calculator rewrite. Rollback: restore discovery UI while preserving stable aliases. Human 1–2 days, agent estimate after route inventory. Submit matrix and `submissions/B40.md`.
