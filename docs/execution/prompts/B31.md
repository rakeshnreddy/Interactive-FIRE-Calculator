# B31 — Close visual accessibility and performance acceptance matrix

Owner sequencing amendment 2026-09-26: C11 order is **B41 → B38 → B12 → B31**. B31 additionally depends on accepted B36, B38 and B41; read `CHECKPOINTS.md`, `TASK_STATUS.json`, `RESUME.md` and the dated protocol before work. Use PA-1's shared runner and PA-4 storage, then perform the consolidated actual native 200% zoom and real screen-reader checks deferred from earlier checkpoints. Older boilerplate below cannot release B31 early or authorize a new bespoke hosted harness.

Role: implementing model. Deliver a small, verified change for primary-reviewer acceptance. You do not own task closure.

Checkpoint: **C11**. Prerequisites: **B18, B19, B20, B21, B22, B23, B24, B26, B27, B28, B29, B30, B12**. Confirm checkpoint release in CHECKPOINTS.md before writing code. B01 is already complete and must not be redone.

## Task contract

- [ ] **B31 — Planned: Close visual accessibility and performance acceptance matrix.**
  - User problem/evidence: Individual polished screens do not prove a coherent accessible product. Visual audit V16.
  - Expected outcome/scope: All objective visual gates have evidence; no unresolved major issue; subjective rubric justified; full suite/CI/preview verified at submitted code SHA.
  - Non-goals: No production launch, waived missing tests, blanket WCAG certification or new broad refactor.
  - Files likely affected: docs/design validation matrix; targeted UI/styles fixes only; existing tests; build/evidence reports.
  - Acceptance/tests: follow the task-specific prompt and VISUAL_DESIGN_SPEC; preserve engine/API regressions; verify changed UI with actual browser evidence and full suite before push.
  - Analytics: no new collection; measure task completion/readability with synthetic checks, use only B12-approved events if already implemented.
  - Security/privacy: public tools stay public; no real records or bypass; hosted writes require B06 isolation; no secrets in output.
  - Dependencies: B18, B19, B20, B21, B22, B23, B24, B26, B27, B28, B29, B30, B12. Checkpoint release is additionally required.
  - Migration/rollback: no schema in visual work; revert only this task's reviewed commits; preserve safety fixes and additive data migrations. Human 2–3 days; agent 6–12 hours, estimates excluding review/provider waits.
  - Detailed implementer prompt: [prompts/B31.md](B31.md).


Additional prerequisite: B32 accepted. Use COLOR_AND_GLASS_SYSTEM.md for theme/material authority.

## Read in addition to the shared protocol

- `docs/CURRENT_STATE_AUDIT.md` and `docs/TECHNICAL_AND_SECURITY_ROADMAP.md` for known data/auth risks.
- `docs/VISUAL_AND_UI_AUDIT.md` and `docs/VISUAL_DESIGN_SPEC.md` for observed defects and target composition.
- Existing source files/tests named above; follow imports to the actual implementation if files moved. A stale path is not permission to create a duplicate implementation.
- The relevant strategy/measurement document for product or lifecycle changes. B06 reads PRODUCTION_AUTH_RUNBOOK.md; B13 reads RETENTION_AND_MONETIZATION_PLAN.md; B14 reads COMPANION_APP_DECISION.md.

## Implementation sequence

1. Enumerate all public routes and authenticated journey states; record real renderer family coverage, not just HTTP status.
2. Run VISUAL_DESIGN_SPEC gates across required widths/themes, 200% actual zoom, keyboard, screen-reader smoke, reduced motion/transparency and long-value/error states.
3. Compare against accepted checkpoint screenshots; fix bounded visual regressions with tests where behavior changes. Large new findings return to reviewer for a task amendment.
4. Collect three-run reproducible before/after lab performance, entry/chunk sizes, console errors and all public-route smoke. Separate lab from unavailable field metrics.
5. Verify actual isolated hosted save/review/export/deletion journey and confirm production guard still blocks unconfigured production.
6. Assemble final scorecard and issue list. Mark READY_FOR_REVIEW; only master can close, approve final design or discuss launch readiness.

## Required working rules (read, do not infer permission from tool access)

- Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`; inspect actual branch, HEAD, remotes, working tree and applicable AGENTS.md first. Read `docs/COLOR_AND_GLASS_SYSTEM.md` (latest color authority), `docs/execution/README.md`, `CHECKPOINTS.md`, `TASK_STATUS.json`, this prompt, PRODUCT.md, DESIGN.md and the linked audit/spec. Read relevant current code and tests before editing. Never assume the handoff SHA or preview is still current.
- User authorization covers bounded implementation, local tests, small commits, pushing a codex/ branch and Cloudflare previews after tests. It does not cover merging main, production deployment/data writes, DNS, purchases, financial connections, live payments, messages to others or new retention/legal policy. Never bypass fail-closed auth preflight. Do not print credentials or user records.
- Use the dedicated execution branch assigned by the protocol. One writer at a time; preserve unrelated changes. Never force-push, reset another task or auto-stash user work. Read prerequisite submissions and reviewer records, not just a checkbox.
- Behavioral change: write a meaningful failing test first, implement the smallest correct fix, then prove green. CSS-only change: capture the defect and before/after computed styles/screenshots; do not add tests that merely assert CSS strings. Never weaken tests to make them pass. Formula edits require cited references, regression tests and a written migration decision; `src/lib/fire.ts` remains untouched unless a separately approved task explicitly authorizes it.
- Keep routes, public access, privacy boundaries and exact financial units. New dependencies require a concrete justification and reviewer decision; prefer existing packages/native controls. No analytics outside the B12 contract. No synthetic fixture auth path in the production bundle.
- For UI work apply every relevant gate in `docs/VISUAL_DESIGN_SPEC.md`: desktop/tablet/mobile/320px, light/dark, keyboard, real 200% zoom, reduced motion, no overlap, readable digits, error/empty/loading states and screen-reader smoke. State precisely which evidence is unavailable; do not claim pass. Never treat HTTP200 as rendered UI proof.

## Mandatory verification and submission

1. Run relevant task tests after each meaningful change. Before pushing run `./scripts/test_all.sh` from repository root; capture command, exit status, counts and log path. Run `npm audit` for dependencies (CI also audits). Do not substitute only TypeScript/build for full verification.
Before any backend/functions/auth/migration code push or deployment, B33 must be accepted and effective isolation rechecked; this includes automatic Git deployments. Without it, keep changes local and report BLOCKED publication. B33 bootstrap exception: with explicit owner authorization, publish configuration only using unchanged, identified backend code. Verify intended isolated preview binding before publication and effective deployed binding afterward. This is the narrow setup operation needed to prove isolation, not permission to publish changed APIs or perform financial writes. Those remain blocked until B33 is accepted. If the provider cannot establish the intended binding safely before publication, stop and request a separate isolated target.

2. Run `git diff --check`, inspect staged diff, commit this task separately. Push only the assigned codex/ branch. Create/update the checkpoint PR with exact problem, solution, tests, limitations and task IDs. Keep it unmerged.
3. After tests pass, use an explicit preview branch: `npx wrangler pages deploy dist --project-name interactive-fire-calculator --branch <actual-codex-branch>`. Never use a production/default-branch deployment shortcut. Verify effective DB isolation before any hosted write. If deployment credentials/tooling fail, submit a blocked release evidence item, not a fabricated preview.
4. Run `npm run smoke:calculators -- <immutable-preview-url>` and separately render/exercise the changed journey in the browser. Check CI on the exact submitted code commit, console errors and relevant protected boundaries. Public smoke verifies delivery, not all calculator math.
5. Copy `docs/execution/SUBMISSION_TEMPLATE.md` to `docs/execution/submissions/<TASK_ID>.md`. Fill every criterion with pass/fail/blocked plus direct evidence; record baseline and final code SHA, PR URL, immutable preview, screenshots, test logs, actual known limits, files touched, rollback and next task. Use synthetic data only.
6. Set only your task's status to `ready_for_review` (or `blocked` with exact reason), and populate submission/owner/code_sha fields in TASK_STATUS.json. You must NOT set `done`, check the master backlog checkbox, edit reviews, release checkpoints or approve yourself. This rule is an operational review contract, not a technical security boundary.
7. Within the released checkpoint, proceed sequentially only when the next task's prerequisites are accepted or earlier tasks in this same checkpoint have complete ready-for-review evidence. No cross-checkpoint assumption of approval. At the checkpoint boundary stop and hand off the review packet. Master review closes tasks and releases the next checkpoint.

## Escalation and quality

If a prerequisite, secret, owner decision, migration authorization or required verification is absent, record exactly what is missing. Continue independent work within the released checkpoint if it cannot compromise the blocked item; otherwise stop. Do not expand scope, fake hosted authentication or mark blocked tests as not applicable. After three failed attempts at the same problem, return reproducible evidence and a bounded recommended next step. UI screenshots should be actual product/fixture renders, not invented mockups. Success is accepted user behavior and visual quality, not lines changed or a green screenshot snapshot you regenerated yourself.

Task-specific evidence packet: `docs/execution/submissions/B31.md`. Review checkpoint: C11.

Checkpoint final-candidate rule: recheck all applicable criteria of every task in this checkpoint against the final candidate code SHA and immutable preview. If a later task changes an earlier provisional submission, retest it and update evidence. Earlier green task SHAs alone do not prove the combined checkpoint passes.

## Required deferred verification follow-up

Complete the actual screen-reader/VoiceOver check deferred by the owner during C01T. Read [DEFERRED_CHECKS.md](../DEFERRED_CHECKS.md). Record actual reader/browser, actions, announcements, defects and retests. C01T acceptance does not satisfy this requirement. If unavailable, report blocked to the primary reviewer; do not substitute an accessibility tree or mark PASS.


## Owner amendment — 2026-09-13: actual screen-reader checks deferred to B31/C11

The owner explicitly directed: “defer the screen reader … add it to later tasks so that it doesn't block other implementations … deprioritize it for now till we get to closing most of the other important tasks”. This supersedes the earlier C03-only scope. Actual VoiceOver/NVDA smoke checks for C04 and subsequent implementation checkpoints through C10 are DEFERRED to B31/C11, not passed. Their absence alone must not block implementation, task acceptance or the next otherwise-qualified checkpoint. Other acceptance requirements remain in force, including keyboard operation, semantic names/roles, text alternatives, contrast, native zoom and responsive behavior. Discovered accessibility defects are not waived by deferring reader testing. No full WCAG-conformance claim is permitted on this evidence.

Execution owner: the lower-cost implementing/verification agent with actual reader and interactive browser capability. Primary reviewer owns targeted independent review and final closure; routine execution need not use the primary model. Use free VoiceOver/NVDA tooling. Do not consume primary-model browser time merely because a worker lacks a tool; record the capability gap and schedule a capable worker for B31.

Follow-up A11Y-DEFERRED (B31/C11): consolidate the B32, C03 and C04 deferred routes plus all subsequently changed surfaces. Maintain a route/control inventory as work proceeds. After the main implementation checkpoints, test actual announcements and navigation for homepage/discovery, calculator inputs/results/help/tables, both FIRE modes, and authorized synthetic authenticated workflows (dashboard, transactions, saved plans, reports and settings). Cover meaningful reading order, unique names, error association, dynamic updates, disclosures and chart alternatives. Record browser/reader versions, exact candidate SHA, route, actions, expected and observed announcements, defects and retests. An accessibility tree alone is not reader proof. Close only after the real checks pass; if unavailable, B31 retains the exact blocker while earlier implementation acceptance remains valid.


## Owner amendment — 2026-09-15: consolidate zoom verification at B31/C11

The owner directed: “push it to the end of verification of all tasks … if there is any zoom issue it can be fixed later.” Native browser 200% zoom checks and zoom-specific layout repairs are therefore DEFERRED through C10 to the final B31/C11 verification. Their absence or a known zoom-only issue must not block otherwise passing implementation tasks or checkpoint release. This supersedes earlier per-task native zoom gates, including older prompt/contract language. Do not rerun native zoom at every checkpoint. Preserve existing evidence and record newly noticed zoom defects without spending implementation time on them now. Functional correctness, tenancy, deletion, auth boundaries, normal-size usability, mobile layouts, keyboard, contrast and reduced-motion/transparency checks remain required. Deferred means not passed; no full WCAG-conformance claim.

B31/C11 follow-up ZOOM-FINAL: a lower-cost capable agent performs one consolidated actual-browser 200% sweep of final public and authorized synthetic authenticated journeys, in both themes. Verify reachable essential controls, readable inputs/results, no text overlap or clipped actions, and reflow. Record browser version, actual zoom level, exact candidate, route, screenshot, defect and focused retest. Pixel density, CSS zoom and viewport resizing do not substitute for native browser zoom evidence. Primary reviews the evidence and closes the task. Existing C06 account-import/fixture toolbar repairs and screenshots are retained; repeat only if the final sweep finds a regression.
