# B42 — Shared hosted smoke runner plus the C09 revise residual

Role: Gemini implementation worker. Astra alone publishes, deploys, executes hosted writes, records hosted outcomes and accepts B42. Start only after B37 and B39 are accepted in `TASK_STATUS.json` and C09B remains released. Read `GEMINI.md`, PA-1/PA-10 in `IMPLEMENTATION_AND_VALIDATION_PROTOCOL.md`, the C09 closure review and B37 first review. Use the current branch and HEAD, not the old C09 candidate. The C09 Version 2 revise → Version 3 journey and downstream B28 hosted checks are **UNOBSERVED**, not previously passed.

## User problem and result

Bespoke proof scripts and a B37 preflight stub created false confidence. Build one reusable `scripts/hosted_smoke.mjs` with injectable adapters for (1) Cloudflare deployment metadata and effective Pages Functions D1 binding, (2) Clerk development user/session lifecycle, (3) a real browser, and (4) D1 queries. The worker delivers implementation and local fail-closed tests. A later Astra-hosted run against an exact-code isolated preview establishes the live result. Do not create a second checkpoint-specific runner.

## Worker implementation contract

- No default or hard-coded candidate SHA, immutable preview URL, deployment ID, D1 UUID, or credentials in the execution path. Missing input, missing credentials, unavailable metadata or mismatched values exit nonzero with no hosted PASS or synthetic write. Accept a verified immutable preview hostname for the exact deployment; reject project-root/branch aliases, production hosts and production D1. Query provider metadata rather than comparing user-supplied values with each other. Before any user creation/write, verify successful preview deployment, project, full deployed SHA, deployment ID, hostname and effective isolated `finpath-preview` binding. Keep secrets out of stdout, files and child-process command arguments.
- The worker must not access credentials, call the live deployment, create real Clerk users, or make remote D1 writes. Design adapters so tests use explicit fakes and the live adapters run only when Astra invokes the reviewed CLI in its authorized environment. Preserve the B37 hygiene commit; integrate the existing untracked stub only after auditing it, and remove every false-success path.
- Model one bounded scenario with two disposable synthetic tenants. Create a goal and saved FIRE Versions 1 and 2 using current API contracts and real `calculateFirePlan` outputs. After any approved scoped synthetic date setup, load the exact Version 2 deep link. Wait for Clerk to finish loading and for a signed-in session; transient loading is not terminal. Require the URL and hydrated plan ID/version, one visible review panel, and a non-empty exact-version overview. Choose Revise in the real UI, observe an explicit next-step banner, change annual expense from 50,000 to 45,000, save and hard-reload Version 3, compare the displayed value and financial result to D1, and prove the Version 1/2 `plan_versions`, `fire_plan_inputs` and `fire_plan_results` rows are unchanged. Tenant B must be denied User A's plan/review. Then observe the downstream B28 dashboard/goals evidence, light/dark, desktop/375px, keyboard/focus and normal-size layout criteria without inventing a PASS for a skipped stage. Native 200% zoom and actual screen-reader checks remain deferred to B31/C11.
- Derive the user-scoped cleanup table list from the repository migrations, assert the exact expected **15** tables (currently `plan_reviews`, `user_profiles`, `financial_accounts`, `account_balances`, `transactions`, `goals`, `plans`, `plan_versions`, `fire_plan_inputs`, `fire_plan_results`, `assumptions`, `balance_imports`, `transaction_imports`, `saved_calculator_results`, `audit_log`). `users` is a retained tombstone and must be checked separately for non-null `deleted_at`. Cleanup runs in `finally`: delete scoped app data, require one finite integer zero count from every expected table, verify tombstone, then delete the synthetic Clerk user and confirm 404. Missing, malformed or nonzero count, failed app deletion or uncertain provider deletion means failure and a safe cleanup report; never a PASS.
- Use a small reusable scenario interface and separate provider adapters. PA-1 targets at most 150 new hosted-proof lines per checkpoint; document specific necessity if this scenario requires more. No finance formula change, production migration, paid service, real-user record, or production data access.

## Local verification and handoff

Tests must exercise the actual runner orchestration with injected fakes, not only pure helper functions. Mandatory negatives: no credentials; Cloudflare API failure; wrong project/environment/status/SHA/deployment/hostname/DB; transient Clerk loading then success; settled signed-out Clerk; wrong route plan ID/version; empty, duplicate or missing panel; no Version 3; unchanged financial assumption/result; mutated prior versions; foreign-tenant 200; missing/non-numeric D1 count; failed app deletion; failed provider deletion. Every fatal test asserts nonzero exit/no PASS and no write before verified preflight. Include one real local Chromium delayed-hydration test. Run the focused tests and full Python-free suite, typecheck/build, packet validator and diff checks. Supply a criterion matrix, file list, red/green test evidence, safe dry-run command showing missing inputs fail, and an Astra-only hosted command template with placeholders for the **future** exact candidate SHA, deployment ID, immutable URL and preview DB. Never paste old C09 values as a runnable B42 command.

**PA-10 is absolute:** the worker never produces, simulates, or labels live/hosted results. Every authority-dependent step is `NOT RUN — requires Astra`, with exact command and safety prerequisites. Any worker-authored hosted PASS is automatic rejection. Stop with `ready_for_review` for locally complete implementation or `blocked` with a precise local blocker; do not mark `done`, deploy, merge or release B36. Astra alone commits/pushes a reviewed candidate, checks exact-code CI, deploys a fresh isolated preview, performs the hosted run and independently verifies cleanup before deciding B42. A real product defect found there becomes a scoped defect task, not a forced PASS.

Analytics: none. Privacy: disposable synthetic identities only, no secrets or user records in artifacts. Dependencies: B37 and B39 accepted, C09B released, isolated preview binding verified. Rollback: revert runner and tests without changing product or D1 schema. Human 0.5–1 day; Gemini 4–8 hours for code/local tests, excluding Astra hosted execution and review. Submit `docs/execution/submissions/B42.md` and stop.
