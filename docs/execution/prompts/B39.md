# B39 — Extract pure modules from App.tsx

Role: Gemini worker; Astra alone publishes and closes. **C09B is released; B37 must be accepted before this second task starts.** Read the shared protocol, RESUME.md, current source/tests and B37 review. Do not commit, push or touch production.

## Problem, boundaries and intended result

`src/App.tsx` is 8,841 lines at the 2026-09-26 baseline. B36, B29 and B30 will otherwise add to this file. Extract pure code without changing rendered behavior: API request/client helpers and `toX` DTO parsers (inspect actual symbols around old lines 689–1885) to `src/lib/api/`; formatting/CSV/JSON exports (old 2012–2372) to `src/lib/format.ts` and `src/lib/csv.ts`; warning derivation (old 2388–2625) to `src/lib/warnings.ts`; and actually shared `Metric`, `InfoTip`, `Field`, `YearByYearTable` primitives (old 2625–2873) to `src/components/`. File/line references are leads, not proof of current ownership. Add one typed `readApiJson` helper to eliminate repeated `response.json().catch`/error parsing while preserving status, error and retry semantics.

Do not extract stateful panels, routes, authentication boundaries or financial formulas. PA-9 does not demand stateful panel extraction here because this task explicitly opts out; later feature edits must move their touched region unless their prompt opts out. No copy, CSS, result, request-shape or export-format change. Keep exported helper contracts and test fixtures stable; test edits should be import-path-only unless a precise compatibility reason is documented.

## Acceptance

- `App.tsx` shrinks by at least 2,000 measured lines from its 8,841-line baseline without moving code into one equally oversized catch-all file.
- API mapping, warning, formatting, CSV/JSON export and UI primitive tests pass unchanged in behavior. Add tests for the consolidated response parser's success, malformed body, non-JSON error and auth failure behavior; no swallowed error.
- Compare rendered public route/fixture HTML and relevant CSS before/after; no user-visible change. Review at normal desktop and 375px per PA-7.
- Full suite, typecheck, build and all public-route smoke pass; exact-code CI and immutable isolated preview are required before acceptance.

Analytics: none. Security/privacy: preserve auth/tenant token handling and avoid recording response bodies. Dependencies: B37 accepted and C09B released. Rollback: revert this refactor commit without schema impact. Human 1 day, agent 2–4 hours. Submit matrix and `submissions/B39.md`; Astra reviews the actual diff and closes only on parity.
