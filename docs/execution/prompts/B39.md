# B39 — Extract pure modules from App.tsx (Gemini implementation prompt)

Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`, starting from the B37-approved branch. B37 initial candidate `0c6e35c` passed the Python-free suite and exact-code CI; final two-file manifest verification fix `83bc1a27b54203a3d30e85b85a042c5b308190fe` passed post-commit manifest checks. C09B is released and B39 is next, followed by B42 and B36. Read `GEMINI.md`, `docs/execution/IMPLEMENTATION_AND_VALIDATION_PROTOCOL.md` (especially PA-7, PA-9 and PA-10), `docs/execution/RESUME.md`, this contract, and current source/tests. Preserve the two existing untracked `scripts/hosted_smoke.mjs` and `scripts/hosted_smoke.test.mjs` files byte-for-byte: they belong to B42. You implement and test locally. Astra alone commits, pushes, verifies CI, deploys an isolated preview, runs hosted smoke and closes B39. Do not claim a hosted PASS or perform remote writes. Do not mark B39 done, release B42, merge main or touch production.

## User problem and exact scope

`src/App.tsx` is **8,841 lines** at the B37 candidate and mixes React state/rendering with pure API parsing, formatting, warnings, exports and reusable UI primitives. This obstructs review and makes the upcoming FIRE and report changes risky. Make the pure boundaries clear while keeping the experience, API contract and financial outputs byte-for-byte equivalent where they are serialized.

Inspect actual declarations and call sites before moving anything; old line ranges are hints only. Extract cohesive units:

1. API request/client functions and `toX` DTO parsers (historically around lines 689–1885) into small purpose-named modules under `src/lib/api/`. Preserve URL, method, headers, Clerk token handling, status handling, error messages and retry behavior. Introduce one typed `readApiJson` helper only if it can replace repeated parsing without changing the caller's observable behavior. Include negative tests for malformed JSON, non-JSON server errors, 401/403, 404/410 and empty successful bodies where callers currently handle them; never log raw response bodies or auth tokens.
2. Pure formatting and CSV/JSON export generation (historically around lines 2012–2372) into `src/lib/format.ts`, `src/lib/csv.ts` or equally focused names. Preserve locale/currency formatting, column order, headers, quoting, filenames and serialized output. Add or reuse fixture/golden assertions for representative US/India/zero/large-value cases. Do not change financial formulas.
3. Pure warning derivation (historically around lines 2388–2625) into `src/lib/warnings.ts` with existing warning order/severity/text and conditions preserved. Add or reuse assertions for edge/error cases.
4. Only actually shared presentational primitives (`Metric`, `InfoTip`, `Field`, `YearByYearTable`, historically around lines 2625–2873) into individually focused `src/components/` files. Keep DOM structure, semantics, classes, accessibility names, disclosure/focus behavior and CSS unchanged. Do not move stateful panels, routes or auth providers. This task explicitly opts out of PA-9 stateful-panel extraction; do not turn the extraction into a UI redesign.

Do not move copied blocks into one new giant file. Avoid cycles between API modules, components and `App.tsx`; keep exported contracts typed and stable. Preserve existing fixture behavior and edit existing tests only for import paths unless a tested compatibility reason requires more.

## Execution and local verification

1. Before edits, record `wc -l src/App.tsx` and an inventory of symbols/callers, modules, existing tests and potential circular imports. Capture deterministic before-state for `/`, `/calculators/fire`, one calculator detail route and signed-out `/dashboard` at normal desktop and 375px (local browser or rendered fixture): main text/DOM landmarks, relevant accessibility names, console and overflow. Do not treat screenshots alone as parity proof.
2. Move one cohesive group at a time; typecheck and run its focused tests after each group. Keep request/response and export snapshots before/after so regressions are visible. The `readApiJson` change is behavioral risk even if intended as a refactor: write tests first for its actual error/success contract and demonstrate old call-site behavior survives.
3. Final `App.tsx` must be **≤6,841 lines** (at least 2,000 fewer than 8,841) without an equally oversized replacement. Record per-module line counts and import graph/cycle check. `src/lib/fire.ts`, shared formulas, migrations, Functions, CSS and product copy stay untouched unless a minimal import-path-only edit is required; explain any exception before claiming acceptance.
4. Run focused parser/warning/format/export/component tests, `./scripts/test_all.sh` (Python-free), TypeScript, Vite production build and local public-route smoke where available. Compare the same before/after desktop and 375px routes for visible output, keyboard focus, overflow, console errors and actual answer quality. Preserve signed-out auth gate behavior. Run `git diff --check` and `python3 docs/execution/validate_packet.py` separately (packet validator is a governance tool, not part of the Python-free product suite). No native 200% zoom or screen-reader pass is required here; those remain deferred to B31/C11.
5. Write `docs/execution/submissions/B39.md` and a compact criterion matrix with exact changed files, baseline/after line counts, test commands and exit codes, before/after parity evidence, known differences, security review of token/error handling, and rollback. Keep raw output in ignored storage or small durable evidence; do not commit generated bulk screenshots. Mark exact-code CI, isolated preview deployment, hosted smoke and any remote writes **NOT RUN — requires Astra**, with the exact reviewer command/prerequisites. Never simulate them or label them PASS.

Acceptance: at least 2,000-line reduction, no behavior or API/export-format regression, focused parser negative tests, full Python-free suite, local visual/interaction parity, clean diff and reviewable modules. Stop with one consolidated handoff and leave all files uncommitted. Astra alone publishes the exact candidate and performs hosted validation before closure.

Analytics: none. Security/privacy: preserve auth/tenant token boundaries and do not record response bodies or user data. Dependencies: B37 accepted; C09B released. Rollback: revert the isolated refactor candidate, with no schema migration. Effort: Gemini 2–4 hours plus verification; human review about one day.
