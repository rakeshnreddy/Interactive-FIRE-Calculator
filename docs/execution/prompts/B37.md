# B37 — Delivery hygiene and legacy-stack retirement

Role: Gemini implementation worker. Astra is the active architect/reviewer role and sole Git publisher/closure authority. **Draft only; C09B is locked until C09 closes.** Read AGENTS.md, GEMINI.md, PRODUCT.md, the 2026-09-26 protocol amendment, CHECKPOINTS.md, TASK_STATUS.json and RESUME.md. Work only on the current execution branch; do not commit, push, deploy or read secrets. Stop if C09B remains locked.

## User problem and evidence

The current checkout tracks 1,005 files (120,681,806 bytes) under `docs/execution/evidence/`, 53 files (19,544,131 bytes) under `output/`, and two Python bytecode files. `scripts/test_all.sh` still requires Python for the historical Flask implementation even though the released Pages build uses `dist` and `functions`. These are measured 2026-09-26 baseline facts, not a license to delete the sole reference for a formula. The repeated bespoke hosted harnesses also increase proof cost.

## Scope and implementation

1. Inventory the legacy Flask/Firebase paths (`app.py`, `templates/`, `static/`, `requirements*.txt`, `Dockerfile`, `Procfile`, `apphosting.yaml`, `firebase.json`, `babel.cfg`, `messages.pot`, `project/`, `scripts/run_legacy.sh`) and their imports, scripts, docs and CI references. Prove they are not in the Pages build/deployment. Preserve any independent math goldens by porting relevant pytest cases to TypeScript tests with identical inputs/expected values, then remove only unused legacy files and Python test/CI steps. Do not change shipped financial formulas.
2. Create `scripts/hosted_smoke.mjs` as the reusable PA-1 harness. It must accept exact candidate SHA, immutable preview URL/deployment ID, verify successful preview environment and isolated D1 binding before writes, support two disposable synthetic tenants, bounded task steps, tenancy denial and independently checked cleanup. Begin with one minimal reusable proof; do not copy C09's checkpoint-specific runner or move historical C09 evidence. Keep credentials out of stdout/artifacts and fail closed on setup or cleanup uncertainty.
3. Implement PA-4 for new work: ignore `output/` and `docs/execution/evidence/**/raw/`, supply a script or documented command to create a manifest (path, SHA-256, bytes, commit, preview). Determine which accepted review links point to PNG/log files; preserve a retained file or resolvable manifest/artifact target for each. **Do not run `git rm --cached` yourself**; produce the explicit reviewed file list/commands for Astra. No history rewrite. OA-2 must be resolved before Astra untracks bulk evidence.
4. Remove the two tracked `.pyc` files from the index only through Astra's reviewed Git operation. Update README, test script and workflows to reflect the actual Pages/TypeScript stack. Do not erase accepted review records.

## Acceptance and verification

- Ported finance goldens fail when their reference result is intentionally perturbed, then pass unchanged. Existing shipped TypeScript math tests remain green.
- `./scripts/test_all.sh` and hosted CI pass in an environment without Python installed; no silent skipped runtime remains. Verify the script's failure propagation.
- The shared smoke runner's exact-SHA/DB rejection and failed-cleanup paths have negative tests. A healthy isolated preview path produces truthful output without real-user data.
- Before/after tracked bytes, file counts, and fresh full/shallow clone sizes are measured with the exact commands recorded; a full clone is **not** claimed smaller if historical blobs remain. Every previously accepted review link still resolves to a retained file or manifest/artifact pointer. No accepted evidence is silently deleted.
- Build, typecheck, public-route smoke and task-specific tests pass. No production deployment, migration, main merge or formula change.

Analytics: none. Privacy: synthetic users only, no tokens/logged records. Dependencies: B11/B28 accepted and C09B released; OA-2 for untracking. Rollback: restore legacy paths and test script from the task commit; never rewrite history. Human 1–2 days, agent 2–4 hours excluding reviewer/provider waits. Follow PA-7's normal desktop/375px answer-quality walkthrough for any changed user flow (this task should have none). Submit a criterion matrix and `submissions/B37.md`; stop at ready_for_review/blocked for Astra.
