# B37 — Delivery hygiene and legacy-stack retirement

Role: Gemini implementation worker. Astra is the active architect/reviewer and sole Git publisher/closure authority. C09B is released; B37 is first. Read `GEMINI.md`, the PA-10 owner amendment, `TASK_STATUS.json`, `RESUME.md` and the [narrowed rework prompt](../B37_REWORK_PROMPT.md). Preserve the current uncommitted worker changes. Do not commit, push, deploy, access secrets or run hosted writes.

## User problem and evidence

At the 2026-09-26 baseline, the checkout tracks roughly 120.7 MB of `docs/execution/evidence/`, 19.5 MB of `output/` and two Python bytecode files, while `scripts/test_all.sh` still requires Python for the historical Flask stack. The released Pages product builds from React/TypeScript in `src/` and Functions in `functions/`. B37 makes delivery and verification accurate without changing the shipped financial formulas or UI. The shared hosted runner and C09 revise residual are **B42**, after B39; neither is a B37 acceptance criterion.

## Scope

1. Inventory the legacy Flask/Firebase paths (`app.py`, `project/`, `templates/`, `static/`, `requirements*.txt`, `Dockerfile`, `Procfile`, `apphosting.yaml`, `firebase.json`, `babel.cfg`, `messages.pot`, `ref_templates/`, `scripts/run_legacy.sh`) and active imports/scripts/CI references. Prove they are absent from the Pages build/deployment. Port indispensable independent finance goldens from pytest into TypeScript with identical inputs/expected values, demonstrate a deliberate reference perturbation fails, then retire unused legacy code and Python-only CI/test steps. Leave `src/lib/fire.ts` byte-identical.
2. Implement PA-4 ignore rules for `output/` and `docs/execution/evidence/**/raw/`. Build a manifest for index-untracked evidence with path, SHA-256, bytes and the **Git commit that added the file**, verified by `git show <commit>:<path>` and hash. Record a source preview only when independently known; otherwise use `null`/`unknown`. Audit accepted review links from a clean checkout or Git objects, not the current dirty disk; retain files or give an actually resolvable manifest/artifact reference and retrieval command. Inventory the large `docs/execution/evidence/` tree and justify what is retained versus safely untracked. No history rewrite or loss of accepted records.
3. Under approved OA-2, `git rm --cached` may remove the explicit reviewed bulk-evidence list and two `.pyc` files from the **current index only**; files remain on disk. Record the exact list and command for Astra's review. No commit, push, force-push or deletion of accepted evidence by Gemini. Measure baseline tracked files/bytes and provide commands for Astra to measure post-commit index and fresh full/shallow clones. Do not invent “after” clone sizes before a commit.
4. Update only current normative contributor/runtime docs, `scripts/test_all.sh`, its failure-propagation tests, and CI for a Python-free Pages/TypeScript stack. Leave dated historical reviews and counts as history. Remove any B42 `hosted_smoke` import/test invocation from the B37 candidate; the existing untracked runner/test files may be left untouched for B42 but must not be staged or claimed as B37 work.

## Acceptance

- Ported finance goldens fail on a deliberate wrong reference and pass when restored; existing shipped TypeScript math tests remain green; `src/lib/fire.ts` unchanged.
- `./scripts/test_all.sh` and CI run without Python, propagate every failing stage and pass TypeScript, Vitest and production build. No silent runtime skip.
- Manifest hash/source-commit verification works from Git objects or a clean clone; no historical C01 screenshot is attributed to an unrelated C09 preview. Accepted review references remain resolvable after index untracking. Baseline measurements are real; Astra records post-commit clone figures.
- Current normative docs do not point contributors to removed runtime paths. No production deployment, remote D1 write, migration, hosted result claim, formula or UI change.

Analytics: none. Privacy: no credentials, real-user data or new tracking. Dependencies: B11/B28 accepted, C09B released, OA-2 approved. Rollback: restore retired paths and test runner from the B37 commit; no history rewrite. Human 1–2 days; Gemini 2–4 hours excluding reviewer/CI waits. Submit a criterion matrix and `submissions/B37.md`; mark hosted smoke/C09 revise `NOT IN B37 — B42`. Stop for Astra review without marking done or releasing B39.
