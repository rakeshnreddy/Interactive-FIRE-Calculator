# Paste into Antigravity/Gemini — C09 final collector/schema repair

You are the FinPath implementation worker. Astra/Codex is the sole architect, Git publisher, hosted verifier, and task-closure reviewer. Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on branch `codex/finpath-quality-execution`, PR #140. Read `AGENTS.md`, `GEMINI.md`, `docs/execution/MASTER_REVIEW_PROMPT.md`, `docs/execution/reviews/C09-collector-schema-review-2026-09-25.md`, `docs/execution/prompts/B11.md`, `docs/execution/prompts/B28.md`, and the existing C09 matrix/submissions. Inspect and preserve the current **uncommitted** worker changes. Do not run Git mutations (`commit`, `push`, `reset`, `checkout`), deploy, read secrets, touch production, or mark tasks done. Do not call Gemini CLI from this session; this text is for the owner's manual Antigravity workflow.

**Where we stand:** B35 and B10 were accepted. 27/35 tasks are done. Last verified product SHA is `5dda3d2be24246e3470a65e7653a0b6e425cbece`, preview `https://51acbf88.interactive-fire-calculator.pages.dev`, isolated preview D1 `0dbad68e-7493-452f-8504-98d4c61ee5da`. Your latest uncommitted pass improved the revision UI, added 39 passing collector tests and 13 passing component tests, and reports a green full suite of 1,686 Vitest tests. The reviewer found that the candidate hosted runner cannot prove B11 against the real DOM and D1 schema; the new local layout screenshots are fixture images that overwrite a hosted-evidence filename. There is no new candidate commit, CI, or preview. B11/B28 remain blocked; C10 is locked.

Make the six corrections in the review as **one bounded pass**, with meaningful failing tests first. In particular:

1. In `docs/execution/evidence/C09/run_c09_proofs.mjs`, replace `#fire-annual-expense input` with the actual input selector (`input#fire-annual-expense` or equivalent). Inspect `src/App.tsx` `Field` implementation. Assert the value is `50000` after loading Version 2, fill `45000`, and assert the edited control before saving Version 3. Fail closed if the value did not load or change. Do not change FIRE formulas.
2. Use the real D1 schema from `migrations/0001_initial_financial_platform_schema.sql`: one `fire_plan_inputs` row has `input_json` containing the full snapshot and one `fire_plan_results` row has `result_json`. `functions/_lib/firePlans.ts` stores `JSON.stringify(payload.snapshot)` and `JSON.stringify(payload.result)`. Parse and validate those JSON strings; compare finite `snapshot.plan.annualExpense` and finite `result.requiredPortfolio` for v2/v3. Require v2 expense 50000, v3 expense 45000, and a changed calculated result. Require exactly one version/input/result row for v3. Do not pass when result data is missing, malformed, equal, or nonfinite. Unit tests must use raw D1-shaped rows with `input_json`/`result_json`, including malformed/missing/unchanged negatives. Do not assert exact portfolio outputs without checking the actual plan's withdrawal rate and formula inputs.
3. The real `.planning-overview` shows the loaded version and modeled portfolio, **not annual expense**. On `/plans?planId=...&version=3`, assert Version 3 is loaded, use the product's route to the calculator, and assert `input#fire-annual-expense` displays 45000 after reload. Cross-check that D1 v3 snapshot and UI agree. Avoid treating the notes or version title as evidence of the numerical input.
4. For the persisted Version 2 `revise` review, replay exactly the persisted key/date/decision/notes. Assert response HTTP 200 with `isDuplicate: true`, and query exactly one review row. For changed intent with the **same** key, assert HTTP 409 with code `IDEMPOTENCY_CONFLICT`; query D1 **after** that request and assert one row with the original `revise` intent and original key. Validate count in both helper branches. Add negative tests for absent duplicate flag, wrong conflict code, and a second row after conflict. Preserve the production idempotency contract.
5. Strengthen `verifyPriorVersionsImmutability`: require exactly one row each in `plan_versions`, `fire_plan_inputs`, and `fire_plan_results` for Versions 1 and 2, both before and after saving v3. Only then compare all six row sets byte-for-byte; missing rows must fail. Add negative tests.
6. Fix `scripts/measure_local_clearance.mjs` before running it again. It is a synthetic layout fixture with a simplified header and an extra focusable heading, **not the real authenticated product**. Write its screenshots only under `docs/execution/evidence/C09/local-fixture/` with fixture names; restore the existing tracked hosted screenshot `docs/execution/evidence/C09/screenshots/04_b11_review_completed_panel_focused.png` from committed content without a Git mutation, and remove misleading untracked images from the hosted screenshots directory. Eliminate arbitrary static file serving: use a strict, exact asset allowlist or no server, bind to `127.0.0.1`, request an ephemeral port, and reject path traversal/unlisted paths. Add a safe negative test using a harmless outside-repository fixture file; do not read private files. Update the matrix and submissions to label all four local cases as fixture-only diagnostics and hosted B28 overlap/keyboard checks as pending. The real product `h2` is not focusable, unlike the fixture; do not claim actual keyboard/focus proof from it.

After implementation, run the collector tests, component tests, `./scripts/test_all.sh`, and `git diff --check` once on the final diff. Record exact commands, counts, exit codes, and any known limitation in `docs/execution/evidence/C09/validation-matrix.md` and B11/B28 submissions. Keep B11/B28 `blocked` and C10 locked. Do not overwrite prior hosted evidence or cite the old preview for new code. Return a concise handoff with changed files, raw-schema test evidence, the exact v2→v3 numeric assertion, retry behavior, fixture security test, remaining hosted proofs, and the commands the reviewer should run. Astra will commit/push, check exact-code CI, deploy an isolated preview, run the hosted synthetic proof, inspect real desktop/mobile light/dark screenshots and cleanup, then decide closure.
