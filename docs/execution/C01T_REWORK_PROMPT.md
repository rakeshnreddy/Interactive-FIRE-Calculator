# C01T rework complete — next worker handoff

B32 is accepted by the primary reviewer. Do not repeat its completed palette, print or evaluator repairs. See [final review](reviews/C01T.md); actual-reader testing is explicitly deferred to B31.

## Copyable prompt for the next agent

Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`. Inspect git status and remote HEAD first; preserve all existing changes and do not reset the checkout. Read `docs/execution/README.md`, `docs/execution/MASTER_WORKER_PROMPT.md`, `docs/execution/CHECKPOINTS.md`, `docs/execution/TASK_STATUS.json`, and `docs/execution/prompts/B33.md`.

C01I/B33 is the only released checkpoint. Begin its read-only inventory of effective Cloudflare preview bindings, including automatic Git deployments and manual publication. Identify exact code and environment relationships without exposing credentials or user records. Follow every B33 acceptance criterion and evidence requirement. Before creating a remote database, changing a binding, or running remote migrations, obtain the scoped owner authorization required by B33. Do not infer that checkpoint release supplies that authorization.

Do not publish changed backend code until B33 isolation is accepted. Do not merge main, deploy production, alter DNS, change production data or bypass authentication. Use the narrow configuration-only bootstrap exception only after its explicit owner authorization and binding checks. Stop at a real owner blocker with the smallest exact request, or submit B33 ready_for_review with immutable evidence. Do not close your own task or release C02. The primary reviewer alone closes tasks.
