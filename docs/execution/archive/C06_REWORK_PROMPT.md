# C06 consolidated rework — implement and prove R1–R5

Work in /Users/Rakesh/Projects/Interactive-FIRE-Calculator on codex/finpath-quality-execution, PR #140. Safely synchronize, preserve unrelated changes. Read C06_START_PROMPT.md, reviews/C06.md, IMPLEMENTATION_AND_VALIDATION_PROTOCOL.md, FREE_TIER_EXECUTION.md, ACCESSIBILITY_DEFERRALS.md and original B05/B07/B25 contracts. Candidate reviewed was d23def571c3df98571da71aef5541ec4ceb1721e. Primary owns closure. C07 is locked. Do not repeat C05 work.

Implement these repairs autonomously, using small commits. Preserve passing two-user handler/migration tests, real component reuse, formulas and B02/B03/B04 semantics. Routine tests, screenshots and evidence are your responsibility. Do not delegate them back merely because the first headless tool lacks native control; check available browser capabilities early. Record an exact capability blocker truthfully if none is available.

## R1 — deterministic clean-checkout verification (B05/B25)

- Reproduce CI run 34917339958 failures. SQLite/backend tests must execute in a Node environment compatible with pinned CI runtime; split browser/localStorage cleanup tests out of backend tests rather than importing App into backend test setup unnecessarily. Do not stub persistence to avoid node:sqlite.
- Production isolation assertions must inspect a fresh build of current candidate. Move artifact checks to a post-build validation command integrated with full-suite runner, or build in a clean dedicated temp directory in integration setup. No dependency on preexisting dist, no skipping assertions when dist is absent, no weakening test discovery. Check source maps and HTML as well as JS/CSS for test-only entries and markers.
- Validate `./scripts/test_all.sh` from a fresh isolated checkout with dependencies installed and no existing build output, using CI's supported Node version. Preserve runner tests and failing exit propagation. Capture command, version, full log, exit and exact code SHA. One final full run is sufficient unless code changes afterward.

## R2 — atomic deletion boundary and coherent exports (B07)

- Start with reviewer probes in evidence/C06-review/reviewer-probes.ts.txt. Promote them to durable regressions with proper teardown. Current account race is: normal create passes ensureUserProfile; deletion commits immediately before create's batch; batch inserts new account under tombstoned user. API error afterward is insufficient: assert zero resurrected rows.
- Enforce active-user condition at the actual database mutation boundary across profile creation, accounts/balances, transactions/imports, goals, calculator results/plans/versions. Use a coherent atomic SQL/batch or database-constraint design supported by D1. A second standalone preflight check is not a fix. Preserve atomic save/idempotency; reject late writes predictably without partial inserts or false success.
- Test deletion before identity creation, between ensureUserProfile steps, immediately before domain batch, repeated deletion, and injected batch failure. Inspect all user-owned rows; ensure user B values and counts stay unchanged. Use controlled barriers/hooks around actual DB operations, not sleeps or comments asserting concurrency. Keep test hooks out of shipped auth.
- Export must represent one coherent database state, or fail clearly if consistency cannot be established. Replace sequential independent reads with a supported atomic snapshot approach. Prove export during deletion and during related multi-table updates cannot mix revisions. Adapt the reviewer export test to allow a deliberately documented clean failure; never allow partial success.
- Make tombstone semantics and HTTP response consistent. Do not change deletion into permanent account lockout without truthful user-facing explanation of what remains in Clerk and what happens on subsequent use. Inspect actual existing UI wording against new behavior.
- Audit local cleanup against actual storage keys; test restricted storage access including a getter that throws (the current guard accesses localStorage outside try). Keep unrelated keys/preferences intact, distinguish origin-wide anonymous drafts from user-owned data, and avoid reporting deletion failure after server success merely because local cleanup failed.
- If an additive schema migration is needed, write migration/rollback decision and test it locally. Do NOT apply it remotely without explicit authorization. Finish independent repairs and submit exact migration target/request as blocked publication if necessary.

## R3 — real fixture behavior and theme parity (B25)

- Use canonical product `.app` wrapper with `data-mode={colorMode}` and required token imports. Apply theme to actual component surface, not only a fixture toolbar. Avoid duplicate theme definitions. Verify computed canvas, heading, body and field colors differ appropriately in light/dark and match normal app tokens.
- Replace self-created fetch mock test with actual fixture entry/module and component action tests. Trigger account/transaction imports and representative save/archive/export/delete actions with synthetic data. Assert no outgoing mutation reaches a backend. Provide safe injected adapters for real import UI where needed so named success/failure/loading states can be inspected; simply throwing for every import is not evidence of those states.
- Actually render URL-selected component/state combinations. Assert characteristic empty, populated, loading, failure, stale and long-value output; map N/A explicitly when real component lacks a state rather than claiming all six passed. Fix fixtures whose summaries/units contradict their rows. Test state/theme switching, error recovery and editable controls.
- Capture durable browser evidence after corrections: all seven component domains in representative populated and adverse states; desktop/mobile including 320px; both themes; keyboard/focus; actual native 200% zoom; reduced motion/transparency; composited contrast; no overlap/clipping; chart units. Reuse existing collection patterns only where observations remain actual and assertions meaningful. Browser screenshots must be real component renders. Include paths, viewport/theme/state, timestamps, candidate association, console/page-error logs and pass/fail reasons. Actual reader smoke alone is deferred.

## R4 — executable, honest deletion/recovery contract (B07)

- Rewrite DATA_DELETION_AND_RECOVERY.md around verified behavior. Remove nonexistent function names, response codes and table names; ensure recovery SQL executes on current local migrations and checks every user-owned table.
- Identify how an authoritative deletion list survives the chosen restore scenario. If no independent durable journal is implemented, explicitly block serving after recovery when completeness cannot be established. Do not invent available replicas/offsite logs. Local restore/replay drill should cover missing/incomplete journal, deletion of a user absent from restored snapshot, retry, failure and preservation of user B.
- Do not purchase backup tooling or select legal retention periods. Remove unsupported broad legal claims or leave precise owner/legal questions, separate from implemented guarantees. No claim of Clerk/provider erasure, hosted recovery or full WCAG compliance.

## R5 — one honest final candidate and publication packet (all)

- Fill contracts/validation matrices and submissions with actual assertions and durable files; correct inaccurate local-only claims (production Functions and App were modified). Remove provisional PASS where evidence is absent.
- Before any further Functions push/deploy, inspect B33 records and independently recheck intended effective isolated preview binding, including automatic Git deployments. If current branch auto-deployed earlier, record its actual deployment relation and binding; do not assume the prior worker avoided hosted changes merely because it did not run a manual deploy.
- After local/full checks, push execution branch and require exact successful hosted CI. Publish immutable free isolated preview only with confirmed isolation and no unapproved migration; record deployed full SHA and effective preview DB. Run all public-route smoke checks and signed-out API/health boundaries. Never write real user data or bypass production-auth preflight.
- Combine all task evidence against final code SHA; later docs-only commit may differ if executable content identity is proven. Add progress digest with R1–R5 rows, raw logs and exact missing conditions. Ready_for_review only when every mandatory non-deferred criterion is evidenced; otherwise blocked. Never mark done, release C07, merge main or deploy production.

Stop at C06 primary review with exact code/evidence SHAs, CI, immutable preview or precise publication blocker. No owner input is needed to begin these local repairs.
