# C07 read-only preparation — fix the hosted identity prerequisite

Work in /Users/Rakesh/Projects/Interactive-FIRE-Calculator on codex/finpath-quality-execution, PR140. Read git status and remote HEAD first; preserve all existing changes. Read MASTER_REVIEW_PROMPT.md, CHECKPOINTS.md, TASK_STATUS.json, prompts/B06.md, FREE_TIER_EXECUTION.md and ACCESSIBILITY_DEFERRALS.md. Primary alone accepts tasks and releases checkpoints.

C06 is accepted at0f3ae8d9cc4d447518e484b8de7a34ee1b54f38a. Its preview is https://5e68409d.interactive-fire-calculator.pages.dev; effective DB is isolated finpath-preview UUID0dbad68e-7493-452f-8504-98d4c61ee5da. Migration0006 has already been applied and29 trigger definitions verified. Do not reapply it. No main merge, production deployment, paid services, real identities/data, or auth bypass.

The next observed defect is concrete: /dashboard at390px returns200 with “Account features are currently unavailable.” Homepage and84 public calculator routes pass; protected APIs return401. The direct local build does not provide usable frontend Clerk configuration. Provider environment key presence is insufficient proof that Vite incorporated it. Inspect src/auth.tsx, src/main.tsx, actual build configuration and provider preview metadata securely. Determine whether the approved development Clerk instance, matching frontend/server configuration and exact allowed preview origin exist. Report present/absent and development/production classification only; never print tokens, credentials or provider secret values. Do not paste secrets into chat.

While C07 is locked, perform READ-ONLY diagnosis and write a compact readiness packet docs/execution/evidence/C07/readiness.md. Capture exact candidate, current effective isolated binding, which build-time/runtime configuration is missing, expected change and one reproducible verification command per outcome. Do not modify provider settings, deploy replacement builds with new auth settings, create users, or write hosted financial records until primary releases C07 and the owner authorizes the exact disposable test scope. Do not ask the owner to repeat authorization for migration0006.

Prepare this concrete proposed execution sequence for approval: use existing free Clerk development configuration on the isolated preview, build with the matching frontend publishable key through secure environment injection, publish only the codex preview, verify boundDB, then run two disposable synthetic users through sign-up/in/out, refresh/expiry, save/reload, profile, imports, export, deletion and cross-user rejection. No existing personal identity may be deleted. Identify exact origin and whether temporary disposable Clerk users and synthetic D1 writes are required; request only missing choices/authorization. Keep production preflight fail-closed.

After release, implement only the diagnosed change; tests before behavioral changes, full ./scripts/test_all.sh before pushing, exact candidate CI and immutable preview. Verify normaldesktop/mobile light/dark, keyboard, meaningful error states, console and public routes. Native200% zoom and actual screen-reader tests are deferred to finalB31/C11: do not perform or block on them now. Do not weaken other functional/security checks. Record raw results, distinguish failed from deferred, and stop at primary review without closing B06 or releasing C08.

Return a short readiness digest: root cause and evidence, precise proposed files/provider settings, what can run with existing authorization, exact missing owner input, and expected acceptance checks. No broad audit or repeated C06 suite unless changes actually affect it.
