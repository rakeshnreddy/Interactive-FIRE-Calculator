Review the latest submitted FinPath checkpoint as the primary reviewer and closure owner. Read docs/execution/README.md, CHECKPOINTS.md, TASK_STATUS.json, each submitted task prompt/evidence packet, VISUAL_AND_UI_AUDIT.md, VISUAL_DESIGN_SPEC.md and COLOR_AND_GLASS_SYSTEM.md.

Identify the submitted checkpoint, exact code SHA, PR/base and immutable preview. If ambiguous, ask for the missing identifier. Do not trust ready_for_review status or generated screenshots as proof. Inspect the real diff, tests and deployed code relation, reproduce the changed browser journey, and verify every applicable acceptance criterion. Check desktop/mobile/light/dark, keyboard, actual200% zoom, reduced motion/transparency, contrast, error/empty/long-value states, no overlap and honest chart units. For data/auth tasks verify real local or approved isolated hosted tests as required. Never use production data or bypass auth to get a pass.

Require all checkpoint tasks to pass on the same final candidate code SHA and immutable preview. Earlier per-task SHAs cannot establish combined acceptance. Retest affected provisional submissions, not just previously accepted tasks. Confirm B33 covered automatic/manual deployment bindings before backend publication.

Confirm full-suite and CI evidence belongs to the submitted code revision. Run missing meaningful checks with your capabilities; do not repeat all tests without reason if exact trustworthy evidence exists. Compare accepted prior surfaces for regressions. Apply the visual rubric with concrete reasons, but never trade away accessibility, formula correctness, privacy or data lifecycle for aesthetics. For glass/gradients, inspect worst-case composited contrast and opaque fallback in both themes. Unavailable mandatory evidence blocks closure.

Write docs/execution/reviews/<checkpoint>.md using REVIEW_TEMPLATE.md. Mark only passing tasks done in TASK_STATUS.json and check their EXECUTION_BACKLOG.md items. Return failed tasks as changes_requested with exact file/behavior, reproduction, required correction and retest. A later regression reopens an affected accepted task. Only release the next checkpoint when every current required task passes and its external gates are satisfied. Record accepted code SHA and next release in CHECKPOINTS.md, validate the packet, and commit/push the review records on the execution branch without merging or production deployment.

Finish with accepted/total percentage, task decisions, evidence, remaining risk, precise next task/checkpoint, PR and preview. State exactly what owner input is needed if blocked. Do not merely say “looks good”; this review is the authority for closure.

B33 bootstrap exception: with explicit owner authorization, publish configuration only using unchanged, identified backend code. Verify intended isolated preview binding before publication and effective deployed binding afterward. This is the narrow setup operation needed to prove isolation, not permission to publish changed APIs or perform financial writes. Those remain blocked until B33 is accepted. If the provider cannot establish the intended binding safely before publication, stop and request a separate isolated target.

Apply IMPLEMENTATION_AND_VALIDATION_PROTOCOL.md section 6 to every subsequent review: consolidate original-scope defects with reproductions, preserve passing work, separate optional improvements from blockers, and take ownership of a repeatedly failed bounded repair after the clarified-contract retry threshold. Check the task contract and validation matrix independently; worker self-review is not acceptance.

Owner amendment 2026-09-13: actual screen-reader/VoiceOver smoke is deferred for implementation checkpoints through C10 to B31/C11; it alone must not block otherwise passing task closure or checkpoint release. This supersedes generic reader-smoke requirements in older task prompts. Follow ACCESSIBILITY_DEFERRALS.md, retain all other checks and make no full WCAG-conformance claim. Lower-cost agents perform the deferred checks; primary owns acceptance.


## Owner amendment — 2026-09-15: consolidate zoom verification at B31/C11

The owner directed: “push it to the end of verification of all tasks … if there is any zoom issue it can be fixed later.” Native browser 200% zoom checks and zoom-specific layout repairs are therefore DEFERRED through C10 to the final B31/C11 verification. Their absence or a known zoom-only issue must not block otherwise passing implementation tasks or checkpoint release. This supersedes earlier per-task native zoom gates, including older prompt/contract language. Do not rerun native zoom at every checkpoint. Preserve existing evidence and record newly noticed zoom defects without spending implementation time on them now. Functional correctness, tenancy, deletion, auth boundaries, normal-size usability, mobile layouts, keyboard, contrast and reduced-motion/transparency checks remain required. Deferred means not passed; no full WCAG-conformance claim.

B31/C11 follow-up ZOOM-FINAL: a lower-cost capable agent performs one consolidated actual-browser 200% sweep of final public and authorized synthetic authenticated journeys, in both themes. Verify reachable essential controls, readable inputs/results, no text overlap or clipped actions, and reflow. Record browser version, actual zoom level, exact candidate, route, screenshot, defect and focused retest. Pixel density, CSS zoom and viewport resizing do not substitute for native browser zoom evidence. Primary reviews the evidence and closes the task. Existing C06 account-import/fixture toolbar repairs and screenshots are retained; repeat only if the final sweep finds a regression.
