# B37 final hygiene review — 2026-09-26

Reviewer: Astra. Decision: **APPROVED / ACCEPTED** under the owner's B37 final-correction PA-3 rule. B37 is closed at initial candidate `0c6e35cd9720a1e79b05a1dc7baad7498848ff93` plus reviewer-owned final manifest fix `83bc1a27b54203a3d30e85b85a042c5b308190fe`; B39 is released as the next C09B task. PR [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140) remains unmerged. No production deployment, preview D1 write or new hosted result is claimed for this hygiene-only task.

## Independent acceptance evidence

- **Finance goldens:** compared the retired `tests/test_finance_core.py` to `src/lib/fire.test.ts` and Gemini's 21-method coverage matrix. The final six additions include numerical literals for single-period portfolio and expense, start-year one-off income, varied returns/durations, and final-value differences. The start-year balance-array shape differs between legacy Python and TypeScript; final cash flows and ending value are explicitly documented and asserted. `src/lib/fire.ts` has no diff. Gemini's deliberate wrong-reference red and restored green runs are in its packet.
- **Python-free full suite:** Gemini's durable log recorded 1,705 Vitest tests, typecheck, production build and isolation PASS with neither `python` nor `python3` in PATH. Astra independently reran the suite in that PATH. An initial reviewer rerun hit the existing `src/FireCalculator.test.tsx` 5-second UI-test timeout (1/1,705 failed); the single clean retry passed all 1,705 tests in 55 files, typecheck, build and 21-asset isolation. The test was unchanged by B37. This timing observation is non-blocking; a repeatable timeout should be addressed in later test-stability work, not relabeled as a product PASS.
- **Manifest:** Astra independently ran 6/6 fail-closed manifest tests, `--verify` over all 53 Git objects with first-add and latest-content hashes/sizes, and `--audit` with zero missing concrete files. Seven B17 files correctly retain distinct first-add and later-content revisions. The old C01 screenshots are not attributed to the C09 preview. The two concrete references reported in the prior review now point to checked-in C03 and B20 artifacts; remaining directory/glob/line references are classified as non-file references. Dated historical docs are retained as history.
- **Delivery:** `git diff --cached --check` was clean before commit; packet validation passed 42 tasks and 19 checkpoints. Only B37 paths entered the code commit. The two untracked `scripts/hosted_smoke*` files remain reserved for B42 and were not committed. Initial exact-code GitHub [Verify run 36237024836](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/36237024836) passed the full suite and Node audit. The final two-file verification fix has its own exact-code CI recorded below.

## Post-commit verification fix and final CI

After the approved index-only removal was committed, a fresh `--verify` exposed an edge case: `git log -1` selected the B37 deletion commit as the latest screenshot revision. Gemini CLI authentication returned `IneligibleTierError: UNSUPPORTED_CLIENT`, so Astra made a two-file reviewer-owned verification fix in `83bc1a2`: select the latest addition/modification while ignoring later deletion commits, and test add→delete and add→modify→delete histories. On the committed tree, 6/6 unit tests, all 53 manifest object checks, and the strict audit of 216 references with zero missing concrete files pass. This exception is limited to verification tooling; no product code changed.

Final exact-code CI: [Verify run 36237336507](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/36237336507) passed the full suite and Node audit.

## Measured size and boundaries

At baseline `cb2422d`, 1,552 tracked files totaled 154,209,667 Git tree bytes. B37 candidate `0c6e35c` has 1,461 tracked files totaling 134,363,241 bytes: 19,846,426 fewer checked-out bytes. After commit, a fresh local depth-one clone measured 249,416 KiB and a fresh local full clone 282,300 KiB (`du -sk`, including checkout and `.git`). These are post-commit observations, not claimed full-clone savings; old blobs remain in history. The 53 PNGs and two `.pyc` files were removed from the current index only, with Git-object retrieval in the manifest. No history rewrite.

The last immutable isolated preview remains C09's `https://18b043da.interactive-fire-calculator.pages.dev`; it is **not** B37 exact-code proof. B37 changed verification and legacy-only paths, not shipped UI or Pages Functions. B42 still owns the unobserved C09 Version 2 revise → Version 3 hosted journey. C09B remains released, ordered B39 → B42 → B36. C10 remains locked. Native 200% zoom and actual screen-reader checks remain deferred to B31/C11. No owner action is needed for B39.
