# C09 independent closure review — 2026-09-26

Reviewer: primary Astra reviewing session
Decision: **APPROVED WITH EXPLICIT HOSTED RESIDUAL** under the 2026-09-26 C09 PA-2/PA-3 owner amendment. This is not a complete hosted PASS.
Reviewed final checkpoint code commit: `c4bf784b106878e7e0c940564216ec44a72ecb38`
PR/base: [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140), execution branch against `main`; no merge.
Exact-code CI: [run 36229177941](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/36229177941), successful.
Immutable preview: <https://18b043da.interactive-fire-calculator.pages.dev>, deployment `18b043da-e736-4f8f-83b0-3a0642ff6685`; primary verified successful preview metadata, exact SHA and isolated `finpath-preview` D1 `0dbad68e-7493-452f-8504-98d4c61ee5da` before the hosted write. Production D1 was untouched.

## Independent checks actually performed

- Reviewed Gemini's five-file Stage 4 proof-runner/documentation change. No product/config code changed from the prior candidate. The intended bounded wait checks route, expected plan/version, hydration and visible review panel; its 62 focused tests pass. A remaining weakness is that a visible auth gate is treated as terminal while Clerk is still loading, and the post-timeout branch can accept a visible panel with empty overview text. This is carried into B37's shared-runner contract, not silently counted as a pass.
- `./scripts/test_all.sh` passed locally: 14 Node runner tests, five preview-auth tests, 79 Python tests plus 21 subtests, TypeScript, 1,686 Vitest tests in 55 files, and Vite build. `git diff --check` passed. Exact-code hosted CI passed. The preview-auth build isolation check passed.
- The **single** final hosted proof ran on the exact candidate and preview. B10 saved v1/v2, deep-link restoration, unsaved-change guard, controlled errors, tenant denial and version immutability passed. B11's early-review rejection, defer, keep, persisted status and idempotent duplicate passed. B28 dashboard due rollup and explicit badges were observed. Review-panel sticky-topbar clearance passed for desktop light (122.5px), desktop dark (76.5px), mobile light 320px (186.7px), and mobile dark 320px (69.7px), with zero overlap in the measured heading, badge and focused control. Primary inspected the focused desktop-light and mobile-dark screenshots. All 84 public calculator routes passed read-only smoke on this preview.
- The run then stopped on navigation to `/plans?planId=<synthetic>&version=2`. The readiness helper saw an auth gate with `window.Clerk.loaded === false` and immediately failed. It did **not** observe a settled signed-out Clerk state, nor the Version 2 revise UI. Thus the revised plan's Version 3 persistence, financial result diff, prior-version immutability, and downstream B28 hosted criteria were not executed in this run. No product behavior assertion failed; a product issue is not ruled out by this partial run. The report correctly says `FAILED`, with later fields undefined. See [raw final attempt](../evidence/C09/attempt-c4bf784/) and the previously reviewed [f422 attempt](C09-hosted-attempt-f422cb3.md).
- Fail-closed cleanup ran for both disposable users. Primary separately queried all 15 user-scoped preview D1 tables for each user (all zero), verified both tombstones, and received 404 for both deleted Clerk users. No real users or production records were used.

| Task | Decision | Evidence and limit |
|---|---|---|
| B10 | Prior acceptance retained | Exact-version restoration, unsaved-change protection and tenant denial re-observed on final candidate. |
| B11 | Accepted under owner residual rule | Local/integration tests and hosted early/defer/keep/idempotent stages pass. The real revise → Version 3 journey remains **UNOBSERVED HOSTED**; B37 must establish it. |
| B28 | Accepted under owner residual rule | Four normal-size focused clearance cases pass; dashboard due rollup observed and local/integration tests pass. Goals/theme/keyboard and full downstream hosted flow after Stage 4 remain **UNOBSERVED on this final run**; B37 must observe them. |

## Visual judgment and safety

Focused desktop light and mobile dark screenshots show a readable review heading, textual status badge, visible choice controls and keyboard focus. The measured clearance applies only to the named review elements, not every page region. No formula or `src/lib/fire.ts` change occurred in this final proof-runner candidate. Native 200% browser zoom and actual screen-reader checks remain owner-deferred to B31/C11; this review makes no WCAG-conformance claim.

The explicit C09 closure rule directs acceptance after a final tooling/readiness stop, provided the missing hosted journey is named as a residual in PA-1's B37 shared smoke. That rule applies here. Do not relabel the failed `report.json` as PASS. No new product-defect ID is justified because no product failure was observed. If B37's corrected runner observes a real defect, register and repair a scoped task before accepting B37. OA-2 is approved for current-index evidence untracking only, with no history rewrite.

## Closure actions

B11 and B28 are `done` under the stated exception; B10 stays accepted. C09 closes at `c4bf784b106878e7e0c940564216ec44a72ecb38`, and C09B releases with B37 first. `docs/execution/prompts/B37.md` carries the exact residual and acceptance gate. C10 remains locked. Current count: 29/41 accepted (70.7% by task count, not effort). No owner input is needed for B37. OA-1, production Clerk setup, remains pending solely for later production readiness. This review does not authorize a `main` merge or production deployment.
