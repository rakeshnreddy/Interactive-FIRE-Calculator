# C09 uncommitted rework review — 2026-09-25

Decision: **CHANGES_REQUESTED for B11 and B28. C10 remains locked.** B10 stays accepted. This review concerns the uncommitted nine-file worker diff on top of `ad2842e79bb3b6a58e925ef80e1b3751de34cbe5`, not a deployable code SHA. The changed code has no immutable preview or exact-code CI. The last verified product preview remains `https://51acbf88.interactive-fire-calculator.pages.dev` at product SHA `5dda3d2be24246e3470a65e7653a0b6e425cbece`. PR: [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140); isolated preview D1: `0dbad68e-7493-452f-8504-98d4c61ee5da`.

## Independent checks

Read all changed product and collector code, the B11/B28 submissions and matrix, the save handler and D1 idempotency contract. Ran `node --test docs/execution/evidence/C09/run_c09_proofs.test.mjs` (31/31 passed), `npx vitest run src/monthlyReviewPresentation.test.tsx` (13/13 passed), and `git diff --check` (clean). The worker reports a green full suite of 1,686 Vitest tests, 79 Python tests plus 21 subtests, typecheck and build; this has not been tied to a committed final code SHA. The new hosted screenshots named in the matrix do not exist yet; current `report.json` and screenshots belong to the previous 5dda deployment. No hosted synthetic writes or migrations were performed in this review.

| Task | Decision | Finding and required correction |
|---|---|
| B11 | **CHANGES_REQUESTED** | **[P1, confidence 10/10]** `run_c09_proofs.mjs:1458–1486` says it edits an assumption, but only fills version notes and label and checks notes in D1. `App.tsx:7355–7375` saves the current financial snapshot independently of those metadata fields. The promised financial revision remains unproved. Exercise a real financial control, save Version 3, verify its snapshot/input and result changed, exact reload, and immutable Version 1/2 inputs **and results**. The banner at `PlanningWorkspace.tsx:783–800` must direct the user to the actual assumption editor rather than describe the metadata form as an assumption form. |
| B11 | **CHANGES_REQUESTED** | **[P1, confidence 10/10]** The UI review sends no explicit idempotency key (`PlanningWorkspace.tsx:380–393`), so the server derives `v2:<evidenceDate>` (`functions/_lib/planReviews.ts:122–145`). The runner's retry sends `review-revise-c09` (`run_c09_proofs.mjs:1516–1537`). Migration 0008 enforces uniqueness only per key, so this is a different operation and cannot prove an exact replay; it may create a second review row. Replay the stored review's exact key/date/decision/notes and separately test changed intent returning 409. |
| B28 | **CHANGES_REQUESTED** | **[P2, confidence 9/10]** The new “Edit version details” button calls `scrollIntoView({ behavior: 'smooth' })` unconditionally (`PlanningWorkspace.tsx:792–800`), including when reduced motion is requested. Respect reduced-motion preference. The `scroll-margin-top` CSS has no current hosted proof: the matrix cites four new screenshots that are absent and marks B28-13 PASS against the old preview. Prove actual heading/badge clearance on the new candidate in desktop/mobile light/dark, including keyboard/focus entry; do not infer a pass from CSS alone. |

The new evaluator tests flip report booleans; they do not exercise the collector's assumptions or its distinct retry key. `run_c09_proofs.mjs:1452` also sets `review_revise_ui_selected = true` after a click without reading the checked radio or persisted action. Add collector-level negative cases for unchanged financial inputs/results, mismatched replay key and unchecked/failed revise selection. Correct the B11-11 and B28-13 matrix rows and submissions: **blocked/pending new preview**, not PASS. Preserve the 31 passing evaluator tests and all previously accepted behavior.

## Closure

No new task closed. Ledger remains 27/35 accepted (77.1%). B11/B28 return to blocked; C09 stays released for focused rework and C10 stays locked. The uncommitted worker code is intentionally preserved for the next Gemini pass. Do not commit/push/deploy this candidate before the focused corrections; do not claim the old preview proves the changed product. Follow `docs/execution/C09_REVISE_FINISH_PROMPT.md`. Native 200% zoom and actual reader checks remain owner-deferred to B31/C11. No main merge or production deployment.
