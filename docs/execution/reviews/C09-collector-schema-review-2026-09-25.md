# C09 collector and evidence review — 2026-09-25

Decision: **CHANGES_REQUESTED for B11 and B28.** B10 remains accepted, C09 remains released for repair, and C10 remains locked. This review concerns the uncommitted Gemini worker diff on top of `72bc0de`; there is no new code SHA, exact-code CI, or immutable preview for it. The last verified product code and preview remain `5dda3d2be24246e3470a65e7653a0b6e425cbece` and `https://51acbf88.interactive-fire-calculator.pages.dev`. No hosted writes, migration, main merge, or production deployment were performed.

The worker improved the product's revision guidance, calculator return path, reduced-motion behavior, and candidate collector. Its handoff reports 39 collector tests, 13 component tests, and a green full suite (1,686 Vitest, 79 Python plus 21 subtests, 14 Node runner tests, 5 preview-auth tests, typecheck/build). The reviewer independently inspected the changed code, D1 schema, UI markup, synthetic screenshot, and collector tests; the 39 collector tests and `git diff --check` passed. These checks do not establish hosted behavior.

| Priority | Finding | Required correction |
|---|---|---|
| P1 | `run_c09_proofs.mjs` targets `#fire-annual-expense input`, but `Field` puts `fire-annual-expense` on the input itself. The selector has no match. The collector logs but does not assert the original 50,000 value. | Target the actual input, assert the loaded v2 value is 50,000, change to 45,000 through the visible UI, and assert the edited UI value before saving. A wrong selector or wrong starting value must fail. |
| P1 | Migration 0001 defines `fire_plan_inputs.input_json` and `fire_plan_results.result_json`, not `annual_expense` or `required_portfolio` columns. `verifyFinancialRevisionDiff` consumes invented flattened rows in unit tests and can pass when results are absent. Raw D1 rows fail its expense lookup. | Parse the actual JSON columns, read `snapshot.plan.annualExpense` and `result.requiredPortfolio`, require finite values and exactly one v2/v3 input and result row, and compare changed assumption **and** changed result. Use raw D1-shaped fixture rows in positive and negative tests. Do not hardcode an expected portfolio figure without showing the actual formula inputs. |
| P1 | The deep-link assertion requires overview text containing both `Version 3 loaded` and `45,000`, but the overview renders version and portfolio, not annual expense. | After exact v3 deep-link reload, verify the plan/version in UI and the 45,000 annual-expense value in the calculator's actual input (or a user-visible v3 value), alongside the D1 snapshot. Do not infer reload from notes or a version label alone. |
| P1 | Conflict replay reuses a row count queried **before** the conflict request; helper only enforces one row for the exact replay branch. It does not inspect `isDuplicate` or `IDEMPOTENCY_CONFLICT`. | Assert HTTP 200 and `isDuplicate: true` on exact replay; after 409 conflict, query D1 again, require exactly one review row and original `revise` intent, and assert structured conflict code. Add negative tests for duplicate rows and wrong/missing response fields in each branch. |
| P1 | `scripts/measure_local_clearance.mjs` runs a simplified HTML fixture with a different header and an added focusable heading. It writes screenshots to the same paths as hosted product evidence, including the previously tracked `04_b11_review_completed_panel_focused.png`. Its static server maps arbitrary request paths to files under or outside the repository and listens on all interfaces by default. | Isolate fixture screenshots in a visibly labeled `local-fixture/` directory, restore the overwritten hosted image from the last committed version, and label fixture results as layout diagnostics, not hosted proof. Replace the file server with a closed asset allowlist (or no server), bind to `127.0.0.1` on an ephemeral port, reject traversal and unlisted paths, and test rejection without reading private files. Do not rerun the current server before this is fixed. |
| P2 | Immutability compares JSON arrays without requiring one row per version/input/result before and after; identical empty arrays can pass. The fixture's heading has `tabindex=-1` while the product heading does not, so the fixture cannot establish product keyboard/focus behavior. | Require one row in all six before/after table result sets for v1/v2. Prove real product keyboard/focus entry on a new hosted preview or record it as pending. Keep the four local viewport/theme measurements separate from hosted evidence. |

Preserve the worker's useful product changes and tests. Run focused collector/component tests plus `./scripts/test_all.sh` and `git diff --check` after the repair. Keep B11/B28 blocked and the matrix's hosted rows pending until the primary reviewer commits, obtains exact-code CI, deploys an isolated preview, runs the hosted synthetic proof, checks cleanup/tombstones, and inspects the real product screenshots. Native 200% zoom and actual screen-reader checks remain owner-deferred to B31/C11. Follow `docs/execution/C09_COLLECTOR_SCHEMA_FINISH_PROMPT.md`.
