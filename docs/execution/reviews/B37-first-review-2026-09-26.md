# B37 first independent review — 2026-09-26

Reviewer: primary Astra reviewing session
Decision: **CHANGES_REQUESTED**. B37 is not accepted; B39 remains locked.
Base HEAD: `cb2422d7bdb2ef942486205936e56f27807d7063`. Gemini's B37 files and index-only untracking are still uncommitted. There is no B37 code commit, exact-code CI or B37 immutable preview. The prior C09 preview `18b043da` is not evidence for B37.

## Independently verified findings

1. **Critical: the hosted runner reports success without running a hosted proof.** `node scripts/hosted_smoke.mjs` returned exit 0 and “Preflight verification complete” with no credentials or network. `runHostedSmoke` at `scripts/hosted_smoke.mjs:396-430` only calls `validateDeploymentPreflight` on caller values and hard-coded C09 SHA/deployment/DB. It does not fetch Cloudflare deployment metadata or effective binding, open a browser, authenticate, create a user, save/revise a plan, query D1, or clean up. The command in `submissions/B37.md` passes the *old* C09 code/preview, so running it could never establish B37 exact-code proof. The 14 companion tests passed independently, but exercise helper functions only and cannot supply the missing behavior.
2. **Critical: cleanup and readiness helpers cannot safely establish the claimed journey.** `USER_TABLES` at `scripts/hosted_smoke.mjs:34-50` names tables such as `profiles`, `accounts`, `balances` and `user_sessions` that are absent from `migrations/`; it includes `users`, whose tombstone must remain, while omitting several actual user-scoped tables. `verifyCleanup({}, 404)` would pass because it never requires the exact 15 counts. `waitForPlanningWorkspaceReady` accepts `expectedPlanId` but never verifies it against URL or loaded plan. No actual UI, tenant or cleanup flow calls these helpers. The C09 Version 2 → Revise → Version 3 and downstream B28 criteria remain **UNOBSERVED**, not PASS.
3. **Evidence provenance is overstated.** `docs/execution/evidence_manifest.json` assigns the new C09 preview to historical C01 screenshots; that is not their source preview. `auditReviewLinks` checks only this machine's disk, where `git rm --cached` deliberately leaves files; it cannot prove links or artifacts resolve in a clean clone. Two sampled manifest entries *are* retrievable with `git show <commit>:<path>` and match their hashes, so the index-only removal itself need not be reversed. The submission's “after” full/shallow clone sizes are copies of pre-commit measurements, not post-untracking clone measurements. Reconcile provenance and measure after an actual candidate commit.
4. **Other scope:** `src/lib/fire.ts` is untouched, and the new TypeScript golden tests are in `src/lib/fire.test.ts`. The Python retirement and CI edits appear bounded to the intended legacy stack. I did not rerun the full suite because the hosted runner's no-op success is already a decisive acceptance failure; Gemini reports a local green suite, which must be rerun on the corrected final candidate. Historical docs referencing Python can remain dated records, but current normative docs must not send contributors to removed paths.

## Required correction and acceptance gate

Follow [B37 focused rework](../B37_REWORK_PROMPT.md). Preserve the valid legacy/test and OA-2 index work. Build a real, fail-closed shared hosted smoke using the isolated preview and disposable Clerk tenants; prove its no-credential/no-metadata command exits nonzero. It must verify exact deployed SHA and effective D1 binding before writes, execute the C09 revise/Version 3 and downstream B28 journey, and independently verify scoped cleanup. Fix schema list and negative tests. Correct manifest provenance and make retained evidence retrieval reproducible outside this machine; measure post-commit clone sizes truthfully. Provide a durable criterion matrix and full-suite log. Astra will review the corrected diff, commit/push a candidate only after safe preflight, deploy a fresh isolated preview, run the hosted proof, and decide B37 closure. Do not mark B37 done or release B39. No production data, main merge or production deployment is authorized.

## Owner scope amendment after this review — 2026-09-26

The findings above stand, but their ownership changed before any Gemini rework was sent. Findings 1–2 and all hosted execution move to B42 after B39. B37 now needs only the hygiene corrections in [the narrowed prompt](../B37_REWORK_PROMPT.md): Git-derived manifest provenance/retrieval, current normative docs, removal of unverified post-commit clone claims and a Python-free full suite. The untracked `scripts/hosted_smoke*` files may remain untouched for B42; they must not be part of B37 acceptance. Astra alone performs B42's future hosted run and records its result. B37 remains changes_requested for hygiene, not for missing worker-hosted proof.
