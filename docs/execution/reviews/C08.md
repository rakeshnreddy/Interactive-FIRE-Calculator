# Checkpoint C08 independent review

Reviewer: primary reviewing session. Date:2026-09-17.
Decision: CHANGES_REQUESTED for B26 and B27. C09 remains locked.
Code candidate:a95053b19108634656aef491e46b9be9fe3ea57d.
Actual evidence HEAD:97c1fe521e9692cde8399f8ae4232549942e56c6 (the full evidence SHA in the chat handoff does not match git).
PR140: https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140 ; base codex/dependency-security-refresh. Reviewed product range8fd0b1b..a95053b; evidence range a95053b..97c1fe5.
Preview:https://f737cfbb.interactive-fire-calculator.pages.dev ; deployment f737cfbb-0d0f-4ffc-9a43-5e3cda77d31a.

## Independent checks actually performed

Read master/start/task contracts, submission matrix/digest, actual source diff and verification harness. Rechecked GitHub run35180566369: success on exact candidate. Evidence HEAD changes only docs/evidence/status after candidate. Ran both added presentation suites:16 tests passed in2 files. Did not repeat the full suite or create additional users: the submitted harness has deterministic fail-open defects that must be repaired first. No new hosted user journey claimed by this review.

Compared all10 light/dark image pairs byte-for-byte: all identical. Inspected actual320px account screenshot: it ends at summary cards before the changed date/history controls. See [independent audit](../evidence/C08/primary-evidence-audit.json). Inspected actual app mode handling: finpath.colorMode persisted by React; emulated OS preference alone is not proof of active app mode.

Read-only provider/deployment and scoped cleanup verification recorded separately in [primary cleanup](../evidence/C08/primary-cleanup-verification.json). This confirms cleanup of the submitted user; it does not make the unsafe cleanup harness acceptable for another run.

| Task | Checked | Decision | Required correction |
|---|---|---|---|
| B26 | cents/stale/profile implementation,16 combined tests, dashboard observation, rendered evidence, hosted harness | CHANGES_REQUESTED | R1 evaluator/cleanup safety, R2 true themes and visible changed controls, R3 actual UI update/reload and required state checks |
| B27 | currency/sign implementation, import observations, duplicate handling, evidence coverage | CHANGES_REQUESTED | R1 fail-closed verification, R2 real visual/keyboard evidence, R3 retry/error cases and complete balance immutability |

## Findings

**R1 — high priority, false acceptance and unsafe failure cleanup.** run_c08_proofs.mjs:490 assigns SUCCESS without evaluating mandatory observations. report.json records dashboard_renders_accounts=false despite SUCCESS. Lines519–580 neither revoke success for cleanup failures nor gate provider deletion on verified app deletion. Deleting the identity after a failed purge can strand records. Global table counts also incorrectly assume an empty shared preview. Add explicit evaluator, scoped cleanup gate and injected-failure tests before another hosted execution.

**R2 — required visual evidence absent.** Lines338–367 and transaction capture use emulateMedia without verifying app theme; all10 pairs are identical. fullPage:false at top leaves changed controls below fold. Thus light/dark, mobile date legibility and no-overlap claims are unsupported. Keyboard/reduced-motion/transparency/contrast observations are also missing from the matrix/harness. Obtain true themes and focused changed-control/state evidence; retain owner zoom/reader deferrals.

**R3 — missing behavioral proof.** B26 claims add/update/reload but harness only posts account creation out of band; no UI account update is exercised. Dashboard false may be a bad selector (first summary grid) rather than product failure; diagnose instead of deleting the assertion. B27 balances_unchanged compares count only, allowing changed values to escape. Required loading/error/retry and broader state coverage is not evidenced by the happy-path screenshots or SSR string tests. Use local fixtures for deterministic negative states, and bounded real hosted actions for persistence.

**R4 — packet precision.** Correct evidence SHA from git, preserve actual failures, and generate final decisions only from explicit evaluated observations. The success label currently exceeds the evidence.

## Visual judgment

No complete final visual score is justified by these captures: actual dark mode and below-fold changed controls were not shown. The inspected mobile top section has legible heading/copy and contained identity wrapping; that is a narrow positive observation. Presentation changes are small and should be preserved. “Dashboard overview is active” remains generic copy; improving it is optional, not a new closure blocker. Existing formula/core/API code is unchanged. Zoom and reader checks remain deferred; no WCAG-conformance claim.

## Closure actions

No tasks accepted;23/34 remain accepted (67.6% unweighted task count). B26/B27 changes_requested, backlog unchecked. C08 stays released for bounded rework; C09 locked. No owner input needed. No main merge or production deployment. Execute [consolidated rework prompt](../C08_REWORK_PROMPT.md); preserve passing implementation and evidence. Primary will spot-check corrected assertions and safety outcomes instead of repeating unrelated verification.
