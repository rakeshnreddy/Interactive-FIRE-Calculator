# Checkpoint C01I independent review

Reviewer: primary reviewing session / sole closure owner
Date: 2026-09-11
Decision: CHANGES_REQUESTED, with a separate owner authorization blocker. B33 is not accepted.
Reviewed revision: `2e2b55d110e243a8e12d22c66d6c0e5d1f1f0271`; range `452fbdc..2e2b55d` adds audit tooling and documentation only. Product/configuration tree is unchanged.
PR: https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140; base `codex/dependency-security-refresh`.
Latest static preview: https://03cba125.interactive-fire-calculator.pages.dev (metadata commit `452fbdca849c28233ba6474a38f36b732949c4a0`). Older API-bearing preview: https://757f65cd.interactive-fire-calculator.pages.dev; its recorded trigger commit is `b2f4399` with dirty worktree, so dd47fa3 is a product-tree attribution, not literal deployed commit metadata.

## Independent checks actually performed

Read submitted audit implementation and JSON, task contract and governance. Compared repository changes against accepted baseline. Independently queried live Pages project and recent deployment metadata using read-only authenticated requests; only allowlisted configuration was saved. Probed public health and signed-out identity responses without user records or writes. [Independent evidence](../evidence/B33/primary-review-metadata.json).

GitHub run [34555982628](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/34555982628) succeeded at `e8a53ace4ad23546b84c19fb93d3faa4aa96290c`. Submission gives an incorrect full SHA suffix; fix it. Reviewed full-suite log; no product changes justify repeating its full suite during this documentation review. That suite does not validate the new B33 audit's logic.

| Task / criterion | Decision | Evidence / correction |
|---|---|---|
| B33 effective isolation | FAIL / owner-blocked repair | Live project preview and production DB both point to a5860350-0a50-4ebe-9f5f-1d9916a908e6. No binding changed during review. |
| Automatic/manual paths | Partial evidence | Current custom filter includes only codex/cloudflare-pages-theme-plan; execution-branch Git events exist but are idle, not successful deployments. Audit must distinguish events from builds and evaluate actual setting, include/exclude rules and unknown states. |
| Migration readiness | Not independently established | verify_b33.cjs hard-codes migrations PASS, count 4 and tables 16; it performs no database schema query. Supplied inventory may be historical evidence but is not generated by this command. Its table list/count also needs a documented system-table counting convention. |
| API boundaries | Bounded observations only | 757f65cd health returns JSON 200 and /api/me JSON 401. 03cba125 has uses_functions=false; both paths return HTML 200. This is static routing, not auth failure or API success. State each preview's role precisely. |
| Overall evaluator | FAIL | Final result depends solely on project DB inequality. Once IDs differ it returns PASS even if endpoint checks fail, migration evidence is missing or preflight is unexpected. It also accepts any different DB rather than the approved target. |

## Required corrections

1. Extract a pure fail-closed evaluator with fixture/CLI tests. Every required check must be known and passing; observed failure cannot disappear behind missing approval. Missing evidence must block. Owner authorization is a separate prerequisite, never inferred from unequal IDs.
2. Collect schema/migration evidence read-only from the approved preview DB, or mark it unavailable. Compare migration names with repository files. Do not label a fixed literal or unrelated stale artifact live verification. No financial rows or production schema queries are needed for this repair.
3. Evaluate automatic deployment policy comprehensively or return BLOCKED for unsupported policy/patterns. Verify effective deployment bindings, not just project defaults. Existing deployments can retain prior bindings after a project update.
4. Check JSON content type and expected public health schema; for private endpoints verify unauthenticated status without printing bodies containing sensitive information. Static-only previews cannot satisfy API checks. A crashed/missing preflight process with zero PASS lines is not a successful fail-closed check. Production readiness is separate from preview isolation; do not require production to remain unconfigured forever.
5. Correct CI SHA, preview attribution and final tooling revision. Add reproducible collection commands and limitations. Use the approved-target configuration-only bootstrap only after the required scoped owner authorization.

## Visual judgment and regression safety

No UI/product changes in this submission; no new visual score or repeated browser matrix warranted. B32 and earlier accepted tasks are not reopened. Static preview remains suitable for public UI review, not backend readiness. No backend changes or financial writes were performed. No main merge or production deployment.

## Closure actions

B33: changes_requested, with explicit owner blocker retained. No task accepted. Total remains 6/34 (17.6% by task count). C01I remains released for its bounded rework; C02 remains locked.
Owner input needed before remote setup: authorize changing only Pages project interactive-fire-calculator's preview DB binding to existing finpath-preview (0dbad68e-7493-452f-8504-98d4c61ee5da), plus the configuration-only bootstrap with unchanged backend and read-only checks. No production settings, migrations, financial writes or DNS changes are included. This authorization has not yet been supplied.
Next worker instructions: [C01I rework prompt](../C01I_REWORK_PROMPT.md).
