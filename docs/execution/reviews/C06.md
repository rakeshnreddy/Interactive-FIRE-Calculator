# C06 independent review — CHANGES_REQUESTED

Reviewer: primary reviewing session. Date: 2026-09-14 (America/Los_Angeles).
Candidate: `d23def571c3df98571da71aef5541ec4ceb1721e`; submission HEAD: `ec449f5ce11e46a1c83ef926e35180ee5c55ff92`.
PR: [140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140), base `codex/dependency-security-refresh`; checkpoint range `458d739..ec449f5`.
Immutable C06 preview: not supplied. Existing C05 preview does not establish changed C06 backend behavior.

## Independent checks actually performed

Read candidate production changes, harness/test assertions, fixture wiring, submissions and release contract. Confirmed later HEAD changes only submissions/status. Inspected exact hosted [CI run 34917339958](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/34917339958): FAILURE, four test files failed. Three suites cannot bundle `node:sqlite`; two fixture assertions fail because dist does not exist before the build step.

Executed two deterministic interleaving probes against actual migrated SQLite and actual repository functions. Command: copy evidence/C06-review/reviewer-probes.ts.txt to src/c06ReviewerProbe.test.ts, then `npx vitest run src/c06ReviewerProbe.test.ts`. Both fail for intended assertions, exit 1. Source and [raw output](../evidence/C06-review/race-probe.log) are retained; the temporary test was removed from normal suite discovery. No hosted writes. These failures are evidence, not approved permanent tests: implementer must promote appropriate regressions and prove green after repairs.

| Task | Decision | Required correction |
|---|---|---|
| B05 | CHANGES_REQUESTED | R1 reproducible Node/D1 execution and exact CI; R5 final combined packet. Preserve actual-handler two-user tests. |
| B07 | CHANGES_REQUESTED | R2 write/delete and export races; R4 recovery truthfulness; R5 candidate publication/evidence. |
| B25 | CHANGES_REQUESTED | R1 fresh build checks; R3 actual theme/network/render assertions; R5 durable browser evidence. |

## Consolidated original-scope findings

- **R1 [P1], confidence 10/10:** `src/test/d1TestHarness.ts:3` imports `DatabaseSync` from `node:sqlite` into default jsdom test configuration; exact hosted CI fails module transformation. `src/fixtures/fixtureSeparation.test.ts:43` asserts an existing dist before `scripts/test_all.sh` runs `npm run build`. Local stale output cannot establish fresh candidate isolation. Separate backend Node tests from DOM cleanup tests and verify a freshly generated build deterministically.
- **R2 [P1], confidence 10/10:** `functions/_lib/persistence.ts:42` reads deletion state before separate writes; `functions/_lib/accounts.ts:184` calls that guard before `await database.batch(statements)` at line 232. Probe deletes immediately before account batch; one financial account row is committed for tombstoned user despite subsequent error. `functions/_lib/accountData.ts:249` onwards exports separate awaited reads. Second probe deletes after financial account read: returned export contains one account and null user. This disproves the documented single-snapshot guarantee. Atomic persistence enforcement and consistent export are mandatory, not optional hardening.
- **R3 [P1], confidence 10/10:** `src/fixtures/FixtureApp.tsx:133–134` sets `data-theme`/`theme-dark`, while real tokens in `src/vivid-theme.css` require `.app` and `.app[data-mode="dark"]`. Render wrappers at lines 219/286 lack that contract. Reported theme PASS is unsupported by actual wiring. `fixtureSeparation.test.ts` says `// Mock the trap installed by FixtureApp`: it tests its own fetch replacement, not shipped fixture behavior. URL regex and six-element array assertions do not render URL-selected states. Exercise the actual entry/actions and canonical theme wrapper.
- **R4 [P2], confidence 10/10:** `docs/DATA_DELETION_AND_RECOVERY.md` claims an atomic export not implemented, includes non-existent `accounts` table in restore SQL, and tells operator to obtain an authoritative tombstone stream without identifying an implemented durable source. Recovery must remain blocked if current deletion history is unavailable; a list supplied in a synthetic test is not an operational backup solution. Correct API symbol/code and retention claims to actual implementation; do not invent legal policy.
- **R5 [P1], confidence 10/10:** submissions label changed production Functions/App code as local-only, omit exact successful CI and immutable preview, and list browser PASS without durable captures/logs. No C06/B05/B07/B25 evidence directory existed at receipt. This violates the final-candidate and B33-before-publication requirements. Do not reuse C05 deployment for these changes.

## Visual judgment

Not scored: no trustworthy rendered browser packet was submitted, and theme wiring fails source inspection. No exhaustive browser retest performed by primary. Worker must supply objective evidence and visual judgments after correction. Actual screen-reader checks remain owner-deferred to B31/C11; all other applicable gates remain.

## Safety and closure actions

No tasks accepted this pass; B05/B07/B25 returned to changes_requested. Prior 19/34 accepted (55.9%, task count only) unchanged. C07 remains locked. No previous task reopened: current findings belong to pending C06. src/lib/fire.ts unchanged in reviewed range. No merge, preview publication, production deployment or paid services performed by reviewer.

Next action: implementing agent executes [C06_REWORK_PROMPT.md](../C06_REWORK_PROMPT.md), preserving passing work. This is the first consolidated C06 rework request; protocol retry ownership rule remains applicable. No owner secret needed for local repairs. Any proposed remote migration or unavailable provider authorization must be separately surfaced after local work; no production changes allowed.
