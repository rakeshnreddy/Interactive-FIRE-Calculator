# C06 independent review — CHANGES_REQUESTED

Reviewer: primary reviewing session. Date: 2026-09-14 (America/Los_Angeles).
Candidate: `d23def571c3df98571da71aef5541ec4ceb1721e`; submission HEAD: `ec449f5ce11e46a1c83ef926e35180ee5c55ff92`.
PR: [140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140), base `codex/dependency-security-refresh`; checkpoint range `458d739..ec449f5`.
Immutable C06 preview: not supplied. Existing C05 preview does not establish changed C06 backend behavior.

## Independent checks actually performed

Read candidate production changes, harness/test assertions, fixture wiring, submissions and release contract. Confirmed later HEAD changes only submissions/status. Inspected exact hosted [CI run 34917339958](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/34917339958): FAILURE, four test files failed. Three suites cannot bundle `node:sqlite`; two fixture assertions fail because dist does not exist before the build step.

Executed two deterministic interleaving probes against actual migrated SQLite and actual repository functions. Command: copy evidence/C06-review/reviewer-probes.ts.txt to src/c06ReviewerProbe.test.ts, then `npx vitest run src/c06ReviewerProbe.test.ts`. Both fail for intended assertions, exit 1. Source and [raw output](../evidence/C06-review/race-probe.log) are retained; the temporary test was removed from normal suite discovery. No hosted writes. These failures are evidence, not approved permanent tests: implementer must promote appropriate regressions and prove green after repairs.

| Task | Decision | Required correction |
|---|---|---|
| B05 | CHANGES_REQUESTED | R1 reproducible Node/D1 execution and exact CI; R5 final combined packet. Preserve actual-handler two-user tests. |
| B07 | CHANGES_REQUESTED | R2 write/delete and export races; R4 recovery truthfulness; R5 candidate publication/evidence. |
| B25 | CHANGES_REQUESTED | R1 fresh build checks; R3 actual theme/network/render assertions; R5 durable browser evidence. |

## Consolidated original-scope findings

- **R1 [P1], confidence 10/10:** `src/test/d1TestHarness.ts:3` imports `DatabaseSync` from `node:sqlite` into default jsdom test configuration; exact hosted CI fails module transformation. `src/fixtures/fixtureSeparation.test.ts:43` asserts an existing dist before `scripts/test_all.sh` runs `npm run build`. Local stale output cannot establish fresh candidate isolation. Separate backend Node tests from DOM cleanup tests and verify a freshly generated build deterministically.
- **R2 [P1], confidence 10/10:** `functions/_lib/persistence.ts:42` reads deletion state before separate writes; `functions/_lib/accounts.ts:184` calls that guard before `await database.batch(statements)` at line 232. Probe deletes immediately before account batch; one financial account row is committed for tombstoned user despite subsequent error. `functions/_lib/accountData.ts:249` onwards exports separate awaited reads. Second probe deletes after financial account read: returned export contains one account and null user. This disproves the documented single-snapshot guarantee. Atomic persistence enforcement and consistent export are mandatory, not optional hardening.
- **R3 [P1], confidence 10/10:** `src/fixtures/FixtureApp.tsx:133–134` sets `data-theme`/`theme-dark`, while real tokens in `src/vivid-theme.css` require `.app` and `.app[data-mode="dark"]`. Render wrappers at lines 219/286 lack that contract. Reported theme PASS is unsupported by actual wiring. `fixtureSeparation.test.ts` says `// Mock the trap installed by FixtureApp`: it tests its own fetch replacement, not shipped fixture behavior. URL regex and six-element array assertions do not render URL-selected states. Exercise the actual entry/actions and canonical theme wrapper.
- **R4 [P2], confidence 10/10:** `docs/DATA_DELETION_AND_RECOVERY.md` claims an atomic export not implemented, includes non-existent `accounts` table in restore SQL, and tells operator to obtain an authoritative tombstone stream without identifying an implemented durable source. Recovery must remain blocked if current deletion history is unavailable; a list supplied in a synthetic test is not an operational backup solution. Correct API symbol/code and retention claims to actual implementation; do not invent legal policy.
- **R5 [P1], confidence 10/10:** submissions label changed production Functions/App code as local-only, omit exact successful CI and immutable preview, and list browser PASS without durable captures/logs. No C06/B05/B07/B25 evidence directory existed at receipt. This violates the final-candidate and B33-before-publication requirements. Do not reuse C05 deployment for these changes.

## Visual judgment

Not scored: no trustworthy rendered browser packet was submitted, and theme wiring fails source inspection. No exhaustive browser retest performed by primary. Worker must supply objective evidence and visual judgments after correction. Actual screen-reader checks remain owner-deferred to B31/C11; all other applicable gates remain.

## Safety and closure actions

No tasks accepted this pass; B05/B07/B25 returned to changes_requested. Prior 19/34 accepted (55.9%, task count only) unchanged. C07 remains locked. No previous task reopened: current findings belong to pending C06. src/lib/fire.ts unchanged in reviewed range. No merge, preview publication, production deployment or paid services performed by reviewer.

Next action: implementing agent executes [C06_REWORK_PROMPT.md](../C06_REWORK_PROMPT.md), preserving passing work. This is the first consolidated C06 rework request; protocol retry ownership rule remains applicable. No owner secret needed for local repairs. Any proposed remote migration or unavailable provider authorization must be separately surfaced after local work; no production changes allowed.


## Resubmission review — 2026-09-14, CHANGES_REQUESTED

Candidate `2d4bbaf60a7377e1e543a66bbc6c319483aeddd2`, submission HEAD `579dd11`. Reviewed repaired deletion/export paths, migration 0006, race tests, fixture guard/theme changes, browser collector and report; inspected actual accounts-320-light and dashboard-desktop-dark screenshots. Primary reran `npx vitest run src/accountDeletionRaces.test.ts src/deletionAndRecovery.test.ts src/localDraftCleanup.test.ts`: **18/18 passed**. Preserve these fixes. Exact hosted CI [34922199841](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/34922199841) is SUCCESS at `1cfd4adaadc9e185273581882023e8750274fb41`, whose product/config code matches candidate. It adds the browser collector/evidence; do not describe this as an identical executable tree. CI covers full suite/build, not proof that browser assertions are meaningful.

R1 runtime/build failures are resolved by current evidence. R2 existing-user write race and export batch repair pass focused tests. Canonical theme wrapper and actual network guard are improved. Recovery documentation now acknowledges unavailable authoritative deletion history; hosted recovery remains unproven. These are provisional verified improvements, not task acceptance.

### Remaining consolidated blockers

1. **R2 [P1], confidence 10/10 — first-use deletion still permits resurrection.** `deleteTargets` in functions/_lib/accountData.ts uses `UPDATE users SET deleted_at = ?, updated_at = ? WHERE id = ?`. If authenticated identity has no D1 user row yet, deletion returns success but stores no tombstone. Calling ensureUserProfile afterward succeeds. Primary reproduced this on real migrated SQLite: [probe](../evidence/C06-review2/missing-identity-probe.ts.txt), [exit-1 log](../evidence/C06-review2/missing-identity-probe.log). Existing test pre-inserts a tombstone, so it misses the explicitly requested deletion-before-initialization case. Atomically persist tombstone even for absent user; retain transaction rollback and other-user isolation. No auth bypass required to reproduce.
2. **R3 [P1], confidence 10/10 — browser PASS claims still exceed observations.** capture_fixtures.cjs uses `deviceScaleFactor: 2` under “Native Zoom 200%”; that changes pixel density, not browser UI zoom. Tablet, adverse, zoom and media rows contain unconditional `passed: true`. Mobile collects overflow but excludes it from pass. Reduced transparency is not emulated/measured; contrast is not measured; focus check merely excludes the string `none`, which allows body focus. Collector prints failures without returning nonzero. Thus 47/47 is not an acceptance result. Actual accounts 320 screenshot shows toolbar consuming most viewport, with almost all account controls below capture. Dashboard dark screenshot shows changed theme but clipped/low-contrast debug URL in toolbar. Inspecting the product below toolbar is mandatory; this is a fixture usability/evidence defect, not a new product redesign request.
3. **R5 [P1], confidence 10/10 — packet/publication remains incomplete.** Submissions reference screenshot names absent from actual packet; use report paths rather than invented names. No immutable C06 preview/binding proof; migration 0006 requires owner authorization. ready_for_review conflicts with admitted mandatory publication blocker. All C06 tasks remain open. Do not infer authorization for 0006 from the earlier authorization for 0005.

Visual outcome: theme repair corroborated in source and desktop screenshot; overall visual/accessibility score withheld because native zoom/media/contrast and essential controls are not evidenced. Actual reader deferral unchanged. No full suite rerun by primary because trustworthy CI exists and no product changes were made in this review.

Next: execute C06_REWORK_2_PROMPT.md. This is the clarified contract after first failed rework per protocol §6. If the same bounded defect survives the next rework, primary takes ownership or supplies a tested patch instead of another identical cycle. No new optional features added. No current tasks accepted; 19/34 (55.9%) unchanged; C07 locked. No remote migration, deployment or merge performed. Owner authorization for exact 0006 preview migration will be requested after the local candidate and migration plan are reviewable; local repairs can proceed now.


## Second rework review / primary repair — 2026-09-15 UTC

Candidate `7b5cc31f2cc6ffae264d915b1a4de6d0bcfe862e`. Decision: **BLOCKED; no task closure**.

Backend independently checked by delegated verification agent: 20/20 accountDeletionRaces/deletionAndRecovery tests pass, including actual missing-user deletion, 29 trigger protections and other-user isolation. Primary independently confirmed exact candidate hosted CI [34930889299](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/34930889299) SUCCESS. Previous packet CI link pointed at an older revision; this current run supersedes it. R2 missing-user repair is verified locally. Preserve these improvements.

Browser evaluator still accepted missing contrast/rendered evidence, NaN ratios, missing geometry widths and a native-zoom boolean without observed level/proof. Rendered loading and unsupported combinations still passed unconditionally. Per protocol §6 primary took ownership and supplied a tested patch rather than a third identical rework request. Added 15 evaluator regressions and four rendered-state regressions: intended failures retained as red logs, all 19 pass after correction. Existing evaluator tests also pass. Full ./scripts/test_all.sh after executable repairs passes (13 runner, 79 Python +21 subtests, 47 Vitest files/1608 tests, typecheck/build/isolation).

Artifacts: evidence/C06-primary/{evaluator-red.log,evaluator-green.log,rendered-red.log,rendered-green.log,legacy-evaluator-green.log,full-suite.log}. Collector now imports tested rendered-expectations module; missing/unknown loading/stale observations cannot manufacture state passes. Contrast parser rejects uncomposited transparent/unknown colors; debug ratio failure is checked. This corrects acceptance mechanics, not a claim that every glass text/background combination has been measured.

Re-evaluated the unmodified worker raw packet with stricter evaluator: 45 PASS /5 FAIL due to missing rendered-state/contrast observations in native zoom, motion, transparency, keyboard and network cases. Native zoom additionally remains unavailable in worker CLI. This is a diagnostic reevaluation, not final visual acceptance; earlier ordinary rows also require meaningful text/geometry/composited contrast corroboration. Raw worker report retained. Primary retains ownership of remaining bounded verification; no task marked done or C07 released.

Primary remote read-only check using explicit preview environment: only migration 0006 pending. Raw log in evidence/C06-primary/remote-pending-migrations.log. Rewrote migration-0006-authorization-packet.md with tracked application, stop if pending set changes, exact preview UUID, schema-before-backend order, and rollback preserving guards. Removed unsafe DROP TRIGGER rollback claim. No remote mutation performed.

Owner input needed: explicit authorization to apply ONLY migrations/0006_user_tombstone_triggers.sql to isolated finpath-preview (0dbad68e-7493-452f-8504-98d4c61ee5da). This does not authorize production, main merge or unrelated migrations. Preview publication and final C06 acceptance follow only after remaining verification gates pass. Existing reader deferral remains. Progress unchanged:19/34 accepted (55.9%).


Native supplement arrived from cheaper verification agent: evidence/C06-primary/native/observations.md and four screenshots. Primary inspected actual Chrome popup showing 200% and accounts dark long-value capture. Native capability is now proven, but the screenshot shows a sticky fixture toolbar covering form content and CSV controls cut at the right edge; AX presence alone does not establish visible usability. This is not a passing native layout check. Preserve evidence; fix fixture header/reflow and recapture before closure. No reader claim. Backend remains verified locally. Primary retains repair ownership; do not send another generic C06 implementation cycle.


## Final primary decision — 2026-09-15: APPROVED within C06 scope

B05, B07 and B25 are accepted together at `0f3ae8d9cc4d447518e484b8de7a34ee1b54f38a`. This supersedes prior C06 blockers. PR140 remains unmerged, base codex/dependency-security-refresh. Immutable preview: https://5e68409d.interactive-fire-calculator.pages.dev. Exact candidate CI34961420359 is SUCCESS.

Evidence: [final packet](../evidence/C06-final/deployment.json), [full suite](../evidence/C06-final/full-suite.log), [migration readback](../evidence/C06-final/migration-summary.json), [focused raw browser report](../evidence/C06-final/special-report.json), [public routes](../evidence/C06-final/public-smoke.log), [hosted API boundaries](../evidence/C06-final/hosted-boundaries.json). Full suite after final CSS repair:13 runner tests,79 Python +21 subtests,1608 Vitest tests, TypeScript/build/fixture isolation pass. Primary evaluator/rendered-state19 regressions pass. Backend code is identical to independently verified7b5cc31; final product delta is two bounded responsive CSS rules. FIRE core remains unchanged.

- B05: actual local D1 two-user boundary and current auth tests pass; no test seam in production build. Hosted health200 and eight protected GET APIs401. Hosted authenticated lifecycle is expressly B06, not claimed here.
- B07: missing-user tombstone, delayed-write triggers, atomic export, rollback and other-user preservation are verified by retained real SQLite handler tests and full suite. Owner-approved0006 applied ONLY to isolated finpath-preview;29/29 trigger SQL definitions match and ledger records migration6. Effective deployed DB UUID0dbad68e-7493-452f-8504-98d4c61ee5da verified. Recovery remains fail-closed without authoritative current deletion history; no operational backup guarantee or hosted restore claim.
- B25: actual component theme/state wiring and production exclusion tests pass. Original45 ordinary cases retain their recorded observations; missing special-case observations replaced with four strict passing cases (motion, transparency, actual keyboard focus/occlusion, network mutation trap). Primary inspected final dark dashboard and retained account light/dark layout captures. Normal layouts expose useful real product controls, readable financial cards and clear synthetic labels. Representative actual product headings measure14.37–14.65:1. This is bounded fixture acceptance using existing accepted theme tokens, not an exhaustive new contrast audit or authenticated product certification. Fixture content polish belongs to subsequent operational-page tasks.

Owner priority amendment now defers native200% testing and zoom-only issues through C10 to final B31/C11, alongside the previous actual-reader deferral. Existing zoom repairs/evidence retained; not a reason to repeat all checks. No full WCAG claim.

One genuine next-stage failure is preserved: hosted mobile /dashboard renders “Account features are currently unavailable.” It has no console error/overflow, but does NOT pass working sign-in acceptance. Local test build omitted usable frontend auth configuration; provider key presence alone did not establish built-client configuration. B06/C07 must fix secure preview build/auth configuration and prove disposable hosted sessions/lifecycle. C06 does not require hosted sign-in to pass.

22/34 accepted (64.7%, task count, not effort). C07 remains locked pending its secure setup and disposable-user write authorization; all current C06 tasks are complete. Next: C07_PREPARATION_PROMPT.md permits read-only diagnosis while locked. No production/main changes or paid services. Rollback only reviewed application code to prior isolated preview if necessary; retain tombstone migration/guards, never remove deletion protections.
