# B37 narrowed hygiene review — 2026-09-26

Reviewer: Astra. Decision: **CHANGES_REQUESTED**. B37 remains open; B39 is locked. Gemini's uncommitted candidate is preserved. HEAD during review was `60ec03b45cc523d124e012e02a2255c88448080c`; the submission's `cb2422d` is an older baseline. No B37 commit, CI or preview exists. The C09 preview is not B37 evidence.

## Independent findings

1. **Manifest origin is wrong for seven B17 screenshots.** `scripts/generate_evidence_manifest.mjs` uses `git log -1`, which returns the latest modifying commit. All seven B17 paths currently attributed to `7521f25a7b78141b74cf0b478fb6cf777e459935` were first added by `9d1d257f4598114807f36fe25b1d3ea16fdd78d0`. The bytes differ: for `B17-after-desktop-dark.png`, addition SHA-256 is `2d77c48326bb81c1f27b6380e81f085e092b0e4fe85729d61f9eb305cce7af3e`, later screenshot SHA-256 is `02911052ae17804f3efaff6828e1f2926078b4faee6c610fc0030ef12526e00f`. Both revisions must be identified truthfully and retrievable. The current `--verify` only proves the later object matches its own hash, not first-add provenance.
2. **Evidence-link claim is overstated.** Directly running `auditReviewLinks` returned 11 MISSING, 1 UNTRACKED_LOCAL_ONLY, 156 TRACKED_IN_INDEX and 46 RESOLVABLE_VIA_GIT_OBJECT. Two missing paths are actual evidence references, not globs: `docs/execution/evidence/B09/interactions.json` in the accepted B09 matrix and `docs/execution/evidence/B20/verification-report.json` in the B20 working contract. The C03/B19 interactions files and `B20/verify_b20_results.json` are tracked potential replacements, but their contents must be checked before changing links. The remaining ten unresolved matches are patterns, directories or line-suffixed source references; classify them individually instead of asserting all twelve are globs.
3. **Current normative state remains stale.** `docs/PROJECT_MEMORY.md` near line 55 still says the Flask/Jinja paths remain for reference, contradicting the new current-state text near lines 353 and 430. Reconcile the current-state inventory while retaining dated historical records. `docs/FINANCIAL_PLATFORM_HANDOFF.md` has legacy run guidance; identify whether it is clearly dated/historical, or label it so readers cannot mistake it for live instructions.
4. **Submission accounting needs correction.** Current HEAD has 1,555 tracked paths; the staged index has 1,500 after 55 index deletions. The submission compares 1,552 paths at older `cb2422d` to 1,500 now, then describes three untracked files as staged additions. Use one named baseline per metric, separate committed docs changes from Gemini's untracked files, and do not infer post-commit clone sizes. The full-suite success is worker-reported; a durable raw log with command, exit code and Python-free PATH evidence is missing from the packet.
5. **Finance-golden retirement evidence is incomplete.** The 13 new test cases are useful, but the submission calls them byte-for-byte parity with the old suite. The old `tests/test_finance_core.py` also contains start-of-year one-off income, single-period portfolio/expense goldens, and varied-return/duration scenarios. Inventory every old `test_*` scenario against existing/new TypeScript assertions; port any independent numerical case not covered, or explicitly justify an intentional semantic divergence. Do not change `src/lib/fire.ts`.

Confirmed bounded work: all 53 PNGs and two bytecode files remain on disk after index-only untracking; the 53 currently named Git objects verify their recorded hashes; `src/lib/fire.ts` is untouched; packet validator passes 42 tasks/19 checkpoints; `git diff --check` and `git diff --cached --check` pass. No hosted PASS was claimed and `scripts/hosted_smoke*` remains excluded from B37. Because the manifest and audit have decisive faults, I have not repeated the full suite on this rejected candidate. Astra will run it on the corrected candidate and measure post-commit clones only after acceptance.

Use [the focused correction prompt](../B37_HYGIENE_CORRECTION_PROMPT.md). Do not mark B37 done, release B39, commit worker files, deploy or perform hosted writes.
