# C09 hosted attempt at candidate 7467116 — 2026-09-25

Decision: **CHANGES_REQUESTED for B11/B28. C09 remains open and C10 locked.** Do not mark tasks done. This is the first hosted synthetic attempt on product commit `74671167fa6f373ccb8f979efad7efd1b188f66e`, immutable preview `https://11955f4b.interactive-fire-calculator.pages.dev`, deployment `11955f4b-c177-4098-a102-13592a1e07da`, isolated preview D1 `0dbad68e-7493-452f-8504-98d4c61ee5da`. PR #140 exact-code GitHub Actions run [36216063911](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/36216063911) passed. The reviewer independently ran `./scripts/test_all.sh` (14 Node runner, 5 preview-auth, 79 Python plus 21 subtests, 1,686 Vitest, typecheck/build clean) and the 55 collector tests; both passed. The preview-auth build passed. Cloudflare deployment preflight independently verified the exact SHA, preview environment, URL and D1 binding before synthetic writes.

The hosted runner authenticated both synthetic users and reached B10 navigation, review deferral, keep-review persistence, and the B28 review-panel clearance step. It then stopped before Version 2 `revise` because `reviewPanelLocator.locator('.review-badge').boundingBox()` is a strict Playwright locator matching **three** elements: the panel's `Up to Date` status badge and `KEEP`/`DEFER` history badges. `report.json` is `FAILED`; downstream B11/B28 fields are unobserved, not failed product behavior. The exact error and partial screenshots are preserved under `docs/execution/evidence/C09/attempt-7467116/`. Historical tracked report/screenshots were restored after copying this failed attempt, so old evidence is not misrepresented as the new run.

Cleanup passed both in the runner and in an independent reviewer read: for each synthetic user, all 15 scoped preview D1 tables have zero rows, a non-null tombstone remains, and Clerk development `getUser` returns 404. No synthetic user remains. No production DB, main merge, migration, payment, DNS or paid service was touched.

Required bounded Gemini repair: scope the clearance badge to the review panel's header status badge and require exactly one match before measuring. Audit other strict locators in the remaining hosted stages for multiple matches caused by review history; use exact semantic/card scoping rather than an arbitrary global `.first()`. Add a local DOM/Playwright regression with one header badge and at least two history badges so the original selector fails and the corrected selector passes. Preserve all 55 collector tests and all product behavior. Rerun `./scripts/test_all.sh` and `git diff --check`. Keep B11/B28 blocked pending a **new exact-code preview** and full hosted proof. See `docs/execution/C09_HOSTED_LOCATOR_FINISH_PROMPT.md`.
