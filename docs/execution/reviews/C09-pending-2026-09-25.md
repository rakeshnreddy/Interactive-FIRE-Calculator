# C09 follow-up review — final proof pending

Reviewer: primary reviewing session
Date: 2026-09-25
Decision: BLOCKED for B10, B11, B28; C10 remains locked.
Final combined product code candidate: `5dda3d2be24246e3470a65e7653a0b6e425cbece` on PR [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140), base `codex/dependency-security-refresh`.
Immutable combined preview: [51acbf88](https://51acbf88.interactive-fire-calculator.pages.dev); verified preview D1 UUID `0dbad68e-7493-452f-8504-98d4c61ee5da`. Stable beta alias points to this same website code. The old C09 authenticated report used code `0fe20e8e55c49808c998ac751e149da65c7eb3d5` on preview 3b006fb1 and is historical evidence only.

## Independent checks

- Reviewed [the original C09 findings](C09.md), R1–R6 repair source and targeted tests. The corrective unique index is in `migrations/0008_plan_reviews_idempotency.sql`; the API distinguishes exact replay from changed intent, rejects invalid/future evidence dates and schedules from trusted server time. Navigation has explicit invalid-version state and dirty-state guard. Dashboard loads persisted due-review state. Goal metadata dates are labeled as metadata updates. These code findings do not substitute for a current hosted journey.
- Gemini repaired the hosted collector's keyboard gap: it now tabs to the actual Workspace menu trigger, sends Enter and checks open DOM/ARIA state, then Escape and checks closed state. Primary confirmed the target exists in `src/App.tsx` and ran `node --test docs/execution/evidence/C09/run_c09_proofs.test.mjs`: **22/22 passed**, including the new focus-without-activation negative case. `git diff --check` and packet validation passed. The hosted action has not yet been observed on current preview.
- The exact B35 candidate passed the full suite (55 Vitest files, 1,685 tests; 79 Python plus 21 subtests; 19 Node runner tests; typecheck and build) and hosted CI [36112726641](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/actions/runs/36112726641). The same candidate passed 84/84 public smoke paths, but these do not prove signed-in C09 flows.
- The C09 runner now targets exact SHA `5dda3d2...`, deployment `51acbf88-db0a-47d1-b399-814dad835a9b` and preview DB. Its exact deployment preflight failed before test-user creation because the local Cloudflare OAuth token returned no deployment result. `npx wrangler d1 execute finpath-preview --remote --command 'SELECT 1 as ping;' --json` returned authentication error code 10000. A `npx wrangler login` attempt opened OAuth and timed out without a callback. No synthetic C09 user or data was created in this attempt; the historical cleanup for the 0fe run remains recorded separately.
- Existing migrations 0007 and 0008 were already applied by the earlier implementing session. The visible conversation and packet do not establish exact owner authorization for those historical remote writes. This is an unresolved governance provenance issue; do not reapply or revert them to manufacture approval. The current follow-up performed no migration or production write.

## Remaining gate and exact next action

The owner must complete Cloudflare Wrangler OAuth in the browser when the login command is reopened. Then run the read-only exact deployment/D1 preflight first. If that passes, perform **one** already-authorized disposable synthetic hosted C09 proof on preview 51acbf88, verify B10 exact restore/dirty cancel and confirm, B11 keep/revise/defer/idempotency/date and tenancy behavior, B28 persisted dashboard/goals, keyboard action, light/dark/mobile contrast, followed by 15-table zero-row/tombstone/provider deletion cleanup. Preserve a failed report and recovery manifest if cleanup fails; never lower an evaluator threshold or treat historical 0fe proof as 5dda proof. Native 200% zoom and actual screen reader remain owner-deferred to B31/C11.

Only after all nondeferred criteria pass on the combined candidate may the primary reviewer mark B10/B11/B28 done and release C10. Current accepted count stays **26/35 (74.3%)**. No main merge or production deployment is authorized by this record.
