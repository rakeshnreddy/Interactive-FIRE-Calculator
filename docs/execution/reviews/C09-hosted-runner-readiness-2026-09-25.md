# C09 hosted-runner readiness review — 2026-09-25

Decision: **CHANGES_REQUESTED for B11/B28; do not publish the current candidate.** B10 and B35 remain accepted. C09 stays released for repair and C10 stays locked. This review is of the uncommitted Gemini diff on top of `9c40676`, not an exact-code CI run or deployed candidate.

The worker fixed the real input selector, raw D1 JSON parsing, missing-value rejection, Version 3 reload assertion, single-row immutability checks, `isDuplicate` response, and local fixture isolation/allowlist. The reviewer independently reran the 47 collector tests (47 pass, including loopback fixture test), checked `git diff --check` and confirmed `src/lib/fire.ts` has no diff. The worker reports a green full suite (1,686 Vitest, 79 Python plus 21 subtests, 14 Node runner tests, five preview-auth tests, typecheck/build). Those results are useful, but the current hosted sequence still fails deterministically:

| Priority | Finding and evidence | Required correction |
|---|---|---|
| P1 | The runner creates Version 2 with `result: {success:true, fireNumber:1250000, yearsToFire:17}` (`run_c09_proofs.mjs` setup). Its new `verifyFinancialRevisionDiff` requires `result_json.requiredPortfolio`. Feeding the actual setup shape to the helper returns `passed:false`, `Version 2 missing finite result.requiredPortfolio: undefined`. The endpoint stores `payload.result` verbatim in `result_json`. | Seed Versions 1 and 2 with truthful `calculateFirePlan(snapshot.plan)` outputs from the existing shared formula, including `requiredPortfolio`; do not invent a 1.25m target. Node 24 in this checkout can import `src/lib/fire.ts` directly, or use another deterministic source-backed method. Add a test feeding the exact setup payload shape through the new verifier; prove the v2 target is finite and the v3 target changes. Do not alter the formula. |
| P1 | Before the `revise` review, the runner already creates one `defer` and one `keep` review on the same plan. After the conflicting replay it runs `SELECT * FROM plan_reviews WHERE plan_id = ?` and sends `reviewsAfterConflict.length` to a helper requiring `1`. The observed logical count is 3. Feeding that state to the helper returns `passed:false`, `Conflicting intent expected 1 review row after conflict, found 3`. | Scope the after-conflict query to the persisted revise review's user, plan, Version 2 and idempotency key (or compare the exact review ID), require exactly one matching row with unchanged decision/key/payload, and separately assert total plan review count remains at its pre-conflict value of three. Make `postConflictReview` mandatory in the conflict helper. Add a test with defer + keep + revise rows that catches a new duplicate without rejecting the valid three-row plan history. |
| P1 | The runner still hardcodes `CANDIDATE_SHA=5dda3d2...`, `DEPLOYMENT_ID=51acbf88...`, and `PREVIEW_URL=51acbf88...`. Its deployment preflight rejects any fresh deployment. Updating source constants after committing changes the candidate HEAD and breaks exact-code identity. | Make these three values explicit runtime inputs supplied **after** candidate commit/deploy, with strict validation and no permissive fallback in hosted mode. Preserve recorded historical evidence separately. Unit tests should cover missing values and mismatch fail-closed; the reviewer must be able to pass the committed code SHA, immutable deployment URL, and deployment ID without editing tracked code post-commit. Keep the isolated preview DB UUID pinned and verify it before synthetic writes. |
| P2 | The new four-way hosted clearance check changes `.app[data-mode]` directly and writes `finpath:theme-preference`; the actual React theme state uses `finpath.colorMode`. It also calls `.focus()` on an unfocusable `h2`, catches the error, and still claims keyboard/focus entry. | Use the existing `switchTheme(page, mode)` behavior or the real theme control, assert mode and computed theme differ in each case, and focus a real interactive review control via keyboard. Assert `document.activeElement` and visible bounding box before claiming focus clearance. Keep native 200% zoom and screen reader deferred to B31/C11 per owner instruction. |

The fixture server is now bound to `127.0.0.1`, uses an ephemeral port and exact CSS allowlist; fixture screenshots are separate from hosted screenshots. No evidence of a private-file read was found. The worker restored the previously tracked hosted screenshot. The 47 passing tests do not simulate the actual Version 2 seed or the three-review history, which explains why these failures were missed.

No product/evidence worker files were staged, committed, pushed, or deployed in this review. No synthetic hosted users were created. The last verified preview remains `https://51acbf88.interactive-fire-calculator.pages.dev` at product SHA `5dda3d2be24246e3470a65e7653a0b6e425cbece`. Follow `docs/execution/C09_HOSTED_RUNNER_FINISH_PROMPT.md` for one bounded Gemini correction, then primary review, exact-code CI, isolated preview deploy, hosted proof, cleanup/tombstone verification, and closure decision.
