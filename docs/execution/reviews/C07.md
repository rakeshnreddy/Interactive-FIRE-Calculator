# Checkpoint C07 independent review

Reviewer: primary reviewing session. Date:2026-09-16.
Decision: CHANGES_REQUESTED; external testing credential also missing.
Website candidate: d81f31187892f636ab9d2b6cb492e90e9b7d166d.
Submitted harness/evidence:4a7e785 (resolve full SHA in git).
PR140: https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140; base codex/dependency-security-refresh.
Immutable preview: https://3eed38e6.interactive-fire-calculator.pages.dev.

## Independent checks actually performed

Read MASTER_REVIEW_PROMPT, submission, validation matrix, raw report/run/cleanup files and complete504-line harness. Compared request/response assumptions with real accounts, balances, profile, me, deletion and persistence handlers. node --check passes, which establishes syntax only. git diff d81f311..HEAD for src/functions/migrations/package.json is empty: no product regression rerun justified. The previously verified candidate CI remains historical evidence; this review does not infer executed hosted proofs from green general CI.

report.json has null actual_status and passed:false for all three remaining proofs; run.log exits before signup because testing credentials are missing. No import, write-isolation or410 proof is present. Cleanup JSON on this no-user run repeats historical outcomes, not new runtime cleanup observations. Primary independently queried isolated D1 for the exact historical A/B IDs: all14 user-scoped tables returned0 rows. Existing tombstone verification retained in primary-cleanup-verification.json. No new hosted identities/writes or browser walkthrough performed during this review.

| Task | Criteria | Decision | Required correction |
|---|---|---|---|
| B06 | Remaining CSV import, direct tenant-write rejection, authenticated deletion410, reliable cleanup evidence | CHANGES_REQUESTED | Repair R1–R4 below before adding credentials/running hosted checks. Preserve previous verified work. |

## Consolidated original-scope findings

### R1 — invalid test setup and incomplete authentication preconditions
The harness creates an account with accountType:'depository'; functions/_lib/accounts.ts accountTypes excludes it (use an actual supported value, e.g.checking). Its create response and /api/me200/valid distinct IDs are not asserted before dependent actions. Calls must use proven live Clerk sessions and the same credential method as the app; do not assume anonymous fetch is authenticated. The testing token is placed only on a FinPath page URL without verifying injection into Clerk frontend requests; use documented setupClerkTestingToken integration. Guessed iframe/OTP/phone assumptions must be replaced with actual supported Clerk test flow. Obtain needed development key securely, but do not run current harness merely because a key becomes available.

### R2 — import proof can misreport success
Proof1 records balances GET status as actual_status for expected commit201; it never observes preview200 or commit201, and never asserts these statuses. It uses guessed CSV headers and a broad optional /import/i button. Install response capture before UI action, use actual template/parser, require exactly identified controls, and assert preview200 + commit201 + expected persisted rows/amount/date + tenant after reload. A preexisting matching balance or skipped button cannot pass. Negative tests must prove missing commit, failed commit and wrong imported values fail.

### R3 — cleanup is not fail-closed
finally deletes only B application data, so any error before A's planned deletion can leave A financial data behind while deleting A's provider identity. Cleanup response statuses are ignored; missing count rows default to0. PASS is decided before cleanup and never revoked when identity deletion/tombstone/count assertions fail. Failed D1 reads can prevent report writing/browser closure. Repair cleanup for BOTH manifest IDs, assert application deletion before provider removal, independently verify provider absence and all scoped counts/tombstones, always persist partial reports and close browser, and force nonzero exit if any proof or cleanup is missing/fails. Add local injected-failure tests for early failure after A creation, failed B delete, provider-delete failure, missing/failed D1 counts, and report/browser-finalization on exceptions.

### R4 — preflight and evidence privacy need the promised boundaries
Current harness only queries hardcoded D1 and labels it effective_db; verify actual immutable deployment's environment/commit/effective binding before writes. It reads all users rather than manifest-scoped IDs, stores manifest.json in tracked evidence without private mode, hardcodes test passwords, and logs raw balance-response bodies. Keep runtime credentials/manifest private+ignored, generate disposable passwords, log only sanitized assertions, and never emit response bodies that can include tokens/financial records. Scope cleanup/readback to exact created IDs, and keep historical-cleanup evidence separate from this run. Missing data must mean unknown/fail, never0/pass. Remove the tracked null runtime manifest after replacing its mechanism.

## Visual judgment

No changed product UI; no repeated visual score or zoom/reader checks. Their owner-authorized deferral remains. No WCAG-conformance claim.

## Closure actions

No task accepted;22/34 (64.7%, task count) unchanged. B06 returned to changes_requested with exact consolidated repair. C07 remains released for this bounded work; C08 remains locked. No main merge or production deployment.

Next: implementing agent repairs harness and proves fail-closed local tests BEFORE requesting/loading missing development CLERK_SECRET_KEY, then executes the single instrumented run specified in C07_FINISH_PROMPT.md. Existing authorization covers exact preview-only test scope; no repeated permission request. If secure owner dashboard access cannot supply the key, owner places it in ignored private local configuration; never in chat or VITE-prefixed variables. One diagnosed retry maximum; preserve runnable harness and consolidated blocker rather than launching another agent cycle. Primary reviews real results before C08 release.


## Repaired submission13ced36 / bounded primary correction — 2026-09-16

Decision: BLOCKED on remaining hosted execution, no B06 closure/C08 release. Submission correctly reports all three actual_status values null; no fresh users created in that attempt. Primary ran the11 supplied local tests successfully but found gaps they did not cover: setupClerkInterception rewrote provider captcha_bypass fields; cleanup success omitted application-delete flags and accepted nonnumeric counts; declared deployment preflight was absent. Some submitted negative tests compare local literals instead of the runtime evaluator, so their green count was not sufficient evidence.

Per repeated-repair ownership rule primary supplied a bounded patch: relay Clerk response unchanged while using supported testing-token transport; withhold provider deletion when application cleanup is unverified; require successful app cleanup, no recorded errors and integer zero counts; fail report-writing errors; verify immutable deployment success/environment/commit/DB before any writes; and reload the authenticated account page after import. Added real injected regressions for provider-response integrity, failed application cleanup, invalid counts and deployment metadata mismatch.15 harness tests pass (remaining/primary-repair-tests.log). Full ./scripts/test_all.sh passes (remaining/primary-full-suite.log). No hosted actions, secret reads or new deployment in this review. Product source remains unchanged.

Local repairs are not hosted proof. Import201, direct cross-tenant404 with unchanged A, authenticated late-write410 and current-run cleanup must still be observed. The caller must supply secure development testing credentials and working deployment/D1 read access; failed Wrangler authorization must be resolved via supported connected tooling rather than removing preflight. Preserve historical cleanup evidence; do not relabel it as a new run.

Next exact action: use owner-authorized Clerk development dashboard access to supply CLERK_SECRET_KEY securely to private ignored .env.preview.local (mode600), or a private process environment. Never paste it in chat, source or a shell command/history; do not use a VITE_ name. Run the revised C07_FINISH_PROMPT once; stop at a repeated blocker. Primary retains closure authority. Accepted total22/34 (64.7%) unchanged.
