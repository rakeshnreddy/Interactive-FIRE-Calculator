# B37 narrowed hygiene rework — Gemini worker

Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`. Preserve the existing uncommitted B37 legacy-retirement, finance-golden, evidence, and OA-2 index-only changes. Read `docs/execution/prompts/B37.md`, `docs/execution/reviews/B37-first-review-2026-09-26.md`, `GEMINI.md`, and the PA-10 amendment. Astra reviews, commits and measures post-commit clone sizes. Do not commit, push, deploy, use credentials, make hosted writes, or label any hosted step PASS.

**Scope is hygiene only.** Do not implement, run or simulate a hosted smoke. The existing untracked `scripts/hosted_smoke.mjs` and `scripts/hosted_smoke.test.mjs` belong to B42: either leave them untouched and explicitly exclude them from B37's staged candidate, or remove them from the B37 diff only if their contents are safely preserved for B42. Since `scripts/test_all.sh` currently invokes `hosted_smoke.test.mjs`, remove that invocation and its runner-test expectation from the B37 candidate; retain the Python-free suite and its failure-propagation tests. B37 acceptance must not depend on B42 files.

1. **Git-derived evidence provenance.** For each index-untracked evidence file, derive the commit that first added that path from Git history and verify `git show <commit>:<path>` returns bytes matching the manifest SHA-256. Do not assign a C09 preview URL to historical C01 screenshots. Record a source preview only when independently known; otherwise use `null`/`unknown` with a truthful note. Use argument-array Git subprocesses (`execFileSync`), never shell-interpolated paths. Test retrieval and hash checks against Git objects or a clean clone, not `existsSync` on this dirty worktree. For accepted review links, either keep the referenced file tracked or provide a resolvable manifest/artifact link and retrieval instructions that remain valid after current-index untracking. Preserve all historical review records and on-disk evidence; no history rewrite.
2. **Normative documentation.** Update only current contributor/runtime instructions that point to removed Flask/Firebase/Python paths. Dated historical reviews and past test counts stay historical. Check README, active scripts/workflows, `docs/PROJECT_MEMORY.md` current-state statements, and any active docs entry point. Record which remaining references are intentionally historical. Keep `src/lib/fire.ts` byte-identical.
3. **Clone measurements.** Remove the unverified “after” full/shallow clone sizes from `docs/execution/submissions/B37.md`. Record only measured baseline figures and exact commands for Astra to run after the B37 commit. Do not predict savings in a full clone: old blobs remain without a history rewrite. OA-2 authorizes index-only untracking of the reviewed bulk list and `.pyc` files; it does not authorize `git commit`, push, force-push, or deletion of accepted evidence.
4. **Python-free verification.** Run the ported finance goldens and `./scripts/test_all.sh` in a PATH without Python, plus `git diff --check`, `git diff --cached --check`, and `python3 docs/execution/validate_packet.py` using a separate local Python invocation where available. Preserve a durable log with exact exit codes. Demonstrate a deliberately perturbed golden fails and the restored case passes; verify `scripts/test_all.sh` propagates each stage failure. B37 submission must label only these locally observed checks PASS. CI will be verified by Astra after publication.

Update `docs/execution/submissions/B37.md` with the hygiene-only criterion matrix, exact file inventory, manifest verification commands, retained/untracked index list, measured baselines, rollback and remaining reviewer actions. Mark hosted smoke and the C09 revise residual **NOT IN B37 — moved to B42**. Do not mark B37 done or release B39. Stop with one consolidated handoff. No unrelated code or product UI change.
