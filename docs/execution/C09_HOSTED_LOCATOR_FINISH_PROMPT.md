# Paste into Antigravity/Gemini — C09 hosted locator repair

You are FinPath's implementation worker; Astra/Codex remains sole architect, Git publisher, hosted verifier and checkpoint reviewer. Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`, PR #140. Read `AGENTS.md`, `GEMINI.md`, `docs/execution/MASTER_REVIEW_PROMPT.md`, and `docs/execution/reviews/C09-hosted-attempt-7467116.md`. Preserve the clean candidate commit `74671167fa6f373ccb8f979efad7efd1b188f66e` and the separate failed-attempt evidence directory. Do not commit, push, reset, checkout, deploy, read secrets, write hosted D1, touch main/production, or mark B11/B28 done. This is for the owner's manual Antigravity workflow.

**Progress:** The exact-code CI run passed, the isolated preview [11955f4b](https://11955f4b.interactive-fire-calculator.pages.dev) is live, deployment/D1 isolation passed, the hosted runner authenticated both synthetic users and reached the review panel, and both users were independently verified fully cleaned up. The 55 collector tests and full suite passed locally. The only observed hosted stop is a strict locator in the evidence runner; product behavior beyond that step remains unverified. Avoid another broad planning pass or UI redesign.

Fix `docs/execution/evidence/C09/run_c09_proofs.mjs` around the B28 `measureClearance` function. `reviewPanelLocator.locator('.review-badge')` matches the header status badge **and** history decision badges (`KEEP`, `DEFER`) after two earlier reviews. `boundingBox()` therefore throws. Scope to the single status badge inside the panel's direct `.panel-heading` and assert the count is exactly 1 before measuring; keep the heading similarly scoped. Do not take `.first()` of all badges without proving identity. Inspect every later strict locator in this runner that operates after reviews or goals have multiple cards, particularly `boundingBox`, `innerText`, `screenshot`, `inputValue`, `isChecked` and `click`; make only the targeted disambiguations needed to select the intended user-visible element. Fail closed if the expected element is missing or duplicated.

Add a focused Playwright/DOM regression (in the existing collector tests or a small companion test) that renders a review panel with one header status badge plus at least `KEEP` and `DEFER` history badges. Demonstrate that the corrected locator identifies exactly the header badge and obtains its bounding box. Add a negative for duplicate header status badges. Do not claim this fixture proves hosted layout; it tests selector identity only.

Retain the 55 existing collector tests and the source-backed financial seed, three-review idempotency check, runtime deployment inputs, real theme/focus checks, and safe fixture server. Run the focused selector test, all collector tests, `./scripts/test_all.sh`, and `git diff --check` once on the final diff. Update B11/B28 submissions and validation matrix to say the `7467116` hosted attempt was **FAILED due to harness selector ambiguity**, Version 3 revise and B28 clearance remain **PENDING HOSTED PROOF**, and cleanup was independently verified. Keep task status blocked and C10 locked. Return changed files, exact command results, and remaining hosted proof. Astra will commit, push, await exact-code CI, deploy a fresh isolated preview, rerun the synthetic lifecycle, inspect desktop/mobile/light/dark evidence and cleanup, then decide closure.
