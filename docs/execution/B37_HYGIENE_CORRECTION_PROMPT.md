# B37 final hygiene correction — Gemini worker

Work on the existing uncommitted B37 candidate in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`, branch `codex/finpath-quality-execution`. Read `GEMINI.md`, `docs/execution/prompts/B37.md`, `docs/execution/B37_REWORK_PROMPT.md` and [Astra's narrowed review](reviews/B37-hygiene-review-2026-09-26.md). This is hygiene only. Preserve all existing worker and staged-index changes. Do not commit, push, deploy, run hosted checks, access credentials, modify financial formulas, or mark a hosted PASS. Leave `scripts/hosted_smoke.mjs` and `scripts/hosted_smoke.test.mjs` untouched and untracked for B42. Do not release B39.

Make **one bounded correction pass** with these acceptance checks:

1. Fix `scripts/generate_evidence_manifest.mjs` and `docs/execution/evidence_manifest.json` so every entry identifies the commit that **first added its path** (`git log --diff-filter=A`, not `git log -1`) and proves `git show <add_commit>:<path>` matches recorded addition hash/bytes. Seven B17 images were later modified; retain a separate latest-content commit/hash/bytes/retrieval command for those so both original provenance and the final screenshot remain recoverable. A schema with `added_commit`, `added_sha256`, `added_bytes`, `content_commit`, `sha256`, `bytes` is acceptable; for unchanged files both commits may match. Preserve an explicit retrieval command for the content used by accepted reviews. Verification must validate **both** objects and the first-add condition from Git history. Add local fail-closed tests with a temporary Git repo containing an added-then-modified binary and a deliberately altered manifest hash/commit. Run the verification after making the on-disk PNGs temporarily unavailable in a safe test fixture or clean checkout; do not delete or move real accepted evidence. Use argument-array subprocesses and bound object reads. Do not assign C09 preview provenance to C01 screenshots.
2. Fix the evidence-link audit and claim. Classify all twelve currently unresolved references, not just a total. The accepted `docs/execution/evidence/B09/validation-matrix.md` points to missing `B09/interactions.json`; compare tracked `C03/interactions.json` or `B19/interactions.json` and correct that reference to the exact real artifact if it carries the B09 interaction case. `docs/execution/contracts/B20-working.md` points to missing `B20/verification-report.json`; use the tracked `B20/verify_b20_results.json` only if it actually supports those rows, otherwise mark historical missing evidence explicitly. Directory/glob/line-number text is not a broken file link, but make the audit classify it separately rather than counting it as a resolved file. For every accepted review link that names a concrete file, require a tracked Git object or a manifest entry whose object and hash verify. Test the audit from Git objects or a clean clone, never local-disk existence. No fabricated files or PASS labels.
3. Reconcile current-state docs. Update `docs/PROJECT_MEMORY.md`'s current inventory line that says Flask paths remain. Ensure active contributor/runtime instructions do not reference removed paths. Keep dated history as history; where `docs/FINANCIAL_PLATFORM_HANDOFF.md` contains old instructions, label it historical if appropriate instead of rewriting its dated record. In `docs/execution/submissions/B37.md`, correct the HEAD/baseline/index path counts and explain the 1,552-path `cb2422d` baseline versus 1,555-path current HEAD before 55 index removals. Do not call untracked files staged additions. Remove the unsupported “byte-for-byte parity” wording unless proven. Do not invent post-commit full or shallow clone measurements; Astra does those after committing.
4. Audit every old `test_*` case in `git show HEAD:tests/test_finance_core.py` against TypeScript tests. Add a compact coverage matrix in the submission showing case → assertion file/line or documented semantic divergence. Port missing independent numerical goldens, at least start-of-year one-off income and the single-period portfolio/expense cases, unless an existing TypeScript test already asserts the same input/output. Cover varied-return/duration cases if otherwise absent. Use source literals from the old suite, not values computed by the implementation under test as the only expected result. Run a deliberate wrong-reference red test, then restore and run green. `src/lib/fire.ts` stays byte-identical.
5. Run `./scripts/test_all.sh` with Python absent from PATH; capture a durable raw log under `docs/execution/evidence/B37/` (keep it small and redact any environment output), including exact command and exit code. Run manifest unit tests, `--verify`, strict evidence-link audit, `git diff --check`, `git diff --cached --check`, and `python3 docs/execution/validate_packet.py` separately. Update `docs/execution/submissions/B37.md` with exact counts, observed local outcomes, and remaining Astra-only steps. No CI/preview/hosted claim from Gemini.

Stop after one consolidated handoff. Report changed files, exact local commands/results, the 12-link classification, first-add versus latest revisions for seven B17 screenshots, any intentionally unported Python case, and the files Astra must explicitly stage. B37 remains `changes_requested` pending Astra review. B39/B42 remain locked until B37 acceptance.
