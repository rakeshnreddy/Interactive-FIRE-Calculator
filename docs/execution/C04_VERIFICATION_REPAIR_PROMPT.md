# C04 final clarified verification repair

Work in /Users/Rakesh/Projects/Interactive-FIRE-Calculator on codex/finpath-quality-execution, PR140. Read reviews/C04.md, C04_START_PROMPT.md and IMPLEMENTATION_AND_VALIDATION_PROTOCOL.md. Preserve repaired B08/B20/B21 product code. This is the second clarified C04 rework; no redesign, shared formula rewrite, dependencies, production, main merge or C05 work.

## Contract: evidence must be observed, complete and reject failure

Primary file: evidence/C04/verify_hosted_c04.cjs and its test_evaluator_c04.cjs. Update only necessary evidence/submissions, plus the one rounding wording correction below. Authoritative requirements remain unchanged. Reuse working C03 collection techniques; do not build a large replacement framework.

1. **Evaluator schema first.** Define mandatory fields per required case. For required flags accept only boolean true; undefined/null/string do not count. Reject missing/wrong-type telemetry arrays. Require finite numeric measurements and case-specific ranges; NaN/Infinity cannot pass. Require actual theme fields/colors for every themed case and match requested theme to case ID. Reject missing/duplicate/unexpected IDs. A status is not evidence by itself.
2. **Write regression tests before fixing.** Start with submitted hosted-browser.json, then separately remove default CAGR headlinePercent/baseRowText/baseTableText; delete consoleErrors/pageExceptions; assign contrast minRatio=NaN and Infinity in direct evaluator tests, null in JSON CLI tests; remove requestedMode/observedMode; replace media row with ID only. All must fail with the specific reason, persisted verdict and nonzero real CLI status. Keep existing negative tests and a valid complete baseline. Also simulate a zoom collector error and require blocked/fail, never pass.
3. **Native zoom.** Delete the exception-to-success branch. CDP setPageScaleFactor is supplemental page scaling, not native zoom. Use CUA Chrome application controls: reset native zoom, apply native zoom to200%, read actual Chrome `Zoom: 200%` control, capture affected representative surfaces/themes and restore state. The previous primary C03 evidence describes a working method. If unavailable, persist BLOCKED with attempted method; do not fabricate output. Complete all other work first.
4. **Actual keyboard.** Use the changed metric help button (`About <metric>`), focus it via Playwright, press Enter or Space through browser keyboard, assert aria-expanded=true and associated help text visible, press again and verify closure/focus. Do not set/remove DOM attributes or call handlers to manufacture keyboard behavior. Include at least one representative calculator per affected shared layout family. Schedule details can be an additional check, not a substitute.
5. **Media behavior.** Activate reduced motion and reduced transparency independently using supported CDP features. Record query matches, actual theme, affected surface selectors and computed opacity/backdrop/animation/transition properties in both modes. Assert required behavior. No hardcoded pass:true; no claiming both media checks after only activating one. Restore media settings per case. Unknown support must be blocked, not pass.
6. **Actual contrast.** Remove missing-element foreground fallbacks. Measure changed metric/help/control/chart text against the rendered background underneath it, in both themes. Reuse C03's text-run background capture/composition code where useful; account for descendant glyphs and alpha, sample gradient positions, record source screenshot/coordinates, unrounded ratio and required threshold. A single heading against app canvas is insufficient for other panels/buttons. Do not invent observations or mark a computed unknown as pass.
7. **Complete original gates honestly.** Carry forward proven unaffected results with source revision links. Retest affected current product states. Record required checks not performed as blocked, including reader/print requirements where applicable under the original contract; do not silently extend the C03-only deferral. Do not ask the owner for assistance until independent work is finished. A blocked packet is an acceptable truthful submission; it is not checkpoint acceptance.
8. **Small wording fix.** In mortgage-payoff-rounding.md, correct “<=0.005 rounds to0 under half-up”: exactly0.005 rounds to0.01. Explain the inclusive final-payment settlement rule as a separate policy. Do not change tolerance to fit incorrect prose.

## Efficient execution and handoff

Run targeted verifier tests during repair. Because executable tooling changes, run ./scripts/test_all.sh once before combined push; later evidence-only changes need tree relation, not another full suite. The current website preview https://21a762ad.interactive-fire-calculator.pages.dev can be reused if website/functions/config assets are identical; do not redeploy unchanged product solely for tooling repair. Record website SHA, final tooling SHA, evidence SHA and their exact relationship. The previous post-candidate negative-row collector edit was executable, so do not call the entire later diff documentation-only.

Run actual collector on the identified preview and inspect persisted JSON and exit code yourself. Update each R4 item with fixed/failed/blocked and direct proof; stale positive claims must be superseded. Run packet validation and diff check, push the existing branch. Do not set tasks done or release C05. Use ready_for_review only if every mandatory requirement is evidenced; otherwise blocked with exact reason. Return a compact evidence index and exact remaining condition, not a pasted log dump.

Primary will reproduce the supplied omission/error cases and audit the live observations. If this second clarified pass still fails the same bounded repair, primary takes ownership or supplies a tested patch; do not initiate a third equivalent prompt cycle.
