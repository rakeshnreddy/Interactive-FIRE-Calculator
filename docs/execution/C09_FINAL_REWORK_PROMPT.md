# Antigravity/Gemini handoff — finish C09, then stop for Astra review

Paste this prompt into the existing Antigravity/Gemini coding session. Gemini is the implementer and evidence collector; the primary Codex/Astra session is the only task-closure reviewer. Do not invoke Gemini through a CLI from the Astra session. Work in the stable checkout `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`, on `codex/finpath-quality-execution` (PR [#140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140)). Inspect actual HEAD and working tree first; preserve all current uncommitted C09 evidence and reviewer changes. Never reset, discard, or silently overwrite them. Coordinate with the primary reviewer before changing their review record.

## Significant progress since the last handoff

B35 (FIRE assumptions and calculator audit) was independently accepted at product code `5dda3d2be24246e3470a65e7653a0b6e425cbece`. Its advanced controls now precede Calculate, optional cash-flow assumptions default to zero where appropriate, and saved snapshots remain compatible. Exact-code CI passed, and the product suite and 84 public calculator routes were verified. The current immutable C09 preview is `https://51acbf88.interactive-fire-calculator.pages.dev`, deployment `51acbf88-db0a-47d1-b399-814dad835a9b`, using only the isolated `finpath-preview` D1 database `0dbad68e-7493-452f-8504-98d4c61ee5da`. The stable beta alias is `https://codex-finpath-quality-execut.interactive-fire-calculator.pages.dev`.

Cloudflare Wrangler OAuth was refreshed without using the expired callback that appeared in chat. A fresh synthetic hosted C09 run on 5dda succeeded for the current evaluator. The collector now has 26 passing negative/behavioral tests, including actual keyboard activation and visible mobile-menu selection. The reviewer independently confirmed both disposable Clerk users were deleted, their 15 scoped D1 tables each contain zero rows, and their deletion tombstones remain. The reviewer accepted B10 (exact saved-decision restoration). **B11 and B28 remain open, and C10 is locked** because two acceptance claims still fail independent review. Read `docs/execution/reviews/C09-final-candidate-2026-09-25.md` and `docs/execution/MASTER_REVIEW_PROMPT.md` before changing anything. The current matrix says B11-11 PASS, but that row is explicitly rejected by the primary reviewer; correct it in the new packet.

## Task 1 — complete and prove the revise decision (B11)

The current `docs/execution/evidence/C09/run_c09_proofs.mjs` lines around 1317–1333 posts a `revise` API request and sets `review_revise_choice_triggers_revision` from HTTP status plus returned decision. That proves a review row, **not** the claimed user journey or Version 3. Inspect `src/PlanningWorkspace.tsx` review-choice UI, save/version flow, `src/lib/planReviews.ts`, and B11 contract. Write a failing behavioral regression first. Then make the smallest product change needed so a user with an eligible review can select **Revise assumptions**, submit that decision, receive an explicit edit/save next step, deliberately change at least one assumption, save a new plan version, reload it, and see the new version. Do not automatically mutate the plan when a review is recorded. Preserve immutable Version 1 and Version 2 snapshots/results and truthful historical labeling. Account for the seven-day eligibility rule with only manifest-scoped synthetic setup; state exactly what date was adjusted for the fixture, never suggest real retention elapsed.

Repair the hosted proof to execute the actual UI radio/button and version save, then verify D1/API state: `decision=revise` linked to the reviewed version, one new Version 3 with the edited assumption, older versions byte-identical, exact Version 3 reload, and no duplicate review on retry. A direct `fetch` to POST revise cannot set the `review_revise_choice_triggers_revision` pass flag. Split that flag into honest observed steps if useful. Add collector negative tests: review POST alone must fail the full journey, a Version 3 that did not persist must fail, and mutation of old versions must fail. Keep the existing keep/defer/idempotency/date/tenant/export checks. Do not weaken the evaluator or turn absent observations into true.

## Task 2 — remove the sticky-header overlap (B28)

`docs/execution/evidence/C09/screenshots/04_b11_review_completed_panel_focused.png` visibly shows the sticky top navigation covering the “Monthly plan review” heading and badge after the panel is brought into view. Reproduce in the real app with an authenticated synthetic/fixture route and record bounding boxes for the nav, heading, badge, and panel before the change. Fix the real scroll/focus layout (likely a suitable scroll offset/margin or equivalent) so heading and badge are visible when reached by deep link, keyboard/focus navigation, or `scrollIntoViewIfNeeded`. Do not merely alter screenshot cropping, hide the top bar, or add arbitrary blank space that harms mobile hierarchy. Capture the same focused viewport after the fix at desktop and 320px mobile, in light and dark themes. Assert no rectangles overlap; inspect the actual screenshots for readability, focus, no horizontal overflow, and contrast. Preserve reduced-motion/transparency behavior. Native browser 200% zoom and actual screen-reader smoke remain owner-deferred to B31/C11; do not spend this checkpoint repeating them or claim full WCAG conformance.

## Verification and handoff

1. Read repo `AGENTS.md` and `docs/execution/README.md`, `CHECKPOINTS.md`, `TASK_STATUS.json`, `C09_REWORK_PROMPT.md`, the B11/B28 task prompts, relevant submissions, and the primary review. Confirm branch/HEAD/diff and identify every existing uncommitted evidence file before edits. One writer at a time. Keep `src/lib/fire.ts` untouched.
2. Tests first for the B11 behavior. Run focused tests after changes. Run the 26 existing evaluator tests plus new negative tests. Run `./scripts/test_all.sh` once before push, record exit/counts; `git diff --check`. No dependency change is expected; use `npm audit` only if dependencies change.
3. Commit the small product fix separately from evidence/docs. Push only the `codex/finpath-quality-execution` branch; never force-push. Wait for exact-code CI green. Deploy a new immutable **preview branch** with `npx wrangler pages deploy dist --project-name interactive-fire-calculator --branch codex/finpath-quality-execution` after tests. Read-only verify the deployed commit SHA, preview environment, and exact isolated D1 UUID before any synthetic write. No main merge, production deployment, production D1, paid service, DNS, or new migration without separately scoped owner authorization. Do not print keys or test passwords.
4. Run all public calculator routes with `npm run smoke:calculators -- <new-immutable-preview-url>`. Perform one combined hosted B10/B11/B28 proof on that exact candidate, including the revised UI→Version 3 journey and the overlap/focus screenshots. Avoid repeated full synthetic runs to chase screenshot selectors: first locally test collector selectors/negative cases, then run once. If a run fails, preserve report, perform full cleanup, diagnose the exact cause, and repair before one focused retry.
5. Fail closed on cleanup: app delete for each exact synthetic user; 15 scoped D1 table counts zero; non-null tombstone; only then delete Clerk identity and verify 404. Preserve a private recovery manifest if cleanup is incomplete. Never touch real users.
6. Update `docs/execution/evidence/C09/validation-matrix.md` and B11/B28 submissions so B11-11 describes what was actually observed, with links to sanitized report/screenshots/logs. State product code SHA, evidence SHA, CI URL, immutable preview/deployment ID/DB UUID and cleanup proof. Leave B11/B28 at `ready_for_review` only if every nondeferred criterion passes. Do not mark tasks `done`, check backlog boxes, release C10, merge main, or deploy production. Stop at the C09 primary-review boundary and return a compact handoff with exact remaining blockers, if any.

Do not redo accepted B10/B35 work except the combined final-candidate regression. Do not spend time on unrelated calculator features or historical review rehash. The primary reviewer will inspect the final product diff and raw evidence, close passing tasks, and release C10 only if both open tasks pass.
