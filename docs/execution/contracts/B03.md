# B03 implementation contract

Checkpoint / task / latest reviewer record: C02 / B03 (prevent mixed-currency account totals) / docs/execution/submissions/B02.md
Baseline (from git) / branch / writer: df14ff034f198ccfd62a54acb8bf3fb0c50b270d / codex/finpath-quality-execution / FinPath implementation agent
User-visible or verification outcome: Group summary totals by currency and require explicit matching currency for plan imports. Display a clear unavailable combined total for mixed sets (USD 100 + INR 100 never displays as USD 200); liabilities reconcile per currency; single-currency behavior unchanged; plan import rejects mismatched units with typed `CURRENCY_MISMATCH` error enum.
Non-goals / preserved files and successful behavior: No foreign exchange (FX) feeds, rates, or conversion; no silent currency relabeling; no database record or schema rewrite. Preserve: `src/lib/fire.ts`, public calculation engines, isolated preview configuration.
Applicable authorization and free-tier limits: Free services only; local Vitest/Python/Node tests; zero billing or paid subscriptions; no production writes or main merge.

## Implementation requirements

| ID | Original criterion | Required behavior / invariant | Files and functions | Valid example | Missing/malformed case | Wrong/failed case |
|---|---|---|---|---|---|---|
| R01 | Group summary totals by currency | `summarizeAccounts` computes totals per currency in `byCurrency` map | `functions/_lib/accounts.ts`, `src/App.tsx` | USD and INR accounts produce `byCurrency.USD` and `byCurrency.INR` | Empty accounts produce empty `byCurrency: {}` | Accounts in different currencies lumped into single sum => failure |
| R02 | USD 100 + INR 100 never displays USD 200 | For mixed-currency account sets, `hasMixedCurrencies: true`, combined totals `assetsCents`, `liabilitiesCents`, `netWorthCents` are `null` (unavailable), and UI displays `Unavailable` with per-currency breakdown | `functions/_lib/accounts.ts`, `src/App.tsx` | USD 10,000 cents + INR 10,000 cents => `hasMixedCurrencies: true`, `assetsCents: null`, UI shows `Unavailable` | Missing currency defaults safely to `'USD'` | Combined assets shown as $200.00 => failure |
| R03 | Single-currency behavior unchanged | When all active accounts share the same currency, `hasMixedCurrencies: false`, `primaryCurrency: currency`, and combined totals are exact integer cents | `functions/_lib/accounts.ts`, `src/App.tsx` | 2 USD accounts totaling $150 => `assetsCents: 15000`, `primaryCurrency: 'USD'` | N/A | Single currency returns null or broken totals => failure |
| R04 | Liabilities reconcile per currency | Each currency tracked in `byCurrency` records its own `assetsCents`, `liabilitiesCents`, `liabilityAccountCount`, and `netWorthCents = assets - liabilities` | `functions/_lib/accounts.ts`, `src/App.tsx` | USD asset $100 + USD liability $30 => USD net worth $70 | 0 liabilities => `liabilitiesCents: 0` | Liabilities from INR subtracted from USD assets => failure |
| R05 | Archived and inactive accounts excluded | Accounts with `isActive: false` or `archivedAt: string` are excluded from summary calculations | `functions/_lib/accounts.ts`, `src/App.tsx` | Active $100 + Archived $500 => summary reflects only $100 | N/A | Archived account included in balance totals => failure |
| R06 | Negative balances preserved per currency | Overdrawn accounts or negative liabilities correctly add into that currency's totals | `functions/_lib/accounts.ts`, `src/App.tsx` | Account with -$20 balance updates USD assets by -$20 | N/A | Negative values clamped to 0 or dropped => failure |
| R07 | Plan import rejects mismatched units | `previewPlanSeed` checks selected account currencies against profile currency, rejecting mismatches with `reason: 'CURRENCY_MISMATCH'` | `src/lib/planWorkspace.ts` | Profile USD + account USD => plan seeded | Account EUR + profile USD => rejected with `CURRENCY_MISMATCH` | Mismatched currency imported into plan => failure |
| R08 | Dashboard and Account Workspace UI rendering | Dashboard and accounts strip display "Unavailable" for mixed totals and show per-currency chips/breakdown | `src/App.tsx` | Mixed account set renders `Unavailable` for net worth, with `INR: ₹100 · USD: $100` | N/A | UI displays false combined sum => failure |

## Validation requirements

| ID | Expected result for each case | Independent assertion / command | Test layer | Evidence destination | Actual result |
|---|---|---|---|---|---|
| V01 | `summarizeAccounts` with USD 100 + INR 100 sets `hasMixedCurrencies: true`, `assetsCents: null`, and separate `byCurrency` entries | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V02 | Single-currency account summary preserves exact totals and sets `hasMixedCurrencies: false` | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V03 | Liabilities reconcile independently per currency without cross-currency contamination | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V04 | Archived and inactive accounts are excluded from summary totals | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V05 | Negative balances in accounts are computed correctly per currency | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V06 | Empty account list returns 0 totals and `hasMixedCurrencies: false` | Unit test in `src/accounts.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V07 | `previewPlanSeed` rejects mismatched account currencies with `reason: 'CURRENCY_MISMATCH'` | Unit test in `src/lib/planWorkspace.test.ts` | Unit (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V08 | API endpoints (`/api/accounts`, `/api/dashboard`) return per-currency summaries | Endpoint tests in `src/accounts.test.ts` | Integration / Mock DB (Vitest) | `docs/execution/evidence/B03/validation-matrix.md` | Pending |
| V09 | Full verification suite passes | `./scripts/test_all.sh` | End-to-end full suite | `docs/execution/evidence/B03/full-suite.log` | Pending |

## Work order

1. Write failing tests for USD 100 + INR 100, liabilities reconciliation per currency, archived accounts, negative balances, empty accounts, and plan import currency mismatch.
2. Implement per-currency grouping and `hasMixedCurrencies` in `functions/_lib/accounts.ts`, `src/lib/planWorkspace.ts`, and `src/App.tsx`.
3. Update UI rendering in `src/App.tsx` for dashboard and accounts strip to show `Unavailable` and per-currency breakdown on mixed sets.
4. Run unit and integration tests, verify green.
5. Run full test suite `./scripts/test_all.sh` and capture log.
6. Create validation matrix and task submission, update `TASK_STATUS.json`.
