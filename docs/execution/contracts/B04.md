# B04 implementation contract

Checkpoint / task / latest reviewer record: C02 / B04 (atomic, retry-safe calculator save) / docs/execution/submissions/B03.md
Baseline (from git) / branch / writer: d66e6939841a49da73b07676b343d9dc09175281 / codex/finpath-quality-execution / FinPath implementation agent
User-visible or verification outcome: Calculator result save and downstream entity creation execute as one atomic database transaction via D1 batch; retries with identical payload return original entity without duplication; retries with mismatched payload return HTTP 409 Conflict; tenant isolation ensures foreign user cannot access or collide on another user's key; injected failure leaves zero orphan records.
Non-goals / preserved files and successful behavior: No automatic linkage of old historical results; keep existing public calculations, share, and export intact; no paid cloud services; preserve `src/lib/fire.ts`.
Applicable authorization and free-tier limits: Free services only; Cloudflare Pages preview; local Vitest/Node tests; zero billing or paid subscriptions; no production writes or main merge.

## Implementation requirements

| ID | Original criterion | Required behavior / invariant | Files and functions | Valid example | Missing/malformed case | Wrong/failed case |
|---|---|---|---|---|---|---|
| R01 | User-scoped idempotency key and database uniqueness | `saved_calculator_results` adds `idempotency_key` and `payload_hash` columns with user-scoped unique index | `migrations/0005_saved_calculator_idempotency.sql`, `functions/_lib/calculatorResults.ts` | User saves with key `key_abc` => recorded in D1 with `payload_hash` | Empty or malformed key => rejected or handled safely | Global unique key without `user_id` scoping => failure |
| R02 | Canonical payload hashing | Compute deterministic SHA-256 hash of canonical validated payload attributes | `functions/_lib/calculatorResults.ts` (`hashCalculatorPayload`) | Identical inputs produce identical hash string | N/A | Non-deterministic ordering producing different hash for identical payload => failure |
| R03 | Single atomic transaction for destination + result | Destination draft insert (`goals`, `accounts`, `plans`) and `saved_calculator_results` insert are bundled in a single `database.batch(...)` transaction | `functions/_lib/calculatorResults.ts` (`createSavedCalculatorResult`) | Goal insert + result insert execute in single batch | Injected failure on result insert leaves 0 goals | Destination insert succeeds but result insert fails leaving orphan goal => failure |
| R04 | Identical retry returns original entity | Save request with existing `idempotencyKey` and matching `payload_hash` returns original entity and result with `saveStatus: 'retry'` | `functions/_lib/calculatorResults.ts`, `functions/api/calculator-results/index.ts` | Second call with identical payload and key returns HTTP 200 with original goal and saved result | N/A | Second call creates duplicate goal row in database => failure |
| R05 | Conflicting payload returns HTTP 409 Conflict | Save request with existing `idempotencyKey` but different `payload_hash` returns HTTP 409 with `code: 'IDEMPOTENCY_CONFLICT'` | `functions/_lib/calculatorResults.ts`, `functions/api/calculator-results/index.ts` | Key `key_123` with $10,000, then `key_123` with $20,000 => 409 Conflict | N/A | Overwriting previous result or creating duplicate => failure |
| R06 | Tenant isolation on idempotency key | Idempotency uniqueness is scoped strictly to `user_id`; User B using same key as User A creates own record | `functions/_lib/calculatorResults.ts` | User A saves with `key_x`, User B saves with `key_x` => both succeed independently | Foreign user key access | User B receiving User A's goal or collision error => failure |
| R07 | Transaction destination creates no fake ledger entry | `conversionRoute: '/transactions'` creates `saved_calculator_results` with `created_entity_type: null` and zero transaction rows | `functions/_lib/calculatorResults.ts` | Transaction calculator save creates result only | N/A | Dummy entry inserted into `transactions` table => failure |
| R08 | Additive migration | `migrations/0005_saved_calculator_idempotency.sql` safely adds columns and partial unique index | `migrations/0005_saved_calculator_idempotency.sql` | Migration runs cleanly on existing schema | N/A | Dropping tables or destroying existing records => failure |
| R09 | Client save integration | Client generates and passes `idempotencyKey` for calculator save requests | `src/App.tsx` (`createCalculatorResultRecord`) | Client passes unique idempotency key on save | N/A | Client omitting idempotency key on retryable operations |

## Validation requirements

| ID | Expected result for each case | Independent assertion / command | Test layer | Evidence destination | Actual result |
|---|---|---|---|---|---|
| V01 | Injected failure in D1 batch rolls back completely leaving zero orphan goals/accounts | Real SQLite D1 transaction test in `src/calculatorResults.test.ts` | Integration / Real D1 (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V02 | Simultaneous identical save requests create exactly one entity and one saved result | Concurrency test in `src/calculatorResults.test.ts` | Integration / Real D1 (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V03 | Identical retry with same idempotency key returns original entity and result with `saveStatus: 'retry'` | Unit and API test in `src/calculatorResults.test.ts` | Integration / API (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V04 | Conflicting payload with same idempotency key returns HTTP 409 Conflict | Unit and API test in `src/calculatorResults.test.ts` | Integration / API (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V05 | Tenant isolation: same idempotency key across different users creates distinct records without interference | Multi-tenant test in `src/calculatorResults.test.ts` | Integration / Real D1 (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V06 | Transaction destination creates no fake ledger entries in `transactions` table | Destination test in `src/calculatorResults.test.ts` | Integration / Real D1 (Vitest) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V07 | Additive migration `0005` applies cleanly to existing schema | Migration runner check | Integration (Vitest / Node) | `docs/execution/evidence/B04/validation-matrix.md` | Pending |
| V08 | Full verification suite passes | `./scripts/test_all.sh` | End-to-end full suite | `docs/execution/evidence/B04/full-suite.log` | Pending |

## Work order

1. Plan migration `migrations/0005_saved_calculator_idempotency.sql`.
2. Write red tests in `src/calculatorResults.test.ts` using a real SQLite D1 harness covering:
   - Injected failure atomicity (zero orphans).
   - Identical retry returns original entity (`saveStatus: 'retry'`).
   - Conflicting payload with same key returns 409 (`IDEMPOTENCY_CONFLICT`).
   - Multi-tenant isolation for identical keys.
   - Transaction destination zero ledger entries.
3. Implement `hashCalculatorPayload`, atomic batch generation, and idempotency lookup in `functions/_lib/calculatorResults.ts` and `functions/api/calculator-results/index.ts`.
4. Update `createCalculatorResultRecord` in `src/App.tsx` to provide `idempotencyKey`.
5. Verify all tests pass, run `./scripts/test_all.sh`.
6. Document validation matrix and submission, update `TASK_STATUS.json`.
