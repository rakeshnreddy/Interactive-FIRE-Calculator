# B02 implementation contract

Checkpoint / task / latest reviewer record: C02 / B02 (reject incompatible currency conversion into goals) / docs/execution/reviews/C01I.md
Baseline (from git) / branch / writer: 7700a972716c401c718627314762bdf2abd230ec / codex/finpath-quality-execution / FinPath implementation agent
User-visible or verification outcome: Server rejects unsupported goal conversion before any database write with a user-safe message and typed 400 error (`INCOMPATIBLE_GOAL_CURRENCY`). Database write spy proves zero inserts into `goals` or `saved_calculator_results` on rejected conversions. USD `/goals` save retains exact behavior. Supported routes (`/accounts`, `/plans`) retain arbitrary currency (e.g. EUR, INR). Public calculation, share, and export continue in original currency without switching.
Non-goals / preserved files and successful behavior: No FX rate conversion; no silent currency relabeling; no database record rewrite; no schema migration (keep current USD contract until later task). Preserved: `src/lib/fire.ts`, public calculation engines, isolated preview configuration.
Applicable authorization and free-tier limits: Free services only; Cloudflare Pages preview; local Vitest/Python/Node tests; zero billing or paid subscriptions; no production writes or main merge.

## Implementation requirements

| ID | Original criterion | Required behavior / invariant | Files and functions | Valid example | Missing/malformed case | Wrong/failed case |
|---|---|---|---|---|---|---|
| R01 | Server rejects unsupported goal conversion before any write | When `conversionRoute === '/goals'` and `currency !== 'USD'`, reject with typed 400 `code: 'INCOMPATIBLE_GOAL_CURRENCY'` and message without amounts | `functions/_lib/calculatorResults.ts` (`parseCalculatorSavePayload`, `createSavedCalculatorResult`), `functions/api/calculator-results/index.ts` | `conversionRoute: '/goals'`, `currency: 'USD'` => parsed ok | Missing currency => 400 `currency must be a three-letter code.` | `conversionRoute: '/goals'`, `currency: 'EUR'` => 400 `INCOMPATIBLE_GOAL_CURRENCY` |
| R02 | Zero inserts on rejected goal conversion | No calls to `database.prepare`, `batch`, `exec`, or `run` on database when goal conversion is rejected | `functions/_lib/calculatorResults.ts` (`createSavedCalculatorResult`), spy harness | USD goal save creates goal and saved result | Non-USD goal save throws `IncompatibleGoalCurrencyError` before `ensureUserProfile` | Any write to `goals` or `saved_calculator_results` on non-USD goal save is a failure |
| R03 | Safe fallback in `goalPayloadFromCalculator` | If `payload.currency !== 'USD'`, return `null` defensively | `functions/_lib/calculatorResults.ts` (`goalPayloadFromCalculator`) | USD payload returns `GoalCreatePayload` with target amount cents | Malformed payload returns `null` | Non-USD payload returns non-null goal payload => failure |
| R04 | Direct goal creation currency guard | If direct `POST /api/goals` or `PUT /api/goals/[id]` payload contains `currency` other than `'USD'`, reject with `INCOMPATIBLE_GOAL_CURRENCY` | `functions/_lib/goals.ts` (`parseGoalCreatePayload`, `parseGoalUpdatePayload`), `functions/api/goals/index.ts`, `functions/api/goals/[id].ts` | No currency or `currency: 'USD'` => valid | Malformed currency => 400 `INCOMPATIBLE_GOAL_CURRENCY` | `currency: 'EUR'` => 400 `INCOMPATIBLE_GOAL_CURRENCY` |
| R05 | Supported non-USD routes retain original currency | `conversionRoute: '/accounts'` or `'/plans'` with `currency: 'EUR'`, `'INR'`, etc. preserves original currency in saved result and destination account | `functions/_lib/calculatorResults.ts` | `conversionRoute: '/accounts'`, `currency: 'EUR'` => parsed ok, account created with EUR | Missing required field rejected | Currency coerced to USD on account route => failure |
| R06 | Public calculations, share, and export continue in original currency | Public calculation engines, share URLs, and CSV exports retain original currency without switching | `src/lib/calculatorEngagement.ts`, `src/lib/seoCalculators.ts`, public components | Export/share EUR compound interest retains EUR | N/A | Switching currency to USD on share/export => failure |

## Validation requirements

| ID | Expected result for each case | Independent assertion / command | Test layer | Evidence destination | Actual result |
|---|---|---|---|---|---|
| V01 | `parseCalculatorSavePayload` returns typed error `{ code: 'INCOMPATIBLE_GOAL_CURRENCY', error: ... }` for non-USD goals (`EUR`, `INR`, `GBP`) | Unit test in `src/calculatorResults.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V02 | `createSavedCalculatorResult` throws typed `IncompatibleGoalCurrencyError` and makes zero database calls on non-USD goal conversion | Unit test with spy database asserting 0 calls to `prepare`, `batch`, `exec` | Unit / Mock DB (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V03 | `goalPayloadFromCalculator` returns `null` for non-USD payloads | Unit test in `src/calculatorResults.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V04 | `parseGoalCreatePayload` and `parseGoalUpdatePayload` reject explicit non-USD currency with `INCOMPATIBLE_GOAL_CURRENCY` | Unit test in `src/goals.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V05 | Valid USD goal save parses and creates destination goal and saved result | Unit test in `src/calculatorResults.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V06 | Non-USD account save (`/accounts`) parses and preserves currency (`EUR`, `INR`) without coercion | Unit test in `src/calculatorResults.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V07 | Public calculator export and share preserve currency | Tests in `src/lib/calculatorEngagement.test.ts` | Unit (Vitest) | `docs/execution/evidence/B02/validation-matrix.md` | Pending |
| V08 | Full test suite passes | `PATH="/opt/homebrew/bin:$PATH" ./scripts/test_all.sh` | End-to-end full suite | `docs/execution/evidence/B02/full-suite.log` | Pending |

## Work order

1. Capture baseline/reproduction and write red tests for behavioral changes.
2. Implement smallest fix; preserve explicitly passing behavior.
3. Run contract cases, actual CLI/integration and required hosted checks.
4. Run full suite before push; bind evidence to final executable/config revision.
5. Self-review required-field omissions and wrong-resource cases; submit, do not close.

Unresolved decisions: None. Scope and contracts are strictly defined.
Rollback preserving data/safety: Revert B02 commit cleanly; no database schema or data rewrite is introduced.
