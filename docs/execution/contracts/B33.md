# B33 remaining audit repair — concrete contract

Authority: reviews/C01I.md and prompts/B33.md; this document specifies the remaining repair without adding a new product objective. Live b8f9d6ec isolation passes. Preserve bindings, deployed backend, formulas and UI. Free services only. No new owner decision is needed for local audit repair.

## Implementation boundaries

Modify evidence/B33/evaluator.cjs, verify_b33.cjs, test_evaluator.cjs and a small shared parser/adapter module if needed. Update evidence/submission after testing. Do not redo wrangler or remote setup without a newly demonstrated defect. Export pure validation and parsing; only the real CLI entrypoint invokes collectors. Use one canonical validation result for console, persisted JSON and exit status. Keep resource identity in collected evidence, never fill an observed identity with the expected constant.

All required checks passing => PASS/0. Observed contract violation => FAIL/1. Missing, malformed, unperformed or unsupported evidence => BLOCKED/2. FAIL takes precedence if other checks are blocked. A process exception remains nonzero with a bounded redacted diagnostic. Production preflight readiness is a diagnostic; valid0/6 with exit1 and valid6/6 with exit0 both establish that the diagnostic ran, not that production is ready in both cases.

| ID | Implementation requirement | Mandatory validation cases and outcomes |
|---|---|---|
| V01 | Require observed production and preview IDs; preview equals approved UUID and differs from production | Missing/null/empty production or preview ID BLOCKED; same IDs FAIL; wrong preview UUID FAIL; valid IDs PASS |
| V02 | Bind effective deployment evidence to preview environment, successful stage, Functions=true, exact ID and immutable URL | Missing fields BLOCKED; production environment, failed stage or observed Functions=false cannot PASS; DB mismatch FAIL; probe URL differs from deployment URL FAIL |
| V03 | Canonical private probe set: /api/me, /api/profile, /api/plans, /api/accounts. Validate each independently | Missing/null/empty set or each individually missing BLOCKED; any observed non401 FAIL; extras do not substitute for required keys; all four401 pass |
| V04 | Health status200, JSON content type and exact required values: ok=true, app=interactive-fire-calculator, runtime=cloudflare-pages | HTML200 FAIL; wrong runtime/app/ok FAIL; missing health evidence BLOCKED; valid body passes; do not log unrestricted response bodies |
| V05 | Read schema/migrations against approved UUID; validate successful collector response and identity before filename comparison | Wrong DB FAIL; missing ID or malformed query response BLOCKED; pending repository migration FAIL; valid known migration lists pass; empty list must not establish current nonempty repository schema; invalid/null rows BLOCKED |
| V06 | Preserve raw known source policy and flags; one policy evaluator drives logs/report | Missing policy/flags BLOCKED; supported none/all/custom handled consistently; codex/finpath-* must match or explicitly BLOCK as unsupported, never silently exclude; malformed pattern arrays BLOCKED; include/exclude examples and enabled/disabled states covered |
| V07 | Preflight parser recognizes exact named check set from current script, summary totals and consistent process result | SixFAIL+0/6+exit1 diagnostic recognized; sixPASS+6/6+exit0 recognized; emptyexit0, duplicate/partial names, CLERK-only error, crash, inconsistent summary/exit BLOCKED. No substring-based recognized flag |
| V08 | Actual collector/CLI outcome persistence uses same evaluator; no test-only replacement CLI | Fixture success persists PASS/0; observed bad response FAIL/1; unavailable evidence BLOCKED/2; fetch/subprocess failure never0; known FAIL retained alongside BLOCKED; import has no live calls |
| V09 | Attribution is observed rather than manually invented | Record git HEAD, CI actual head and provider trigger/dirty separately; CI success tied to candidate/tree equivalence. Existing CI34557424040 actual head cf350f9197b2b6b63aa01f525946c47287b71ccc |

## Validation work order

1. Reproduce primary-rereview-reproductions.json cases before edits; write red tests for V01–V08 gaps. Keep existing tests, correcting incomplete passing fixtures to satisfy the real contract.
2. Implement validators/parsers and wire the actual collector to them. Do not coerce unknown source policy to none, missing probe maps to success, or queried identity to approved identity.
3. Exercise the real CLI using injected in-memory/temporary fixtures, including error paths; no network/unit-test billing. Retain a live mode for exact deployment verification, preferably explicit deployment ID instead of first item in a ten-row list.
4. Fill evidence/B33/validation-matrix.md with one V01–V09 row each, test names, commands/exits and result paths. Include negative cases, not only counts.
5. Run read-only live audit on b8f9d6ec (or explicitly justified replacement), full suite, diff checks, push and exact CI. No unchanged-site redeploy is required for audit-only fixes.
6. Independently self-review the full contract. Submit once all rows pass; otherwise name the exact unresolved row and finish independent work. Stop for primary review; B33/C02 closure is not yours.

## Review checkpoint

The next primary review tests V01–V09 and original B33 criteria. Preserve passing infrastructure. If the same specified audit contract still fails after this clarified rework, primary reviewer owns the bounded repair rather than issuing another substantially identical prompt. No acceptance is automatic.
