# B09 implementation contract

Checkpoint / task / latest reviewer record: C03 / B09 (remove internal instructions from calculator copy) / docs/execution/reviews/C02.md
Baseline (from git) / branch / writer: 1e69101 / codex/finpath-quality-execution / FinPath implementation agent
User-visible or verification outcome: Internal backlog and roadmap terminology (e.g., "Phase 22", "Phase 25", "phases are implemented") are eliminated from calculator studio charts, summaries, and quality specifications. Internal developer directives (e.g., "Connect loan results to a liability account or payoff plan.") in calculator decision checks are replaced with concrete, user-facing financial evaluation guidance. The unconfigured authentication gate ceases exposing internal environment variable names (`VITE_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, etc.) and vendor setup instructions; it honestly communicates that account features are unavailable in this preview while providing an honest disabled account action and a working primary button to explore public calculators. Loading state copy is refined to neutral session checks without vendor plumbing details. Strict fail-closed auth guards and calculation formulas (`src/lib/fire.ts`) remain untouched.
Non-goals / preserved files and successful behavior: No new calculator formulas, ranking claims, or marketing copy. `src/lib/fire.ts` calculation math remains completely untouched. Preserved exact calculator routes (`/calculators/fire`, `/calculators/mortgage`, etc.), canonical theme tokens, and existing auth security boundaries. No synthetic bypass in production bundles.
Applicable authorization and free-tier limits: Free services only; local Vitest tests; Cloudflare Pages preview; zero production writes or main merge.

## Implementation requirements

| ID | Original criterion | Required behavior / invariant | Files and functions | Valid example | Missing/malformed case | Wrong/failed case |
|---|---|---|---|---|---|---|
| R01 | Eliminate Phase and roadmap references | Remove all internal phase references ("Phase 22", "Phase 25", "phases are implemented") from calculator studios and quality specs | `src/lib/calculatorStudios.ts`, `src/lib/calculatorQuality.ts` | Amortization chart summary explains balance decline and cumulative interest over time | Chart summary says "gives Phase 22 a schedule-ready primitive to expand" | Leaving phase numbers or roadmap milestones in user copy |
| R02 | User-facing decision checks | Replace internal directives in `interpretationChecks` and quality specs with user-facing financial context and evaluation guidance | `src/lib/calculatorQuality.ts` (`studioDefaults`, `formulaOverrides`) | "Compare loan payoff options against current monthly cash flow and repayment goals." | "Connect loan results to a liability account or payoff plan." | Directing user to non-existent features or developer tasks |
| R03 | Auth gate unconfigured honesty & privacy | When auth is not configured, explain that saved plans, accounts, and cross-device sync are unavailable; remove all environment variable names (`VITE_CLERK_*`, `CLERK_*`) from UI | `src/App.tsx` (`AuthGate`) | "Account features are currently unavailable. Saved plans, accounts, and cross-device sync require account services that are not active in this preview." | Displaying `VITE_CLERK_PUBLISHABLE_KEY` and setup instructions to end-users | Falsely claiming accounts work when unconfigured |
| R04 | Working public navigation on AuthGate | Unconfigured AuthGate provides an honest unavailable account action (disabled button) and a working primary action navigating to `/calculators` | `src/App.tsx` (`AuthGate`) | Primary "Explore public calculators" button that triggers `onNavigate('/calculators')` | Trapping users on a dead-end page with only disabled buttons | Removing navigation or breaking existing public routes |
| R05 | Clean loading & signed-out states | Refine loading copy to neutral session verification without vendor plumbing details; retain clear sign-in and registration actions for signed-out states | `src/App.tsx` (`AuthGate`) | "FinPath is confirming whether there is an active session for this browser." | "confirming whether there is an active Clerk session" | Removing session check or breaking sign-in flow |
| R06 | Auth security & math preservation | Retain fail-closed auth protection on all platform routes; keep `src/lib/fire.ts` formulas and calculation engines identical | `src/App.tsx`, `src/lib/fire.ts`, `src/auth.tsx` | Platform routes require valid authentication; FIRE math unchanged | Permitting unauthorized access to protected routes | Modifying `fire.ts` or changing calculator calculation results |

## Validation requirements

| ID | Expected result for each case | Independent assertion / command | Test layer | Evidence destination | Actual result |
|---|---|---|---|---|---|
| V01 | All 82 calculator quality specs and studio metadata contain zero phase, SEO, or internal directives | Vitest content regression test in `src/lib/calculatorQualityCopy.test.ts` | Unit / Content regression | `docs/execution/evidence/B09/validation-matrix.md` | Pending |
| V02 | Amortization and all studio chart summaries contain clean user-facing copy without "Phase" or "primitive" | Vitest chart summary regression test | Unit / Studio charts | `docs/execution/evidence/B09/validation-matrix.md` | Pending |
| V03 | Unconfigured AuthGate renders honest unavailability message, zero env vars, and working public route | Vitest component test in `src/authGateCopy.test.tsx` | Unit / Component | `docs/execution/evidence/B09/validation-matrix.md` | Pending |
| V04 | Loading and signed-out AuthGate states render appropriate neutral copy and actionable controls | Vitest component test in `src/authGateCopy.test.tsx` | Unit / Component | `docs/execution/evidence/B09/validation-matrix.md` | Pending |
| V05 | Fail-closed auth guards remain active across all platform routes without credentials | Vitest security / auth regression test | Unit / Security | `docs/execution/evidence/B09/validation-matrix.md` | Pending |
| V06 | Full repository test suite passes with zero regressions | `PATH="/opt/homebrew/bin:$PATH" ./scripts/test_all.sh` | End-to-end full suite | `docs/execution/evidence/B09/validation-matrix.md` | Pending |

## Work order

1. Write red regression tests in `src/authGateCopy.test.tsx` and `src/lib/calculatorQualityCopy.test.ts` covering R01–R05.
2. Update `src/lib/calculatorStudios.ts` to replace "Phase 22" summary and third-person phrasing with clean user-facing copy.
3. Update `src/lib/calculatorQuality.ts` to replace "Phase 25", "comprehensive tax phases", and internal developer directives with user-centric financial checks.
4. Update `AuthGate` in `src/App.tsx` to remove the environment variable list, provide honest account unavailability text, and ensure working public calculator navigation.
5. Verify all tests pass, run full `./scripts/test_all.sh`, and record evidence in `docs/execution/evidence/B09/validation-matrix.md` and `docs/execution/submissions/B09.md`.
