# Task B06 submission

Status: BLOCKED
Implementing session/owner: C07 implementation agent
Checkpoint: C07
Worktree absolute path: `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`
Branch and PR URL: `codex/finpath-quality-execution`; https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140
Baseline commit: `a99efdc23214476702b079ccb1eca2223a924145`
Submitted code commit (full SHA): `383790b35249ddf36086bcb1d2253de5a5a93b20`
Final checkpoint candidate SHA: blocked before hosted candidate
Immutable preview URL and verified deployed code SHA: none; unsafe deployment withheld
CI URL for submitted code SHA: pending push/CI

## Problem and change

Direct Pages uploads compile Vite locally, so Cloudflare runtime secrets cannot populate `import.meta.env.VITE_CLERK_PUBLISHABLE_KEY`. The existing preview therefore compiled the signed-out Clerk provider away. `scripts/build_preview_auth.mjs` now provides a repeatable preview build that accepts only a Clerk development publishable key, reads an ignored mode-600 `.env.preview.local` when needed, resolves Vite's effective production-mode environment, and rejects client-prefixed secret/private/JWT variables before building. The full suite runs its five regression tests.

Provider ownership is unresolved. The owner confirmed they never created a Clerk account, and the Clerk dashboard has no authenticated session. Existing Cloudflare secret names and a signed-out API 401 do not prove tenant ownership or credential compatibility. No deployment, provider write, identity creation, financial write, migration, DNS change, or production action was performed.

## Acceptance evidence

See `docs/execution/evidence/C07/validation-matrix.md`. Secure build and local regression rows pass. Hosted signup/session/lifecycle/isolation/deletion and browser quality rows are blocked on one owned free Clerk development tenant.

## Tests and review

- Focused regression tests: `node --test scripts/test_all.test.mjs scripts/build_preview_auth.test.mjs`; 19/19 passed in the captured run.
- `./scripts/test_all.sh`: exit 0 on 2026-09-15; Python 79 tests + 21 subtests, Vitest 47 files / 1608 tests, typecheck/build/isolation pass. Evidence: `docs/execution/evidence/C07/test-all.log`.
- Build path: synthetic development-format public key; build passed, 19 production assets inspected, 0 fixture leaks, 0 secret-shaped server values. Evidence: `preview-build.log`.
- `git diff --check`: pass.
- Hosted/browser/CI evidence: blocked or pending as stated above; no pass inferred from HTTP status alone.

## Security, privacy and data

Effective preview DB readback is `0dbad68e-7493-452f-8504-98d4c61ee5da`, distinct from recorded production DB `a5860350-0a50-4ebe-9f5f-1d9916a908e6`. No hosted write occurred. No real or synthetic identity, token, provider key, personal record, or financial record appears in evidence. Migration 0006 was not repeated.

## Rollback and remaining issue

Rollback commit `383790b35249ddf36086bcb1d2253de5a5a93b20`; existing fail-closed behavior remains intact. The smallest missing owner action is to create/sign into an owner-controlled free Clerk account and create/select a development instance for FinPath. Once available, securely connect the exact client and server credentials, allow only the exact preview origin(s), then resume the authorized two-user hosted lifecycle and cleanup. No secret should be sent in chat.

Implementation is submitted, not accepted. C08 remains locked.
