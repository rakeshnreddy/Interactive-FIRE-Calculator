# Task B03 submission

Status: READY_FOR_REVIEW
Implementing session/owner: FinPath implementation agent
Checkpoint: C02
Worktree absolute path: `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`
Branch and PR URL: `codex/finpath-quality-execution`; [PR #140](https://github.com/rakeshnreddy/Interactive-FIRE-Calculator/pull/140)
Baseline commit: `ecc4cfc4ba01e4beecf5d2b78fa068e1a89b3f36`
Submitted code commit (full SHA): `d66e6939841a49da73b07676b343d9dc09175281`
Final checkpoint candidate SHA: To be determined at C02 boundary (B02 → B03 → B04)
Earlier provisional tasks affected/retested: B02 unaffected; B33, B32, B15–B17, B34, B01 re-verified via `./scripts/test_all.sh`.
Immutable preview URL and verified deployed code SHA: Existing isolated preview deployment [b8f9d6ec](https://b8f9d6ec.interactive-fire-calculator.pages.dev) (isolated preview D1 binding `0dbad68e-7493-452f-8504-98d4c61ee5da`).
CI URL for submitted code SHA: Pending push

## Problem and change

Task B03 eliminates cross-currency contamination in financial accounts and plan seeding. Previously, account balances in disparate currencies (e.g. USD and INR) were naively summed into a single integer, causing USD 100 + INR 100 to incorrectly display as USD 200. Furthermore, plan import lacked typed validation against importing mismatched currency accounts into a plan with a different default currency.

Implemented changes:
1. **Isolated Per-Currency Summary Calculation (`summarizeAccounts` & `summarizeAccountList`)**:
   - In `functions/_lib/accounts.ts` and `src/App.tsx`, computes `accountCount`, `assetsCents`, `liabilityAccountCount`, `liabilitiesCents`, and `netWorthCents` per currency inside `byCurrency`.
   - When accounts have mixed currencies (`currencies.length > 1`), sets `hasMixedCurrencies: true`, `primaryCurrency: null`, and combined totals `assetsCents`, `liabilitiesCents`, `netWorthCents` to `null` (`Unavailable`).
   - When accounts share a single currency, sets `hasMixedCurrencies: false`, `primaryCurrency: currency`, and preserves exact integer totals with 100% backward compatibility.
   - For 0 active accounts, returns zero totals and `hasMixedCurrencies: false`.
   - Excludes inactive accounts (`isActive: false`) and archived accounts (`archivedAt`).
   - Accurately aggregates negative balances per currency without clamping or dropping.
2. **Plan Seeding Currency Matching (`src/lib/planWorkspace.ts`)**:
   - Exports `PLAN_SEED_ERROR_REASONS` enum including `CURRENCY_MISMATCH`.
   - `previewPlanSeed` checks selected account currencies against the target profile currency. If any mismatch occurs, import is blocked with typed `errorDetails` containing `reason: PLAN_SEED_ERROR_REASONS.CURRENCY_MISMATCH`, `accountName`, `currency`, and `expectedCurrency`.
3. **UI Rendering in Dashboard and Accounts Strip (`src/App.tsx`)**:
   - Added `formatAccountMetric` which displays `Unavailable` if `hasMixedCurrencies` is true or amount is null.
   - Added `formatCurrencyBreakdown` which renders an inline per-currency breakdown string (e.g. `INR: ₹100.00 · USD: $100.00`).
   - Updated `DashboardPanel` (net worth, assets, liabilities) and `AccountsPanel` overview strip to render `formatAccountMetric` with `formatCurrencyBreakdown`.
4. **API Integration & Validation**:
   - Handlers `/api/accounts` and `/api/dashboard` return the updated `AccountSummary` with `byCurrency` and currency isolation fields.
   - Added comprehensive tests in `src/accounts.test.ts` and `src/lib/planWorkspace.test.ts`.

## Acceptance evidence

| Criterion copied from task prompt | Pass / fail / blocked | Exact test, screenshot or reproduction evidence |
|---|---|---|
| group summary totals by currency | PASS | `src/accounts.test.ts`: test `prevents false combined totals for mixed currencies (USD 100 + INR 100 never displays USD 200)` verifies `byCurrency.USD` and `byCurrency.INR` group totals cleanly. |
| require explicit matching currency for plan imports | PASS | `src/lib/planWorkspace.test.ts`: test `rejects account sources with mismatched currency using CURRENCY_MISMATCH error enum` verifies rejection with typed `CURRENCY_MISMATCH` reason. |
| clear unavailable combined total on mixed sets | PASS | `src/accounts.test.ts` asserts `assetsCents`, `liabilitiesCents`, `netWorthCents` are `null`. `src/App.tsx` displays `Unavailable` via `formatAccountMetric`. |
| USD 100 + INR 100 never displays USD 200 | PASS | `src/accounts.test.ts`: USD 100 + INR 100 produces `hasMixedCurrencies: true`, combined totals `null`, separate USD $100 and INR ₹100 breakdown. |
| liabilities reconcile per currency | PASS | `src/accounts.test.ts`: test `reconciles liabilities per currency without cross-currency pollution` confirms USD debt ($200) only subtracts from USD assets ($500 -> $300), and INR debt (₹400) only subtracts from INR assets (₹1,000 -> ₹600). |
| single-currency behavior unchanged | PASS | `src/accounts.test.ts`: test `preserves exact numeric totals for single-currency accounts` asserts exact integer cents for assets, liabilities, and net worth with `hasMixedCurrencies: false`. |
| plan import rejects mismatched units with typed `CURRENCY_MISMATCH` | PASS | `src/lib/planWorkspace.test.ts` asserts `reason: PLAN_SEED_ERROR_REASONS.CURRENCY_MISMATCH`. |

## Tests and review

- Failing behavioral test before fix:
  - Red tests in `src/accounts.test.ts` (failed before implementation: `hasMixedCurrencies` undefined, `assetsCents` was lumped sum).
  - Red tests in `src/lib/planWorkspace.test.ts` (failed before implementation: `CURRENCY_MISMATCH` enum and `errorDetails` missing).
- Relevant green tests:
  - `npx vitest run src/accounts.test.ts`: 8/8 tests pass.
  - `npx vitest run src/lib/planWorkspace.test.ts`: 6/6 tests pass.
  - Vitest full suite: 28 test files, 1297 tests pass.
- `./scripts/test_all.sh` command/date/exit/counts and durable log:
  - Command: `PATH="/opt/homebrew/bin:$PATH" ./scripts/test_all.sh`
  - Date: 2026-09-11
  - Exit code: 0
  - Counts: 13 runner tests, 79 Python pytest tests + 21 subtests, `tsc --noEmit` clean, 28 Vitest test files / 1297 tests, `tsc -b && vite build` clean.
  - Durable log: [`docs/execution/evidence/B03/full-suite.log`](../evidence/B03/full-suite.log)
- Audit if dependencies changed: Dependencies unchanged.
- `git diff --check`: Exit 0 (zero whitespace/lint defects).

## UI evidence (N/A only with explanation for a non-UI task)

- Dashboard and accounts overview strip:
  - In single-currency mode: displays standard formatted currency amounts (e.g. `$400.00`).
  - In mixed-currency mode: displays `Unavailable` as the headline metric (preventing misleading sums) and renders an inline breakdown below (e.g. `INR: ₹100.00 · USD: $100.00`).

## Security, privacy and data

- Effective preview DB isolation proof before any write: Preview binding isolation verified in B33 (`0dbad68e-7493-452f-8504-98d4c61ee5da`). B03 performs zero remote writes; all tests run locally with mock and spy databases.
- Synthetic identity/data only: Synthetic user IDs (`user_123`) and simulated amounts used throughout.
- Schema migration and scoped authorization: No database schema migration; per-currency totals are calculated dynamically in memory.
- No secret or real-record exposure check: Confirmed zero secrets or real financial records accessed.

## Rollback and remaining issues

- Task-specific revert/disable plan: Revert git commit cleanly.
- Open failures/blockers: None.
- Next task within checkpoint: B04 (atomic, retry-safe calculator save) to complete Checkpoint C02.

## Contract coverage and independent self-review

- Contract path: [`docs/execution/contracts/B03.md`](../contracts/B03.md)
- Validation matrix path: [`docs/execution/evidence/B03/validation-matrix.md`](../evidence/B03/validation-matrix.md)
- Negative cases executed:
  1. Mixed currency accounts (USD 100 + INR 100) => `hasMixedCurrencies: true`, combined totals `null`.
  2. Mismatched currency accounts in plan import (EUR into USD plan) => rejected with typed `CURRENCY_MISMATCH` reason enum.
  3. Inactive and archived accounts => excluded from calculations.
  4. Empty account list => zero totals and `hasMixedCurrencies: false`.
  5. Liabilities across multiple currencies => reconciled strictly per currency without cross-contamination.
- Self-review: Checked edge cases (empty account list, inactive accounts, negative balances). All 9 validation requirements pass.
