# Paste into Antigravity/Gemini — C09 Stage 4 readiness repair

You are the FinPath implementation worker. Astra/Codex is the sole architect, Git publisher, hosted verifier and checkpoint closure reviewer. Use high reasoning effort. Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`, branch `codex/finpath-quality-execution`, PR #140. Read `AGENTS.md`, `GEMINI.md`, `docs/execution/MASTER_REVIEW_PROMPT.md`, `docs/execution/reviews/C09-hosted-attempt-f422cb3.md`, and the existing B11/B28 contracts and submissions. Start from the branch's current HEAD; the proof-runner candidate commit is `f422cb31f517d747247207996dcdb3007bc760a5`, followed by a reviewer-only evidence commit `cedf743`. Do not check out an older SHA. Keep existing untracked `docs/execution/intake/` untouched.

**Do not** commit, push, deploy, run remote hosted writes, read or print secrets, touch production or main, mark B11/B28 done, or release C10. Preserve the prior failed-attempt evidence under `docs/execution/evidence/C09/attempt-f422cb3/`. Do not modify financial formulas or unrelated product/UI code. Submit a clean, narrow working-tree diff for Astra to review and publish.

## Observed failure and bounded task

The prior selector repair passed 57 collector tests, the full repository suite, exact-code CI, isolated preview deployment preflight, all 84 public routes and the hosted B10/DEFER/KEEP path. The hosted run reached the B28 review panel in four viewport/theme combinations and measured no sticky-topbar overlap. It then stopped at `docs/execution/evidence/C09/run_c09_proofs.mjs` Stage 4 (around line 1951): after `pageA.goto('/plans?planId=...&version=2', {waitUntil:'domcontentloaded'})`, it awaited only `.planning-workspace`, then `assertTargetLocatorVisible(pageA.locator('.planning-review-panel'))` failed immediately. Stage 3 correctly awaited `.planning-review-panel` after a prior navigation. The runner may have raced authenticated plan hydration; a real Version 2 rendering defect remains possible. The two disposable users were independently verified deleted from Clerk and all 15 preview D1 tables; tombstones remain. Full evidence and log are in the review linked above.

1. Inspect the actual Stage 3 and Stage 4 navigation code, `assertTargetLocatorVisible`, `src/App.tsx` deep-link loading, and `src/PlanningWorkspace.tsx` rendering. Determine what readiness signal establishes that the requested plan and **exact Version 2** have loaded. Do not infer success from the workspace shell alone. Ensure the route still points to the expected plan/version and that the UI visibly identifies Version 2 before proceeding to the revise choice.
2. In Stage 4, wait for that exact ready state and `.planning-review-panel` to be visible with a bounded timeout. Reuse an existing helper if sound. Do not use an arbitrary fixed sleep or `.first()` that hides an ambiguous target. A missing, duplicated, wrong-version or controlled-error state must fail explicitly.
3. If the panel does not appear, record safe diagnostics *before throwing*: current pathname/query shape (omit or redact synthetic user identifiers), whether Clerk is loaded and signed in, counts/visibility for workspace/panel/loading/controlled-error/auth-gate nodes, and a sanitized visible error message. Include a failed-state screenshot only if it cannot expose credentials or real records. Never log cookies, tokens, keys, request authorization headers or full DOM. The failure must still reach the existing fail-closed two-user cleanup.
4. Add a real-Chrome regression in `run_c09_proofs.test.mjs` or a small companion test. Positive: render the workspace shell first, mount the correct Version 2 overview and panel asynchronously, and prove Stage 4 waits until the panel is visible and selects Version 2. Negative: shell appears but the panel never does, or a controlled error/wrong version appears; prove the collector fails with a clear diagnostic instead of silently advancing. Keep tests bounded so they do not add minutes to every suite run. If source inspection shows a genuine product defect rather than a runner race, document a minimal reproduction first and implement only the smallest B11/B28-scope product fix with behavioral regression tests.
5. Scan the remaining hosted stages for the same `goto` → workspace-shell → immediate element assertion pattern. Fix only demonstrated readiness races before another hosted run. Keep all existing collector negative checks, source-backed financial seed, exact deployment SHA/DB preflight, and fail-closed cleanup unchanged in behavior. Do not hard-code passing flags or weaken assertions.
6. Run focused new regressions, the entire collector test file, `./scripts/test_all.sh`, and `git diff --check` once on the final diff. Update `docs/execution/evidence/C09/validation-matrix.md` and B11/B28 submissions accurately: prior attempt `f422cb3` FAILED at Stage 4; topbar clearance 4/4 observed; Version 3 revise and downstream B28 gates remain pending hosted proof; independent cleanup passed. Leave `TASK_STATUS.json` blocked and C10 locked.

Return: exact changed files; root-cause assessment with source line references; test counts and commands; any unresolved uncertainty; and the single command Astra should run after committing/deploying. Stop for Astra review. Do not claim C09 acceptance from local tests.
