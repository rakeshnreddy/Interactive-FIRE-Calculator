# C05 verification repair — preserve product repairs

Work in /Users/Rakesh/Projects/Interactive-FIRE-Calculator on codex/finpath-quality-execution, PR140. Read reviews/C05.md and C05_REWORK_PROMPT.md. C05 remains released only for rework; C06 locked. Preserve product code78bc2ca and website candidate1c73bff unless a test proves a product regression. Actual-reader checks deferred to B31. Free previews only, no main/production/real financial writes.

1. Run evidence/C05-second-review/omission-reproductions.cjs. Both rows currently return PASS despite missing evidence. Turn these exact reproductions into failing regression tests before repairing the actual evaluator/CLI. For the contrast-only case supply a complete valid native fixture so the rejection must be caused by absent pairs, not an unrelated blocked zoom.
2. Define native observations explicitly: actual observed browser UI zoom200, route, theme, viewport, tested input/result/expanded states, observed clipping/overflow outcome and durable proof reference. Require full changed-route/theme coverage or an explicit agreed representative shared-component justification. A status string/pass boolean or image path alone is insufficient. CLI automated collection may honestly BLOCK and accept a separately supplied assisted record with provenance; do not fabricate observations or switch a blocked status to PASS by hand.
3. Contrast evaluator: require the expected target/theme set, nonempty typed pairs, finite actual ratios/thresholds, correct thresholds, valid source colors and per-pair pass; derive minima instead of trusting supplied summaries. Tests: missing pairs, missing target/theme, NaN/null, low-ratio pair plus high claimed minimum, wrong threshold, falsely labelled normal/large text. All must exit1 through the real CLI. Complete valid fixture exits0; mandatory BLOCKED exits2. Preserve errors in output.
4. Measure the actual changed .hero-result normal and stale state, .stale-result-badge, visible help and representative dedicated-result/warning labels in both themes. Include inherited opacity in composition or measure foreground rendering in a way that accounts for it. Reuse the real pixel method; no guessed colors/default pass. Save observed per-target evidence. Do not claim that .metric-accent elsewhere measures the hero.
5. Replace comparison card-count proof with numeric before/after assertions. Calculate a fixed plan; capture scenario values and projection rows/CSV; change base withdrawal/timing input without recalculating: values/metadata remain consistent with the previous calculation. Edit a scenario modifier: compare changes against the same saved base. Recalculate: verify new expected values/metadata. Cover both FIRE modes, draft restore and CSV agreement. Expected values come from existing engine fixtures, not a second call into the same UI rendering logic. Strengthen the R3 component test as well; do not change formulas.
6. Keep passing InfoTip Enter/Escape checks. Add real Tab/Shift+Tab journeys on compound-interest, savings-goal, budget, net-worth, emergency-fund and FIRE. Open relevant timing/schedule/FAQ/help via keys; observe expanded content, focused element and Calculate/result reachability. Measure sticky occlusion after each actual keyboard step. Programmatic focus enumeration is not the route's keyboard test.
7. Capability preflight: if native Chrome/CUA exists in your cheaper session, perform real UI200% and save the above observations. If absent, finish all other work and submit BLOCKED with that one capability gap; never report no blockers. Screen reader remains deferred and should not consume effort now.
8. Run targeted tests and evaluator negative fixtures after edits, then required full suite before pushing executable changes. Verify exact current CI; replace stale run34784371718 in all three submissions with the actual run and full SHA. Website source unchanged means existing clean preview46714a3f may be reused; prove product/config tree relation and separately identify tooling HEAD. If product changes, rebuild/deploy a clean final candidate, preserve isolated DB binding, rerun84-route smoke and affected journeys. Do not force an unnecessary deployment for docs-only changes.
9. Refresh one compact criterion-to-evidence matrix, R4a–d decisions and native status. Preserve previous passing media/theme/unit tests and historical evidence. ready_for_review only when every non-deferred mandatory criterion passes; otherwise blocked. Do not close tasks, edit primary reviews, release C06 or merge. Submit candidate/tooling SHAs, exact CI, immutable preview, logs, raw observations, and exact remaining limitation.

Primary will spot-check critical assertions and owns closure. This is the clarified verification retry; repeated defects will trigger primary ownership under protocol6, not another identical worker loop.
