# B33 final audit-contract rework

Work in /Users/Rakesh/Projects/Interactive-FIRE-Calculator on codex/finpath-quality-execution. Read MASTER_WORKER_PROMPT.md, FREE_TIER_EXECUTION.md, prompts/B33.md and reviews/C01I.md. Preserve all unrelated work. The live isolation setup passes; do not redo it. No paid service or new owner authorization is needed for the following local fixes.

1. Read primary-rereview-reproductions.json and reproduce all five false PASS cases from the submitted report. Write meaningful failing tests first. Fix the evaluator and collector contract together, not by modifying evidence to fit it.
2. Define one canonical required endpoint set (/api/me, /api/profile, /api/plans, /api/accounts). Require every entry, schema/type validation and complete health fields including runtime. Missing/partial probes BLOCK; observed bad responses FAIL. Add tests for zero, partial and malformed sets, wrong runtime and extra irrelevant probes.
3. Require actual production DB identity and approved preview identity; validate preview environment, successful stage, Functions presence and exact deployment ID/URL correspondence with probe evidence. Require migration collection from the same approved UUID. Use the UUID in the read-only command or obtain an authoritative name-to-ID mapping first. Test missing/wrong production identity, non-preview/static/failed deployment, mismatched probe URL, wrong migration DB and malformed query result. Do not query financial rows or production schema.
4. Preserve missing/unknown source policy as BLOCKED; do not default to none. Evaluate real enabled flags. Support Cloudflare's documented pattern semantics, or explicitly block patterns the implementation does not support. At minimum test codex/finpath-* against this branch: it must never silently mean excluded. Use one evaluator result for logs and persisted policy; remove the misleading exact-match calculation.
5. Extract a preflight-output parser with tests for complete0/6 exit1, complete6/6 exit0, emptyexit0, partial checks, duplicate checks, unrelated CLERK error, crashed process and inconsistent exit. Require recognized named check set and matching summary/count, not a substring. This remains a production diagnostic, not a demand for paid/production configuration.
6. Test persisted outcome and the real collector/CLI failure handling, not only a generated miniature CLI wrapper. Fixture injection or adapters may avoid network in tests. Network/import must not run during pure unit imports. Redact errors; do not print secret values or unrestricted response bodies.
7. Correct full CI SHA using gh output verbatim; actual previous run head cf350f9197b2b6b63aa01f525946c47287b71ccc. Record website tree, changed wrangler config/tooling revision and deployment trigger separately. Current b8f9d6ec trigger is 092a4a2cd0a59a889fe06a47bb4b2b1de1bec861 with dirty=true; document equivalence honestly. No identical-site redeploy required for tooling-only fixes.
8. Run all evaluator/collector tests, read-only live audit on the exact isolated deployment, ./scripts/test_all.sh, diff checks and CI. Capture final HEAD and logs, update submission/status ready_for_review only if all criteria pass. Stop at C01I; no task closure, C02 release, main merge or production deploy. Report remaining genuine blockers rather than inventing another permission requirement.

Success: supplied valid live evidence passes; every omitted/mismatched requirement fails or blocks without a false PASS; current verified free preview isolation is preserved. Primary reviewer alone accepts B33.
