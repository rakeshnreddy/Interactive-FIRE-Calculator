# C09 consolidated rework: correct product behavior and truthful proof

Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator`, branch codex/finpath-quality-execution, PR140. Read reviews/C09.md and C09_START_PROMPT.md. B10/B11/B28 remain open, C10 locked. Preserve passing work. No main merge, production, paid services, DNS, financial formula changes or unapproved migrations.

## Execute in this order

1. **Regression tests first (R2–R6).** Demonstrate concurrent duplicate review creation, conflicting same-key choice, invalid/future dates, scheduling from old evidence, dashboard stale after completed/deferred review, navigation losing edits, malformed explicit version loading latest, and metadata changes falsely refreshing evidence. Use real local D1 harness/API tests and client interaction tests; SSR string presence alone does not establish behavior. Preserve src/lib/fire.ts.
2. **R2 atomic review contract.** Separate exact replay from a different intended decision. Choose a documented operation/cycle identity compatible with deliberate deferral/revision and future reviews. Enforce it in SQLite/D1, not SELECT-then-INSERT alone. A conflict must return a structured truthful response; UI must not announce the newly requested decision when the server returned a prior different one. Keep immutable historical reviews. Add an additive corrective migration since0007 is already applied, with deterministic duplicate handling that preserves/audits history rather than silently dropping rows. Prepare SQL, migration/recovery note and local tests before requesting exact isolated-target authorization. Do not execute remote corrective SQL without that authorization; finish independent code/tests first.
3. **R3 dates.** Validate real YYYY-MM-DD dates by UTC round-trip; reject future evidence. Evidence date describes financial source data. Completion/defer action time is trusted server time; derive next due from that time. Keep >=7-day eligibility based on real baseline age. Specify30-day versus calendar-month wording consistently. Test midnight/timezone/leap dates, old evidence, future tampering and repeated requests.
4. **R4 exact navigation.** Distinguish omitted from invalid explicit plan/version parameters. Invalid/foreign/missing/archived destinations get a controlled error, never a different decision. Share dirty-state protection with app navigation and history, not just internal workspace buttons. Preserve input and metadata on cancel; confirm loads the requested snapshot/version and updates URL coherently. Test reload/back/forward and switching between multiple plans/versions. Do not set latest inputs while only changing the version label.
5. **R5 persisted dashboard state.** Consume the actual due-review API or an equivalent typed authenticated persisted read. Remove `(plan as any).dueStatus` reliance. Refresh completion/defer state after save and after reload. Verify a single aged plan's due card changes correctly after keep and defer; do not create a new unrelated aged plan as substitute evidence. Include pending/error/retry and avoid displaying authoritative due status before the read succeeds.
6. **R6 provenance.** Preserve actual reviewed financial evidence date; don't automatically claim today. Where only goal.updatedAt is known, label it goal update date, not latest balance/transaction evidence. A name/notes edit must not silently refresh financial evidence. Show missing/stale evidence honestly, and keep review completion separate from data refresh.

## Repair verification BEFORE any next hosted run

The current run_c09_proofs.mjs is rejected: `modalVisible || true`, pass-on-missing-modal branches, unconditional revise/keyboard/stale flags, and invented contrast values must go. Search the whole harness for these patterns and inspect every required result producer, not just evaluator consumers. Record actual expected/observed status and state. Capture a missing target as FAILED. Do not lower small-text contrast from4.5 to3.0. Compute actual composited backgrounds; test unavailable/transparent/unsupported color failure instead of defaulting to a passing color.

Add negative tests that exercise collectors as well as evaluator: missing modal, missing badge, unchanged destination after confirm, draft lost on cancel, unexecuted revise action, stale warning absent, keyboard action not reaching target,3.1:1 small text, thrown step, failed report write and cleanup failure. A failed mandatory check must keep nonzero exit even if other observations pass. Reuse corrected C08 helpers with their negative tests; never copy the earlier rejected shortcuts. Keep local fixtures versus hosted evidence explicitly separate.

Preserve prior rejected report/claims under clearly labeled history, then generate a fresh report from observed actions. Do not claim the old report is repaired by editing JSON. Correct actual endpoint/column names in submissions/matrix and remove unsupported counts/contrast ratios. Include exact candidate and evidence SHAs from git and exact CI/deployment metadata.

## Hosted proof after local tests and any new migration authorization

Reuse existing private0600 ignored Clerk configuration, documented test emails/phones and real isolated sessions. Before creation verify preview deployment environment, exact SHA, Functions DB0dbad68e-7493-452f-8504-98d4c61ee5da and current schema. Production DB remains forbidden. Record exact synthetic IDs immediately, retain sessions through assertions, and do not print credentials/real records. Include the source of owner authorization for already-applied0007 plus execution/readback artifacts; never reapply it.

One prepared run must prove: A v1/v2 exact restoration; actual dirty cancel/confirm and malformed/missing errors; B denial with A unchanged; server-time eligibility and keep/revise/defer outcomes; exact replay and conflicting intent; dashboard persistence after each outcome; actual provenance/stale warning; export inclusion; no mutation of immutable versions. Backdating only manifest-scoped synthetic setup records must be explicit, never presented as real elapsed retention. Do not alter stored dates merely to force a expected screenshot/result.

Use true theme toggles/state and representative computed colors, actual keyboard Tab/Enter actions, visible focus, local deterministic error/retry tests, and screenshots of changed controls at desktop/tablet/mobile320px. Verify reduced motion/transparency and composited contrast. Native200% zoom/readers stay deferred to B31/C11. Missing mandatory proof fails; it is never replaced by `true` or a fabricated ratio.

Finally delete app data for exact manifest users; check all15 relevant user tables (and any added table), tombstones, then gated provider deletion and actual404 absence. Scope every count by user. Preserve partial evidence and recovery manifest if cleanup fails. Do not delete identity before verifying application cleanup.

## Submit

Run meaningful focused regressions and ./scripts/test_all.sh before pushing. Obtain green exact-code CI, deploy isolated preview after tests/authorized migration, verify binding, run all public-route smoke checks and affected combined B10/B11/B28 proofs. No full unrelated audit. If the same diagnosed blocker recurs after one focused retry, report precise cause and remaining cleanup rather than looping or weakening assertions.

Prepare corrected submissions/B10.md, B11.md, B28.md, compact progress, assertion matrix, raw sanitized evidence, negative-test/full-suite logs, authorization/migration artifacts and cleanup. All three use the same final candidate/preview. Mark ready_for_review only if every nondeferred criterion has actual proof, else blocked with exact reason. Leave backlog unchecked, C10 locked; primary owns acceptance. End with primary C09 review command and exact identifiers.
