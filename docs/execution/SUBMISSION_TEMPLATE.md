# Task BXX submission

Status: READY_FOR_REVIEW / BLOCKED (choose accurately)
Implementing session/owner:
Checkpoint:
Worktree absolute path:
Branch and PR URL:
Baseline commit:
Submitted code commit (full SHA):
Final checkpoint candidate SHA (refresh evidence after later edits):
Earlier provisional tasks affected/retested:
Evidence/documentation commit (if later):
Immutable preview URL and verified deployed code SHA:
CI URL for submitted code SHA:

## Problem and change

State user problem, observed baseline, implementation and why it satisfies the task. List files and affected previously accepted task IDs. Explain any divergence from the prompt; unapproved scope changes remain open.

## Acceptance evidence

| Criterion copied from task prompt | Pass / fail / blocked | Exact test, screenshot or reproduction evidence |
|---|---|---|
| Every criterion gets a separate row | | |

## Tests and review

- Failing behavioral test before fix (command, exit, relevant assertion), or CSS-only baseline evidence and explanation:
- Relevant green tests:
- `./scripts/test_all.sh` command/date/exit/counts and durable log:
- Audit if dependencies changed:
- `git diff --check`:
- CI on exact code SHA:
- Public-route HTTP smoke command/output:
- Actual changed browser journey, console errors and endpoint boundaries:

## UI evidence (N/A only with explanation for a non-UI task)

| Case | Actual route/state/theme/viewport | Method and durable before/after files | Result |
|---|---|---|---|
| Desktop 1440 and 1024 | | | |
| Tablet 768 | | | |
| Mobile 390 and 320 | | | |
| Light and dark | | | |
| Keyboard and focus | | | |
| Actual browser 200% zoom | | | |
| Screen-reader smoke | | | |
| Reduced motion/transparency | | | |
| Empty/loading/error/long values | | | |
| Contrast measured from effective pairs | | | |
| Chart truth/units and accessible equivalent | | | |
| Build size/performance where affected | | | |

## Security, privacy and data

Effective preview DB isolation proof before any write:
Synthetic identity/data only:
Schema migration and scoped authorization, if applicable:
Auth/tenancy/exports/deletion impact:
No secret or real-record exposure check:

## Rollback and remaining issues

Task-specific revert/disable plan, preserving safety/data contracts:
Open failures/blockers and exact missing owner input:
Next task within checkpoint, or checkpoint STOP for master review:

Implementation is submitted, not accepted. Do not check the backlog complete.
