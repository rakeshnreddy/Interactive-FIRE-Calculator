# B39 focused correction — Gemini Antigravity worker

Work in `/Users/Rakesh/Projects/Interactive-FIRE-Calculator` on `codex/finpath-quality-execution`. Baseline HEAD is `42780b4c5a46004e3d008bd1b6dafee6ba5bc501`. Read `GEMINI.md`, `docs/execution/prompts/B39.md`, `docs/execution/RESUME.md` and [Astra's B39 review](reviews/B39-first-review-2026-09-26.md). Preserve the complete uncommitted B39 extraction and all passing tests. Preserve both untracked `scripts/hosted_smoke*` files byte-for-byte for B42. Do not commit, push, deploy, run hosted writes, mark B39 done or release B42. This is a bounded correction, not a new architecture pass.

1. **Make parity evidence real and read-only.** Replace `src/appParity.test.tsx` so normal test execution never writes a fixture or report. Generate a baseline artifact from the actual Git object `42780b4` using a temporary `git archive`/clean directory and the same deterministic rendering/capture script used on the candidate; do not `git checkout` or reset the shared checkout. Include baseline SHA and capture command in the artifact. Capture the full deterministic SSR HTML or SHA-256 of full HTML, not just string length, for `/`, `/calculators/fire`, `/calculators/mortgage` and signed-out `/dashboard`. Drive route rendering through `App` for each path (direct component assertions may be additional but must be labeled component-only). Stabilize only truly volatile attributes with documented normalization. The checked-in parity test must read the immutable baseline fixture, render the current candidate, compare full normalized HTML hashes/bytes and meaningful landmarks, and fail under a deliberate changed text/DOM fixture. Do not hand-author `parityWithBefore` booleans. If an exact difference exists, show the before/after diff, explain whether it is unintended and fix it within scope; do not quietly change the baseline.
2. **Prove responsive interaction locally.** With local browser tooling, compare baseline and candidate at normal desktop (e.g. 1440×900) and 375px for the four routes above in the same signed-out state. Record exact viewport, console/page errors, horizontal overflow, focus/keyboard operation of relevant controls, and any visible difference. Test one representative API/error UI state if practical. Screenshots are optional when unchanged; a concise machine-readable report plus reproducible command is required. If a capability is unavailable, label it `NOT RUN` with the exact missing capability and leave that part for Astra—never call SSR a browser pass. No remote hosted result from Gemini.
3. **Restore API parse-error parity.** For `readFinancialAccountResponse` and `readGoalResponse`, a malformed or empty *successful* JSON response must reject in the same way as baseline `await response.json()`; non-OK statuses must retain their existing generic messages, valid response validation must remain unchanged, and 409 plan conflict behavior must remain intact. Keep one typed `readApiJson` helper if useful, with focused tests for these exact cases. Do not expose response bodies or tokens. State precisely which call sites use the helper; remove the submission's claim that all repeated parsing is consolidated if that is not true.
4. **Repair the packet and rerun checks.** Update `docs/execution/submissions/B39.md` and `docs/execution/evidence/B39/` with verifiable baseline/candidate provenance, real HTML hashes or byte comparison, browser results or honest NOT RUN, and any intentional differences. Correct CR-8 and remove unsafe broad `git add src/ docs/` and `git checkout`/`rm -rf` advice. Run focused API/parity tests, `./scripts/test_all.sh` without Python, typecheck/build, `git diff --check`, and the governance validator separately. Record exact exit codes and preserve a small durable log; no generated bulk screenshots in Git. Confirm `src/App.tsx` remains ≤6,841 lines and `src/lib/fire.ts` untouched.

Stop after one consolidated handoff. List exact changed files, independent baseline command/SHA, parity hash outcomes, local browser cases, API malformed-200 behavior before/after, full-suite counts, and Astra-only publication/hosted steps. B39 stays `changes_requested` until Astra accepts it.
